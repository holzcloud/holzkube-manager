---
phase: 02-transport-seam-talossim-image-factory
plan: 31
type: execute
wave: 3
depends_on: ["02-29", "02-30"]
gap_closure: true

# gap_ids ist die maschinenlesbare Behauptung "dieser Plan bearbeitet diese Luecken".
#
# **Zwei Ids, und eine davon steht auch in Plan 02-30. Das ist Absicht und wird hier begruendet,
# statt implizit zu bleiben.**
#
#   G-02-29  ==  02-VERIFICATION.md Runde 6, gaps[0], die ANKER-Haelfte  (rundenlokal G6-1a)
#            Die Luecke hat eine Code-Haelfte und eine Register-Haelfte. Plan 02-30 traegt die
#            Code-Haelfte (die Behauptung des Waechters wird verengt und die Blindheit messbar),
#            dieser Plan die Register-Haelfte (der amendierende Ledger-Eintrag 72). **KEINE der
#            beiden schliesst sie** — der Eintrag existiert genau deshalb, weil sie offen bleibt.
#            Eine Ueberschneidung ist hier ehrlicher als eine Aufteilung, die so tut, als seien es
#            zwei Befunde.
#
#   G-02-30  ==  KEINE Verifikationsrunde. Vom Orchestrator dieser Runde gemessen und in keinem
#            anderen Dokument der Phase notiert: `requirements.revert-phase` ueberspringt still
#            Requirement-Zeilen, deren Id-Zelle Markup traegt. `| **TRANS-06** 🚫 | Phase 2 |
#            Complete |` wurde weder vom Revert der Runde 5 (`2e57ee3`) noch von dem dieser Runde
#            (`b7a1ab9`) zurueckgenommen, waehrend alle 13 undekorierten Phase-2-Ids es wurden.
#            Der einzige Release-Blocker dieser Phase ist damit die eine Zeile, die faelschlich
#            `Complete` liest, in einer Phase, deren Verifikation `gaps_found` sagt.
#
# Die Buchhaltungsregression selbst — Runde 6, gaps[1] — ist NICHT hier: der Betreiber hat sie
# bereits mit `b7a1ab9` (REQUIREMENTS.md) und `77ed0f3` (ROADMAP, STATE) geschlossen. Dieser Plan
# prueft nur nach, dass sie geschlossen geblieben ist, und plant sie nicht erneut.
#
# Naechste freie Nummer nach dieser Runde: G-02-31.
gap_ids: [G-02-29, G-02-30]
gap_id_mapping:
  - gap_id: G-02-29
    verification_round: 6
    round_local_label: G6-1a
    verification_status: partial
    review_finding: "WR-01 (Blockkommentar und Template-Literal erfuellen den Anker), IN-05 (Eintrag 70 zeigt auf eine stale Zeilennummer)"
    truth: "A guard reports a pass only when it measured the property it is named for"
    id_decision: "geteilt mit Plan 02-30 — dieser Plan traegt die Register-Haelfte (Ledger-Eintrag 72), 02-30 die Code-Haelfte"
    disposition: "NICHT GESCHLOSSEN. Ledger-Eintrag 70 bleibt `open` und bekommt den amendierenden Eintrag 72, der ebenfalls `open` bleibt und eine sechspunktige, pruefbare Schliessbedingung traegt."
  - gap_id: G-02-30
    verification_round: null
    round_local_label: null
    verification_status: null
    source: "vom Orchestrator der Runde 7 gemessen; in keinem Verifikations-, Review- oder Entscheidungsdokument dieser Phase notiert"
    truth: "Das Traceability-Register behauptet nur, was der Phasenstand traegt — auch fuer die Zeilen, die ein Werkzeug nicht erreicht"
    id_decision: "neue Id — ein neuer, unabhaengig gemessener Befund mit einer anderen Wurzel als die Buchhaltungsregression der Runde 6: dort schrieb ein Werkzeug zu viel, hier erreicht es zu wenig"

files_modified:
  - .planning/WINDOWS.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md
# `internal/imagefactory/guard_drift_test.go` steht bewusst NICHT hier: dieser Plan schreibt keinen
# Code. Er misst gegen den Stand, den die Plaene 02-29 und 02-30 hinterlassen haben, und traegt
# das Ergebnis in die beiden Register. `web/` wird nicht angefasst.
autonomous: true
requirements:
  [
    FOUND-12,
    TRANS-01,
    TRANS-02,
    TRANS-03,
    TRANS-04,
    TRANS-05,
    TRANS-06,
    TRANS-07,
    FACT-01,
    FACT-02,
    FACT-03,
    FACT-04,
    FACT-05,
    FACT-06,
  ]
# **Warum dieser Plan als einziger der Runde alle vierzehn traegt, und warum das keine Ueberdehnung
# ist.** Sein Task 2 prueft die Statuszeile JEDER dieser vierzehn Ids im Traceability-Register von
# `.planning/REQUIREMENTS.md` und korrigiert die eine, die falsch ist. Das ist eine reale,
# messbare Handlung an allen vierzehn Zeilen — nicht die Behauptung, dieser Plan liefere die
# vierzehn Requirements. **`requirements-completed` in der SUMMARY bleibt trotzdem LEER**, und
# zwar ausdruecklich: die Phase traegt `gaps_found`, und genau ein `requirements-completed`-Feld
# hat in Runde 6 die Regression ausgeloest, die der Betreiber mit `b7a1ab9` zurueckgenommen hat.

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/WINDOWS.md` traegt einen Eintrag 72, der in Grossbuchstaben mit `AMENDS ENTRY 70, WHICH STAYS OPEN (no amend verb exists)` eroeffnet, `open` bleibt und auf den Symbolnamen `refusedRangesDecl` zeigt statt auf eine Zeilennummer — die Zeilenspalte bleibt leer. Damit ist der stale Zeiger von Eintrag 70 auf Zeile 83 miterledigt (02-REVIEW IN-05), ohne dass 70 bearbeitet wird."
    - "Eintrag 70 bleibt woertlich stehen und bleibt `open`. Es gibt kein Aenderungs-Verb, und ein `fixed` an 70 waere formal dieselbe Bewegung, die 70 an 69 als Fehler benennt — obwohl 70s geschriebene Schliessbedingung von Runde 6 woertlich ausgefuehrt und in beiden Richtungen erfuellt wurde. Dass die geschriebene Bedingung und der tatsaechliche Offen-Grund auseinandergelaufen sind, ist ein echter Mangel, und er wird durch den Text von 72 behoben, nicht durch ein `fixed` an 70."
    - "Eintrag 72 traegt die vollstaendige, sechspunktige, pruefbare Schliessbedingung: (1) `REFUSED_RANGES` praefixiert umbenannt -> rot; (2) Deklaration nur hinter `//` -> rot; (3) ein Eintrag mit einer Grenze oberhalb `utf8.MaxRune` -> rot; (4) eine wirklich leere `= []` neben einer zweiten eintragsfoermigen Tabelle -> die Leer-Meldung; (5) die geschriebene Ausschlussliste wird gegen jede darin genannte Form einzeln gemessen und die Ausgabe im Protokoll zitiert; (6) die Runde versucht ausdruecklich, eine siebte Form zu bauen, die die Liste nicht nennt, und protokolliert das Ergebnis — findet sie eine, bleibt 72 offen und die Liste wird um sie erweitert."
    - "Eintrag 72 sagt ausdruecklich, dass die Richtung dieser Runde auf einer NICHT RATIFIZIERTEN Empfehlung beruht, dass Variante A punktgleich daneben liegt und dass der Verifier die Frage dem Betreiber zugewiesen hat. `overrides:` wird nirgends benutzt und `waived_count` bleibt 0 — das Ledger-Verb fuer bewusste Annahme heisst `waive`, und dieser Rest wird nicht angenommen, sondern offen gefuehrt."
    - "Eintrag 72 benennt die fuenf Reste, die auch nach dieser Runde bleiben, statt sie in Fussnoten zu verteilen: die Ableitung vom Literal zum Wert (gemessen gruen auf echter Drift, dieselbe Schwere wie G-02-11 selbst); die eine tragende Verbindung bei `images.tsx:159`; der Rest, den `strings.TrimSpace` in Plan 02-29 uebriglaesst; die drei Hinweiszeilen ueber `images.tsx:105`, die diese Runde bewusst NICHT geschrieben hat, samt Grund; und die 2046 nie verglichenen Codepoints im Inneren des Surrogat-Bereichs."
    - "`.planning/WINDOWS.md` traegt einen Eintrag 73 ueber den gemessenen Werkzeugdefekt: `requirements.revert-phase` vergleicht die Id-Zelle der Traceability-Zeile nach Trimmen und Kleinschreibung auf **exakte Gleichheit**, waehrend die Checkbox-Ersetzung derselben Funktion einen `**fett**`-Wrapper toleriert. Eine dekorierte Zeile ist damit fuer die eine Haelfte sichtbar und fuer die andere nicht. Der Eintrag nennt die 16 dekorierten Release-Blocker-Zeilen des ganzen Projekts, die davon betroffen sind, und bleibt `open`, weil der Defekt in der GSD-Laufzeit und nicht in diesem Repository liegt."
    - "Alle vierzehn Phase-2-Zeilen im Traceability-Register lesen `Gaps Found`, und alle vierzehn Checkboxen sind leer. Die eine Zeile, die es nicht tat, ist von Hand korrigiert worden — das ist die Anwendung der Regel, die der Betreiber mit `2e57ee3 docs(phase-02): revert premature Complete requirements after gaps found` selbst ausgesprochen hat, und keine neue Entscheidung."
    - "`.planning/WINDOWS.md` traegt einen Eintrag 74 ueber die Geschwister-Leser, jeder von ihnen **einzeln gemessen und nicht aus dem Entscheidungsdokument uebernommen**. Sie stillschweigend stehen zu lassen waere dieselbe Operation, die Eintrag 69 falsch gemacht hat."
    - "Der Ledger ist in allen drei Repraesentationen selbstkonsistent und traegt danach 58 open / 0 waived / 16 fixed / 74 total, mit lueckenlosen und doppelfreien Ids 1..74. Unabhaengig vom Werkzeug nachgezaehlt, nicht nur `windows status` geglaubt."
    - "`02-DECISION-drift-guard-lexik.md` bekommt einen datierten Nachtrag, der ANGEHAENGT und nicht eingearbeitet wird, in der Form, die `02-DECISION-probe-budget.md` vormacht: was Runde 7 auf dieser Grundlage geplant und ausgefuehrt hat, welcher Plan welchen Teil traegt, dass der Statusblock unveraendert `offen — vorgelegt, nicht ratifiziert` bleibt, und die Byte-Integritaet des entschiedenen Bereichs als Praefix-Hash."
  prohibitions:
    - statement: "`.planning/WINDOWS.md` nicht von Hand bearbeiten — weder die Markdown-Tabelle noch den JSON-Block noch die Frontmatter-Zaehler. Nur `gsd-tools windows append`, `windows fixed` und `windows status`. Drei Repraesentationen muessen im Gleichschritt bleiben, und nur das Werkzeug haelt sie so. **Es gibt kein Aenderungs-Verb**: eine Behauptung, die sich verschoben hat, bekommt einen neuen Eintrag, der in Grossbuchstaben eroeffnet, was er ueberholt — nie eine bearbeitete Zeile."
      status: unresolved
    - statement: "Kein `windows fixed` in dieser Runde. Weder 70 noch 56 noch 58 noch 66 werden geschlossen, und auch 72, 73 und 74 werden bei der Anlage NICHT als `fixed` markiert. Ein bei der Anlage geschlossener Eintrag ueber eine allquantifizierte Eigenschaft war der Fehler von 69, und er ist mechanisch: `workflow.windows_enforce` blockiert `/gsd-ship`, solange `open_count > 0`, und ein `fixed`-Eintrag ist an dieser Grenze unsichtbar."
      status: unresolved
    - statement: "`overrides:` wird nirgends benutzt, und `waived_count` bleibt 0. Beide Verifikationen tragen `overrides_applied: 0`, die einzige Schema-Instanz steht ungehoben in einem Fence bei `01-VERIFICATION.md:476-486`, und das Ledger-Verb fuer bewusste Annahme heisst `waive`. Dieser Rest wird nicht angenommen, sondern offen gefuehrt — das erfuellt `02-VERIFICATION.md:436` auf der ersten Haelfte seiner Disjunktion."
      status: unresolved
    - statement: "Der Statusblock von `02-DECISION-drift-guard-lexik.md` wird NICHT auf `ratifiziert` gesetzt und der entschiedene Bereich des Dokuments wird nicht eingearbeitet. Es hat kein Betreiber ratifiziert; ein Nachtrag wird angehaengt. `02-DECISION-schematic-identity.md` traegt seit Runde 3 unveraendert `This decision is therefore self-resolved, not ratified.`, und jede Verifikationsrunde seither hat den Praefix-Hash nachgemessen."
      status: unresolved
    - statement: "In `.planning/REQUIREMENTS.md` wird ausschliesslich die eine Statuszelle der TRANS-06-Zeile geaendert. Keine andere Zeile, keine Checkbox, kein Requirement-Text, keine Ueberschrift. Insbesondere wird **keine** Zeile auf `Complete` gehoben, solange die Phase `gaps_found` traegt."
      status: unresolved
    - statement: "`requirements-completed` in der SUMMARY bleibt LEER, obwohl dieser Plan alle vierzehn Ids in `requirements` fuehrt. Die beiden sind verschiedene Behauptungen: `requirements` sagt, welche Zeilen dieser Plan anfasst, `requirements-completed` sagt, welche er abschliesst. Die Verwechslung der beiden ist die Wurzel der Regression aus Runde 6."
      status: unresolved
    - statement: "Kein Code. Dieser Plan schreibt keine Datei unter `internal/`, `cmd/` oder `web/`. Er misst gegen den Stand, den 02-29 und 02-30 hinterlassen haben, und traegt das Ergebnis in die Register. `git diff --quiet HEAD -- internal/ cmd/ web/` muss ueber den ganzen Plan gruen sein."
      status: unresolved
    - statement: "**G-02-9 bleibt offen**, und die Eintraege 58 und 66 tragen ihn unberuehrt. Eintrag 56 — dass die SERVER-Menge nicht erschoepfend gegen `factory.talos.dev` gemessen ist und 02-14s Extrapolation erbt — bleibt daneben offen und wird von Eintrag 72 ausdruecklich NICHT ueberholt. Beides gehoert woertlich in den Text von 72."
      status: unresolved
    - statement: "Der Werkzeugdefekt hinter G-02-30 wird NICHT im GSD-Laufzeitverzeichnis repariert. `~/.claude/gsd-core/` liegt ausserhalb dieses Repositories, ist nicht versioniert und gehoert nicht zum Umfang dieser Phase. Gemessen, geledgert und die Zeile im Repository korrigiert — mehr nicht."
      status: unresolved
  artifacts:
    - path: ".planning/WINDOWS.md"
      provides: "Eintrag 72 (amendiert 70, bleibt offen, sechspunktige Schliessbedingung), Eintrag 73 (der gemessene Werkzeugdefekt an `requirements.revert-phase`) und Eintrag 74 (die drei Geschwister-Leser, je einzeln gemessen)"
      contains: "AMENDS ENTRY 70, WHICH STAYS OPEN"
    - path: ".planning/REQUIREMENTS.md"
      provides: "vierzehn Phase-2-Zeilen, die alle `Gaps Found` lesen — die eine korrigiert, die ein Werkzeug nicht erreichen konnte"
      contains: "Gaps Found"
    - path: ".planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md"
      provides: "einen angehaengten, datierten Nachtrag, der festhaelt, was Runde 7 auf einer nicht ratifizierten Empfehlung ausgefuehrt hat, und die Byte-Integritaet des entschiedenen Bereichs"
      contains: "Nachtrag"
  key_links:
    - from: ".planning/WINDOWS.md Eintrag 72"
      to: "internal/imagefactory/guard_drift_test.go"
      via: "die Schliessbedingung des Eintrags ist am Code ausfuehrbar — sechs Punkte, von denen vier gegen die ausgelieferten Tabellen laufen und zwei (die Ausschlussliste und der Versuch einer siebten Form) gegen `guardBlindSpots` und den Punkt-6-Lauf aus Plan 02-30"
      pattern: "refusedRangesDecl"
    - from: ".planning/WINDOWS.md Eintrag 73"
      to: ".planning/REQUIREMENTS.md"
      via: "der Eintrag beschreibt, warum die Werkzeugkette genau eine der vierzehn Zeilen nicht erreicht hat, und die Korrektur dieser Zeile ist die Handlung, die er begleitet. Ohne den Eintrag waere die Handkorrektur eine unbelegte Aenderung an einem Register"
      pattern: "TRANS-06"
    - from: ".planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md"
      to: ".planning/WINDOWS.md Eintrag 72"
      via: "die Ratifikation gehoert laut Entscheidungsdokument in seinen Statusblock UND in den Text von Eintrag 72. Sie ist nicht erfolgt, also traegt der Eintrag ihr Ausbleiben — und der Nachtrag am Dokument haelt fest, was ohne sie ausgefuehrt wurde"
      pattern: "nicht ratifiziert"
---

<objective>
Die beiden Register sagen, was diese Runde getan hat — und was sie nicht getan hat.

Purpose: Runde 7 hat den Anspruch des Waechters verengt und drei Diagnosen begradigt. Sie hat den
**Defekt nicht behoben**: eine `REFUSED_RANGES`-Deklaration, die nur in einem Kommentar, einem
Template-Literal oder hinter einer Ableitung lebt, wird weiterhin gelesen, und der Waechter meldet
Zustimmung ueber eine Tabelle, die das Formular nicht benutzt. Genau das gehoert in den Ledger, und
zwar in der Form, die diese Phase sich erarbeitet hat: ein neuer Eintrag, der in Grossbuchstaben
eroeffnet, was er ueberholt, den alten offen stehen laesst, `open` bleibt und sich eine **pruefbare**
Schliessbedingung gibt.

**Ledger-Eintrag 70 wird von dieser Runde nicht geschlossen, und das ist die schwierigste Aussage
dieses Plans.** Seine geschriebene Schliessbedingung ist von Runde 6 woertlich ausgefuehrt und in
beiden Richtungen erfuellt worden. Ein `fixed` waere trotzdem falsch: 70 existiert nur, weil 69 bei
der Anlage `fixed` gesetzt wurde, waehrend die allquantifizierte Eigenschaft weiterlebte, und 70
nennt das ausdruecklich *"genau der Fehler von 69"* und mechanisch statt stilistisch. Dass die
geschriebene Bedingung und der tatsaechliche Offen-Grund auseinandergelaufen sind, ist ein echter
Mangel — er wird durch den **Text** von 72 behoben und nicht durch ein `fixed` an 70.

**Ein zweiter Befund, den kein Verifikations-, Review- oder Entscheidungsdokument dieser Phase
kennt.** Der Orchestrator dieser Runde hat gemessen: `requirements.revert-phase` ueberspringt still
Requirement-Zeilen, deren Id-Zelle Markup traegt. Die Ursache ist im Werkzeug lesbar — die
Traceability-Haelfte der Funktion vergleicht die erste Zelle der Zeile nach Trimmen und
Kleinschreibung auf **exakte Gleichheit** mit der Id, waehrend die Checkbox-Haelfte derselben
Funktion einen `**fett**`-Wrapper ausdruecklich toleriert. Folge: `| **TRANS-06** 🚫 | Phase 2 |
Complete |` ist weder vom Revert der Runde 5 noch von dem dieser Runde erreicht worden, waehrend alle
13 undekorierten Phase-2-Ids es wurden. **Der einzige Release-Blocker dieser Phase ist damit die eine
Zeile, die faelschlich `Complete` liest, in einer Phase, deren Verifikation `gaps_found` sagt** — und
das ist genau die Aussage, die der Betreiber am 2026-09-04 mit `2e57ee3` von Hand zurueckgenommen
hat. Ein GSD-Werkzeugdefekt, der als falscher Projektzustand auftaucht. Er betrifft nicht nur diese
Zeile: das Register traegt **16** dekorierte Release-Blocker-Zeilen, und jede einzelne wird beim
naechsten `gaps_found` ihrer Phase denselben Weg nehmen.

**Und ein dritter, den das Entscheidungsdokument benennt und ausdruecklich nicht deckt.** Die
Geschwister-Leser dieses Bestands — `stringArrayLiteral` in derselben Datei, der Anker in
`internal/httpapi/handlers/budget_drift_test.go` und die beiden `strings.Contains`-Sweeps in
`internal/imagefactory/warnings_test.go` — tragen dieselbe Gattung Loch. Keine Variante fasst sie an.
*"Sie stillschweigend stehen zu lassen waere dieselbe Operation, die 69 falsch gemacht hat — sie
gehoeren in einen eigenen offenen Eintrag."* Dieser Plan misst jeden der drei **einzeln** und
schreibt auf, was gemessen wurde, nicht was das Entscheidungsdokument vermutet: fuer
`budget_drift_test.go` trifft dessen Vermutung beim Nachrechnen naemlich **nicht** zu, und eine
uebernommene Vermutung waere in einem Ledger genau die Sorte Behauptung, die dieser Ledger fuehrt.

Output: drei neue, offene Ledger-Eintraege mit einer Schliessbedingung, die etwas kosten kann; eine
korrigierte Statuszeile in einem Register, das ein Werkzeug nicht erreichen konnte; und ein
angehaengter Nachtrag an der Entscheidung, der festhaelt, dass sie ohne Ratifikation ausgefuehrt
wurde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-VERIFICATION.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-28-PLAN.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-28-SUMMARY.md
</context>

## Recorded decisions

**Die Richtung dieser Runde ist VORGELEGT, NICHT RATIFIZIERT.** Siehe `## Flagged Assumptions`.
Dieser Plan traegt das Ausbleiben der Ratifikation an zwei Stellen ein — in den Text von
Ledger-Eintrag 72 und in einen angehaengten Nachtrag am Entscheidungsdokument — und setzt den
Statusblock des Dokuments **nicht** auf ratifiziert.

**Der Werkzeugdefekt hinter G-02-30 wurde beim Planen an der Quelle gelesen, nicht erschlossen.**
`requirements revert-phase` ist in der GSD-Laufzeit unter `gsd-core/bin/lib/milestone.cjs` in
`cmdRequirementsRevertPhase` implementiert und hat zwei Wirkflaechen. Die erste ersetzt die Checkbox
und traegt den `**fett**`-Wrapper ausdruecklich in ihrem Muster. Die zweite sucht die Zeile des
Traceability-Registers ueber ein Praedikat, das die **erste Zelle** der Zeile nach Trimmen und
Kleinschreibung auf exakte Gleichheit mit der Id prueft. `**trans-06** 🚫` ist nicht `trans-06`, also
findet das Praedikat die Zeile nicht, meldet die Id unter `unchanged` und schreibt nichts. Die
Beobachtung im Repository passt dazu Zeile fuer Zeile: 13 undekorierte Phase-2-Ids stehen auf
`Gaps Found`, die eine dekorierte auf `Complete`.

**Der Ledger-Stand vor dieser Runde, unabhaengig vom Werkzeug gelesen:** 55 open / 0 waived / 16
fixed / 71 total, `"ok": true`, Ids 1..71 lueckenlos. Nach den drei Anlagen dieses Plans: **58 open /
0 waived / 16 fixed / 74 total**, Ids 1..74.

**Die `must_haves.prohibitions` sind descriptor-los** (kein `check_kind`, `check_target`,
`check_rule`, `check_violation_fixture`, `check_clean_fixture`) und disponieren damit als
**flagged-unverified**, nicht als auto-dismissed. Kein Kandidat wurde automatisch verworfen. Ein
Kandidat der Recall-Stufe ist als Kanon-Verweis fallengelassen und hier als Brotkrume vermerkt statt
gemuenzt: Pfad-Traversal beim Schreiben von `.planning/`-Pfaden aus einem Werkzeugaufruf heraus
(OWASP-Kanon; `gsd-tools windows` loest den Pfad selbst aus der Projektwurzel auf und nimmt ihn nicht
entgegen).

## API coverage

`.planning/phases/02-transport-seam-talossim-image-factory/COVERAGE.md` traegt die Matrix dieser
Phase und ist von diesem Plan unberuehrt. Dieser Plan aendert zwei Register und ein
Entscheidungsdokument; er spricht mit keinem externen Dienst.

<assumption_delta_decision>
- **Primaeres Substantiv:** die **Ledger-Zeile** als Traeger einer Aussage ueber den Code. Dieser
  Plan aendert daran nichts: 72 amendiert 70 als neuer Eintrag, wie 66 es mit 58 und 70 es mit 69
  getan haben.
- **Entscheidung:** `no-change`. Was sich in dieser Runde pluralisiert, ist die Zahl der Register,
  die falsch liegen koennen — und die Antwort darauf ist ein Eintrag, kein neues Modell.
- Ein Begleit-Invariantentest waere hier ein Register-Konsistenztest ueber
  `.planning/REQUIREMENTS.md` gegen den Phasenstatus. Er ist **nicht** Teil dieses Plans: er gehoert
  in die GSD-Laufzeit, in der der Defekt sitzt, und ein projektlokaler Nachbau waere eine zweite
  Transkription — woertlich die Gattung, die diese Phase seit Runde 3 abbaut. Als Beobachtung
  vermerkt, nicht als Aufgabe.
</assumption_delta_decision>

## Flagged Assumptions

### 1. Die Richtung dieser Runde ist nicht ratifiziert

`02-DECISION-drift-guard-lexik.md` traegt im Statusblock `offen — vorgelegt, nicht ratifiziert`,
wurde von einem autonomen Lauf am 2026-09-05 erarbeitet und empfiehlt Variante B; **Variante A liegt
punktgleich daneben, 7,0 gegen 7,0.** Der Verifier hat die Frage ausdruecklich dem Betreiber
zugewiesen (`02-VERIFICATION.md` Human-Verification-Punkt 1), und der Code-Review ordnet begruendet
anders. Diese Phase hat den Praezedenzfall in beide Richtungen: `02-DECISION-probe-budget.md` wurde
in Sitzung ratifiziert und Runde 4 war bis dahin blockiert; `02-DECISION-schematic-identity.md` wurde
vom Ausfuehrenden selbst aufgeloest und traegt das seither im Text.

**Was das fuer diesen Plan bedeutet.** Task 1 und der Nachtrag haengen an der Richtung: waehlt der
Betreiber Variante A, beschreibt Eintrag 72 einen anderen Stand, und die Abschnitte ueber die
Ausschlussliste entfallen zugunsten einer Beschreibung dessen, was der Lexer schliesst und was nicht.
Task 2 (der Werkzeugdefekt und die Requirement-Zeile) und Task 3 (die Geschwister-Leser) sind von der
Richtung **unabhaengig** und blieben unveraendert. Weil Eintrag 72 erst in Welle 3 geschrieben wird,
liegt der Umschaltpunkt weiterhin vor der ersten unumkehrbaren Handlung — und ein angelegter
Ledger-Eintrag ist die erste unumkehrbare Handlung dieser Runde, weil `gsd-tools windows` kein
Loesch- und kein Aenderungs-Verb kennt.

### 2. Der spezifikationslose Probe-Fallback lief und hat nichts klassifiziert

Diese Phase hat keine `SPEC.md`, also `EDGE_ABSENT=1` und `PROHIB_ABSENT=1`. Die deterministische
Edge-Probe lief ueber alle vierzehn Requirement-Ids und lieferte
`{"applicable":14,"resolved":0,"unresolved":14,"byVerification":{"explicit":0,"backstop":0}}` — jede
Zeile `unresolved`, dreizehn davon `category: unclassified` mit `probe: "unclassified — review
manually"`. **Es wurde nichts klassifiziert und nichts aufgeloest.** 14 == 0 uebernommene + 14 hier
sichtbar gemachte. **Keine stille Auslassung, keine automatische Verwerfung.**

| requirement | category | status | Disposition in diesem Plan |
|---|---|---|---|
| FOUND-12 | unclassified | unresolved | Task 2 prueft die Statuszelle dieser Zeile auf `Gaps Found`; sonst von dieser Runde unberuehrt |
| TRANS-01 | concurrency | unresolved | wie oben. **Nicht** auf `backstop` promoviert — dieser Plan fasst die Transport-Naht nicht an, und ein Backstop-Marker waere eine Zusicherung, die kein Task einloest |
| TRANS-02 | unclassified | unresolved | wie FOUND-12 |
| TRANS-03 | unclassified | unresolved | wie FOUND-12 |
| TRANS-04 | unclassified | unresolved | wie FOUND-12 |
| TRANS-05 | unclassified | unresolved | wie FOUND-12; UI-Haelfte nach Phase 3 verschoben |
| TRANS-06 | unclassified | unresolved | **live** — dies ist die eine Zeile, die faelschlich `Complete` liest und von Task 2 korrigiert wird. Ihre Korrektur ist die Anwendung der Regel aus `2e57ee3` und keine neue Entscheidung |
| TRANS-07 | unclassified | unresolved | wie FOUND-12 |
| FACT-01 | unclassified | unresolved | wie FOUND-12; die Waechter-Luecke, an der sie haengt, ist Eintrag 72 |
| FACT-02 | unclassified | unresolved | wie FOUND-12 |
| FACT-03 | unclassified | unresolved | wie FOUND-12 |
| FACT-04 | unclassified | unresolved | wie FOUND-12 |
| FACT-05 | unclassified | unresolved | wie FOUND-12 |
| FACT-06 | unclassified | unresolved | wie FOUND-12; die Waechter-Luecke, an der sie haengt, ist Eintrag 72 |

### 3. Die Policy-Liste des Betreibers wird von dieser Runde kuerzer, nicht laenger — und nicht von diesem Plan entschieden

`02-VERIFICATION.md` Human-Verification-Punkt 6 verlangt eine Policy-Entscheidung des Betreibers
darueber, wo die Nicht-Gap-Befunde der Code-Reviews leben. Diese Runde behebt WR-03, WR-04 und WR-05
(Plan 02-29) und erledigt IN-05 (Eintrag 72 zeigt auf ein Symbol) und IN-01 (die neue
Abschneide-Meldung traegt keine Zahl mehr) als Nebenwirkung. **Sie entscheidet die Policy nicht** und
legt fuer die uebrigen Punkte — WR-06, IN-02, IN-03, IN-04, der Surrogate-Punkttest und die vier
Warnungen der Vorvorrunde — **keine** Eintraege an. Das waere die Entscheidung, die dem Betreiber
gehoert. Die SUMMARY haelt fest, welche fuenf Punkte von der Liste verschwinden und warum, damit die
Entscheidung ueber einen kuerzeren Rest faellt.

<tasks>

<task type="tracer">
  <name>Task 1: Eintrag 72 — der amendierende Eintrag mit einer Schliessbedingung, die etwas kosten kann, und der Nachtrag, der das Ausbleiben der Ratifikation festhaelt</name>
  <reversibility rating="costly">Ein angelegter Ledger-Eintrag laesst sich nicht zurueckziehen: `gsd-tools windows` kennt `append`, `waive` und `fixed` und kein Loesch- oder Aenderungs-Verb. Eine Korrektur ist nur als weiterer, ueberholender Eintrag moeglich — genau die Mechanik, die dieser Task benutzt. Deshalb muss der Wortlaut vor dem `append` stimmen; danach ist er Teil der Aufzeichnung. Kein `one-way`: der Eintrag ist ueberholbar und markierbar, und es entsteht kein irreversibler Zustand ausserhalb der Datei.</reversibility>
  <precondition>Die Plaene 02-29 und 02-30 sind ausgefuehrt und committet, denn dieser Eintrag beschreibt, was sie geliefert haben, und eine Beschreibung eines nicht ausgefuehrten Standes waere die naechste unbelegte Behauptung. Pruefen mit `go test ./internal/imagefactory/... -count=1` (erwartet Exit 0) und `go test ./internal/imagefactory -run '^TestGuardBlindSpotsAreEachMeasured$' -count=1` (erwartet Exit 0). Ausserdem muessen die SUMMARYs von 02-29 und 02-30 vorliegen, weil ihre gemessenen Ausgaben in den Text von 72 eingehen.</precondition>
  <read_first>
    - `.planning/WINDOWS.md`, **Eintrag 70 vollstaendig** — er ist die Form, an der sich 72 misst:
      die Provenienz-Klammer, die Grossbuchstaben-Formel in der ersten Zeile, die
      Kapitaelchen-Abschnitte (`WAS UEBERHOLT IST`, `WAS AN 69 WEITERHIN GILT`, `WAS RUNDE 6 GETAN
      HAT`, `WARUM DIESER EINTRAG OPEN BLEIBT UND WAS IHN SCHLIESST`, `DIE NACHBARSCHAFT`), und der
      Schlusssatz ueber G-02-9 in Grossbuchstaben. **Sein Zeilenfeld traegt 83 — eine Zahl von vor
      Plan 02-27, die heute im Doc-Kommentar liegt.**
    - `.planning/WINDOWS.md`, **Eintrag 66 vollstaendig** — der Praezedenzfall fuer das amendierende
      Verb: `AMENDS ENTRY 58, WHICH STAYS OPEN (no amend verb exists)`. 70 benutzt `SUPERSEDES … WHICH
      STAYS FIXED`, weil 69 `fixed` war; 72 amendiert einen Eintrag, der `open` ist, und nimmt
      deshalb 66s Formel.
    - `.planning/WINDOWS.md`, Eintraege 56, 58 und 69 — die Nachbarschaft, die 72 benennen muss.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md`,
      **vollstaendig**. Insbesondere die Abschnitte "Was die Empfehlung fuer WINDOWS-Eintrag 70
      bedeutet", "Die Schliessbedingung von Eintrag 72, pruefbar und endlich" (die sechs Punkte
      woertlich), "Was keine der vier Varianten schliesst" (die Reste), "Die staerksten Einwaende
      gegen diese Empfehlung" und "Ratifikation".
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-probe-budget.md`, die
      letzten Absaetze — die Form eines angehaengten Nachtrags mit Byte-Integritaet des entschiedenen
      Bereichs. Diese Form wird uebernommen.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-29-SUMMARY.md` und
      `02-30-SUMMARY.md` — die gemessenen Ausgaben dieser Runde. **Sie sind die Quelle fuer den
      Abschnitt `WAS RUNDE 7 GETAN HAT`; aus diesem Plan abzuschreiben waere eine Behauptung ueber
      einen Lauf, den dieser Plan nicht gesehen hat.**
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-28-PLAN.md`, Task 2 — die
      Aufrufform von `gsd-tools windows append`, die Regel gegen Pipe-Zeichen und Zeilenumbrueche im
      Beschreibungstext und die Begruendung dafuer.
  </read_first>
  <files>.planning/WINDOWS.md, .planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md</files>
  <action>
Den Ledger fuehren, ausschliesslich mit `gsd-tools windows append` und `gsd-tools windows status`.
Kein Handanlegen an der Markdown-Tabelle, am JSON-Block oder an den Frontmatter-Zaehlern. Die
Aufrufform ist `windows append --kind <kind> --phase 02 --file <pfad> --description "<text>"`. Lies
die vergebene id aus der Ausgabe von `append` und verwende genau diese, statt eine zu raten. In
diesem Task genau **ein** Aufruf von `append` und **kein** `fixed`.

**Kein Beschreibungstext darf ein Pipe-Zeichen oder einen echten Zeilenumbruch enthalten.** Beide
brechen dieselbe Sache: die Markdown-Repraesentation ist pipe-getrennt und zeilenweise, und die
Konsistenzpruefung liest den Status positionsbasiert als achtes Feld der Zeile. Schreib den Eintrag
als einen durchgehenden Absatz mit Satzzeichen, so wie es die Eintraege 66 bis 71 tun, und uebergib
`--description` als **ein** Argument in einfachen Anfuehrungszeichen ohne eingebettete Umbrueche.

**Eintrag 72.** `--kind unmet-truth`, `--file internal/imagefactory/guard_drift_test.go`, **und
ausdruecklich OHNE `--line`.** Die leere Zeilenspalte ist keine Nachlaessigkeit, sondern die
haltbare Variante, die 02-REVIEW IN-05 verlangt: der Eintrag zeigt auf den **Symbolnamen**
`refusedRangesDecl`, und damit ist 70s stale Zeiger auf Zeile 83 miterledigt, ohne 70 zu bearbeiten.
Der Text sagt das in einem eigenen Satz, weil eine leere Spalte fuer sich genommen wie ein Versehen
aussieht.

Der Text eroeffnet mit der Provenienz-Klammer `[from 02-31]` und unmittelbar danach in
Grossbuchstaben `AMENDS ENTRY 70, WHICH STAYS OPEN (no amend verb exists)`. Die Klammer gehoert zur
Form: `append` reicht die Beschreibung unveraendert durch und stellt nichts voran, also ist sie das
Einzige, was den Eintrag seinem Plan zuordnet. Danach in dieser Reihenfolge:

- **WAS UEBERHOLT IST.** 70 traegt eine Schliessbedingung, die von Runde 6 woertlich ausgefuehrt und
  in beiden Richtungen erfuellt wurde — und der Eintrag steht trotzdem weiter offen, weil sein
  tatsaechlicher Offen-Grund ein anderer ist als seine geschriebene Bedingung. **Diese Divergenz ist
  es, die 72 ueberholt**, nicht die Messungen von 70. Der neue Grund ist gemessen und benannt: der
  Anker ist ein regulaerer Ausdruck ueber Quelltext und kennt keinen lexikalischen Kontext, und er
  bindet an ein Literal statt an einen Wert. Die Messwerte aus `02-30-SUMMARY.md` woertlich
  uebernehmen — je Form Bereichszahl und Fehlerzustand.
- **WAS AN 70 WEITERHIN GILT.** Die Beschreibung des unverankerten und des praefix-durchlaessigen
  Zustands, die sechs Runde-5-Messwerte, die vier Korrekturen der Runde 6 mit ihren Commits, und die
  Feststellung, dass nichts maskiert ist — die beiden Ablehnungsmengen stimmen ueberein, und das
  Loeschen des U+FEFF-Eintrags laesst den Waechter scheitern. Diese Saetze bleiben richtig und werden
  nicht ueberholt.
- **WAS RUNDE 7 GETAN HAT.** Aus den beiden SUMMARYs, nicht aus diesem Plan: der Spiegel-Defekt ist
  ueber die Rumpfform beseitigt statt getauscht; Ueberlauf und Inversion sind zwei Ursachen; keine
  Tabellenzeile prueft mehr eine Zeichenkette, die jeder Fehlerausgang traegt; der Waechter verlangt
  genau eine Deklaration; der Doc-Kommentar steht unter seinem Symbol; die allquantifizierte
  Behauptung ist aus Doc-Kommentar und Fehlertext verschwunden und durch eine Aussage ueber den Text
  plus eine als Daten gefuehrte Ausschlussliste ersetzt; die Blindheit ist nach Mechanismus skopiert
  und ueber den Live-Lesepfad gemessen; Punkt 6 wurde einmal ausgefuehrt und hat beim Planen die
  Doppel-Deklaration gefunden, die Plan 02-29 geschlossen hat.
- **WARUM DIESER EINTRAG OPEN BLEIBT UND WAS IHN SCHLIESST.** Er bleibt offen, weil die Verengung die
  **Behauptung** auf null Ueberhang bringt und den **Defekt** nicht beseitigt: die Leiche bleibt
  lesbar, der Waechter bleibt gruen darueber, und der Zeitpunkt, an dem das schadet, ist unveraendert.
  Danach die sechs Punkte der Schliessbedingung woertlich, mit Punkt 6 und mit dem Satz, der ihn
  traegt — ein Eintrag, dessen Bedingung nur die schon bekannten Formen abfragt, waere ein Eintrag,
  der sich selbst schliesst.
- **DIE RATIFIKATION FEHLT.** Ausdruecklich: die Richtung dieser Runde beruht auf einer Empfehlung,
  die im Statusblock ihres eigenen Dokuments `offen — vorgelegt, nicht ratifiziert` traegt, von einem
  autonomen Lauf erarbeitet wurde, gegen eine punktgleiche Alternative gewonnen hat und vom Verifier
  ausdruecklich dem Betreiber zugewiesen worden war. Dazu der Satz, was ein Wechsel auf Variante A
  kostet. **`overrides:` wird nicht benutzt und nichts wird gewaiv't** — dieser Rest wird nicht
  angenommen, sondern offen gefuehrt.
- **WAS AUCH NACH DIESER RUNDE BLEIBT.** Fuenf benannte Reste: (1) die Ableitung vom Literal zum
  Wert, gemessen gruen auf echter Drift und damit von derselben Schwere wie G-02-11 selbst; (2) die
  eine tragende Verbindung — `images.tsx` hat genau einen lebenden Aufruf, der die Tabelle benutzt,
  und faellt er aus dem Validierungspfad, ist die Tabelle eine uebereinstimmende Leiche; (3) der
  Rest, den `strings.TrimSpace` in Plan 02-29 uebriglaesst, naemlich dass ein Rumpf aus reinem
  Kommentar den Abschneide-Zweig nimmt — kleiner als der Defekt, den er ersetzt, weil die Meldung
  ihren Beleg zeigt, aber dieselbe Gattung, und von einer Tabellenzeile festgenagelt; (4) die drei
  Hinweiszeilen ueber der Deklaration in `web/src/routes/images.tsx`, die der Entwurf von Variante B
  vorsieht und die diese Runde **bewusst nicht geschrieben hat**, weil sie alles darunter um drei
  Zeilen verschoeben und rund ein Dutzend Zeilenverweise im Planungsbestand ungueltig machten — damit
  fehlt der Verengung ihr einziges Geraet, das den Menschen erreicht, der die Leiche erzeugt; (5) die
  2046 nie verglichenen Codepoints im Inneren des Surrogat-Bereichs, deren Server-Zwilling von hier
  nie aufgerufen wird.
- **DIE NACHBARSCHAFT.** Eintrag 56 bleibt daneben offen und traegt eine andere Aussage — die
  SERVER-Menge hinter dem Sweep ist nicht erschoepfend gemessen und erbt 02-14s Extrapolation —, und
  er wird von diesem Eintrag ausdruecklich **nicht** ueberholt. Die Eintraege 58 und 66 tragen G-02-9
  und bleiben unberuehrt offen; **G-02-9 IST OFFEN.** Eintrag 69 bleibt `fixed` und wird nicht
  angefasst. Die Geschwister-Leser sind von diesem Eintrag **nicht** gedeckt und bekommen einen
  eigenen — nenne ihn beim Namen und verweise darauf.

**Danach der Nachtrag am Entscheidungsdokument.** An `02-DECISION-drift-guard-lexik.md` einen
datierten Abschnitt **anhaengen**, nicht einarbeiten, in der Form aus `02-DECISION-probe-budget.md`.
Er haelt fest: dass Runde 7 auf dieser Grundlage geplant und ausgefuehrt wurde, ohne dass ein
Betreiber ratifiziert hat; welcher Plan welchen Teil der Empfehlung traegt und welcher davon
richtungsunabhaengig ist; dass der Statusblock unveraendert `offen — vorgelegt, nicht ratifiziert`
bleibt; welche id Eintrag 72 bekommen hat; und die Byte-Integritaet des entschiedenen Bereichs als
Praefix-Hash — Byte-Zahl und Hash-Wert, mit dem Kommando, das beides erzeugt hat, damit eine
kuenftige Runde ihn nachmessen kann. **Der Statusblock selbst und jeder Absatz oberhalb des Nachtrags
bleiben byteweise unveraendert.**

**Zuletzt die Konsistenz pruefen, unabhaengig vom Werkzeug.** Frontmatter, Markdown-Tabelle und
JSON-Block einzeln parsen, die Status pro Id vergleichen und die Signatur gegen `56/0/16/72` halten;
zusaetzlich `windows status` fahren und `"ok": true` sehen. Und maschinell bestaetigen: 56, 58, 66 und
70 sind `open`, 69 ist `fixed`, und der Eintrag mit der Grossbuchstaben-Formel ist `open`.
  </action>
  <acceptance_criteria>
    - Genau ein `windows append` wurde abgesetzt und kein `windows fixed`; die SUMMARY nennt den Aufruf mit der zurueckgegebenen id
    - `.planning/WINDOWS.md` enthaelt die Teilzeichenkette `AMENDS ENTRY 70, WHICH STAYS OPEN (no amend verb exists)` — zweimal, weil Markdown-Tabelle und JSON-Block dieselbe Beschreibung tragen
    - Die Zeilenspalte des neuen Eintrags ist leer; sein Text nennt den Symbolnamen `refusedRangesDecl` und sagt, dass die leere Spalte Absicht ist und 70s Zeiger auf Zeile 83 damit erledigt
    - Der Text traegt alle sechs Punkte der Schliessbedingung, Punkt 6 eingeschlossen, und den Satz, dass ein Eintrag, dessen Bedingung nur die schon bekannten Formen abfragt, sich selbst schliesst
    - Der Text sagt ausdruecklich, dass die Richtung dieser Runde nicht ratifiziert ist und dass Variante A punktgleich lag
    - Der Text benennt alle fuenf verbleibenden Reste einzeln, die drei nicht geschriebenen Hinweiszeilen eingeschlossen
    - Der Text sagt in Grossbuchstaben, dass G-02-9 offen ist, und nennt 56, 58, 66 und 69 mit ihrem jeweiligen Status
    - Eintrag 70 ist woertlich unveraendert und steht weiterhin auf `open`; Eintrag 69 steht weiterhin auf `fixed`
    - Die drei Repraesentationen des Ledgers sind unabhaengig vom Werkzeug nachgezaehlt und tragen `56 open / 0 waived / 16 fixed / 72 total`, ids 1..72 lueckenlos und doppelfrei, null Status-Abweichungen pro Id; `windows status` meldet zusaetzlich `"ok": true`
    - `waived_count` ist 0, und in keinem Artefakt dieser Runde steht ein `overrides:`-Block
    - `02-DECISION-drift-guard-lexik.md` traegt einen datierten, **angehaengten** Nachtrag; sein Statusblock liest unveraendert `offen` und `nicht ratifiziert`; die SUMMARY nennt Byte-Zahl und Hash des entschiedenen Bereichs samt dem erzeugenden Kommando
    - `git diff --quiet HEAD -- internal/ cmd/ web/` ist gruen
  </acceptance_criteria>
  <verify>
    <automated>grep -Fc 'AMENDS ENTRY 70, WHICH STAYS OPEN (no amend verb exists)' .planning/WINDOWS.md | grep -qx '2'</automated>
    <fails_when>Exit 1, weil die Formel nicht genau zweimal vorkommt. Null heisst, der Eintrag wurde nicht angelegt oder eroeffnet mit einer anderen Formel — dann ist nicht maschinell feststellbar, welchen Eintrag er amendiert, und das war der Befund von Runde 5 an Eintrag 69. Eine andere Zahl als 2 heisst, dass Markdown-Tabelle und JSON-Block auseinanderlaufen oder dass jemand von Hand eine dritte Kopie eingefuegt hat.</fails_when>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status | grep -q '"ok": true'</automated>
    <fails_when>Exit 1, weil `windows status` nicht `"ok": true` meldet — die drei Repraesentationen des Ledgers sind dann nicht im Gleichschritt. Der wahrscheinlichste Weg dorthin ist ein Pipe-Zeichen oder ein Zeilenumbruch im Beschreibungstext: die Markdown-Zeile laeuft dann physisch ueber mehrere Dateizeilen, und das achte Feld der ersten ist nicht mehr der Status.</fails_when>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status --raw | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const l=JSON.parse(s).ledger;const e=l.entries;const by=i=>e.find(x=>x.id===i);const open=[56,58,66,70].every(i=>by(i)&&by(i).status==="open");const fixed=by(69)&&by(69).status==="fixed";const a=e.find(x=>/AMENDS ENTRY 70, WHICH STAYS OPEN/.test(x.description||""));const counts=l.open_count===56&&l.waived_count===0&&l.fixed_count===16&&l.total_count===72;process.exit(open&&fixed&&a&&a.status==="open"&&(a.line===null||a.line===undefined||a.line==="")&&counts?0:1)})'</automated>
    <fails_when>Exit 1. Genau sechs Bedingungen fuehren dorthin, und jede einzelne ist ein eigener Defekt: 56, 58, 66 oder 70 ist nicht mehr `open` (eine Luecke waere still geschlossen worden); 69 ist nicht mehr `fixed` (ein Eintrag waere bearbeitet worden, wo es kein Aenderungs-Verb gibt); der amendierende Eintrag fehlt; er ist bei der Anlage `fixed` gesetzt worden (der Fehler von 69, mechanisch: `windows_enforce` sieht ihn dann beim Ship nicht); seine Zeilenspalte ist gefuellt (IN-05 waere unerledigt); oder die Zaehler stehen nicht auf `56/0/16/72`.</fails_when>
    <automated>grep -c 'nicht ratifiziert' .planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md | grep -qv '^0$'</automated>
    <fails_when>Exit 1, weil das Dokument die Formulierung nicht mehr traegt. Das hiesse, der Statusblock wurde entgegen der ausdruecklichen Prohibition auf ratifiziert gesetzt — eine Ratifikation, die kein Betreiber ausgesprochen hat, und damit exakt die Gattung Behauptung, die diese Phase seit Runde 3 abbaut.</fails_when>
    <automated>git diff --quiet HEAD -- internal/ cmd/ web/</automated>
    <fails_when>Exit 1, weil ein Unterschied unter `internal/`, `cmd/` oder `web/` im Baum steht. Dieser Plan schreibt keinen Code; ein Diff dort heisst, dass eine Register-Aussage an den Code angepasst wurde statt umgekehrt.</fails_when>
  </verify>
  <done>Eintrag 72 steht als offener, amendierender Eintrag ohne Zeilennummer im Ledger, traegt alle sechs Punkte seiner Schliessbedingung samt Punkt 6, benennt die fuenf verbleibenden Reste und das Ausbleiben der Ratifikation; Eintrag 70 ist unveraendert offen; und der Nachtrag am Entscheidungsdokument ist angehaengt, nicht eingearbeitet.</done>
</task>

<task type="auto">
  <name>Task 2: Der Werkzeugdefekt, gemessen statt geglaubt — und die eine Requirement-Zeile, die kein Werkzeug erreicht hat</name>
  <reversibility rating="costly">Der Ledger-Eintrag ist nicht zurueckziehbar (siehe Task 1). Die Korrektur der Statuszelle in `.planning/REQUIREMENTS.md` ist dagegen reversibel: eine Zelle, ein Wort, und die Regel dahinter hat der Betreiber mit `2e57ee3` selbst ausgesprochen.</reversibility>
  <precondition>Der Ledger-Eintrag aus Task 1 existiert und seine id ist bekannt, denn Eintrag 73 verweist auf die Runde und ihre Nachbarschaft. Pruefen mit `grep -c 'AMENDS ENTRY 70' .planning/WINDOWS.md` — erwartet 2 — und die id aus `windows status` lesen.</precondition>
  <read_first>
    - `.planning/REQUIREMENTS.md`, das Traceability-Register vollstaendig — **insbesondere die
      vierzehn Phase-2-Zeilen** und die Beobachtung, die den Befund traegt: dreizehn lesen
      `Gaps Found`, eine liest `Complete`, und genau diese eine traegt Markup in ihrer Id-Zelle.
      Ebenso die Checkbox-Liste weiter oben, in der dieselbe Id undekoriert erscheint.
    - `.planning/REQUIREMENTS.md`, das Register als Ganzes — die Zahl der dekorierten
      Release-Blocker-Zeilen ueber alle Phasen. **Diese Zahl gehoert selbst gemessen und nicht aus
      diesem Plan abgeschrieben**; sie ist die Reichweite des Defekts.
    - Die Implementierung von `requirements revert-phase` in der GSD-Laufzeit unter
      `gsd-core/bin/lib/milestone.cjs`, Funktion `cmdRequirementsRevertPhase` — **beide**
      Wirkflaechen: das Checkbox-Muster, das den Fett-Wrapper in sich traegt, und das Zeilen-Praedikat
      des Traceability-Registers, das die erste Zelle nach Trimmen und Kleinschreibung auf exakte
      Gleichheit prueft. **Die beiden Zeilen woertlich lesen und in den Ledger-Eintrag zitieren** —
      eine Ursache, die aus einer Beobachtung erschlossen statt an der Quelle gelesen ist, ist
      dieselbe Gattung Behauptung, die dieser Ledger fuehrt.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-VERIFICATION.md`, `gaps[1]` und
      Abschnitt 9 Punkt 2 — die Buchhaltungsregression der Runde 6 und die Regel, die der Betreiber
      mit `2e57ee3` ausgesprochen hat. **Dieser Befund ist ihr Gegenstueck**: dort schrieb ein
      Werkzeug zu viel, hier erreicht es zu wenig.
  </read_first>
  <files>.planning/WINDOWS.md, .planning/REQUIREMENTS.md</files>
  <action>
**Zuerst messen, dann schreiben. Der Beleg wird an einer Kopie hergestellt, nicht am Register.**

Eine Kopie von `.planning/REQUIREMENTS.md` in ein Temp-Verzeichnis legen, das eine
`.planning/`-Ebene hat, und in dieser Kopie eine undekorierte Phase-2-Zeile als Kontrolle auf
`Complete` setzen. Dann `requirements revert-phase` gegen dieses Temp-Projektverzeichnis fahren —
`gsd-tools` nimmt dafuer den Schalter, der die Projektwurzel ausdruecklich vorgibt und die
Vorfahrensuche ueberspringt — mit beiden Ids, der dekorierten und der undekorierten Kontrolle.
**Erwartet und zu protokollieren:** die Kontrolle erscheint unter `reverted`, die dekorierte unter
`unchanged`, und in der Kopie steht die Kontrollzeile danach auf `Gaps Found`, die dekorierte
unveraendert auf `Complete`. Das isoliert die Dekoration als Ursache, ohne das echte Register
anzufassen. Die Ausgabe des Laufs gehoert woertlich in die SUMMARY.

Ausserdem zaehlen, wie viele Zeilen des Registers ueber **alle** Phasen eine dekorierte Id-Zelle
tragen. Das ist die Reichweite, und sie gehoert in den Eintrag: jede dieser Zeilen wird beim naechsten
`gaps_found` ihrer Phase denselben Weg nehmen.

**Dann Eintrag 73.** `--kind deviation`, `--file .planning/REQUIREMENTS.md`, ohne `--line`, Text als
ein durchgehender Absatz ohne Pipe-Zeichen und ohne Zeilenumbrueche, eroeffnend mit `[from 02-31]`.
Er sagt in dieser Reihenfolge: **was gemessen wurde** (der Lauf gegen die Kopie mit beiden Ids und
seinen beiden Ergebnissen); **warum es passiert** (die beiden woertlich zitierten Stellen aus der
Werkzeugquelle — das Checkbox-Muster, das den Fett-Wrapper traegt, und das Zeilen-Praedikat, das auf
exakte Gleichheit prueft; die beiden Wirkflaechen derselben Funktion sind sich uneinig darueber, wie
eine Id-Zelle aussehen darf); **was es angerichtet hat** (weder der Revert der Runde 5 noch der dieser
Runde hat die Zeile erreicht, waehrend alle 13 undekorierten Phase-2-Ids zurueckgenommen wurden, und
der einzige Release-Blocker der Phase las damit `Complete`, waehrend die Verifikation `gaps_found`
sagt); **wie weit es reicht** (die gemessene Zahl dekorierter Release-Blocker-Zeilen im ganzen
Register, mit den Ids); **was Runde 7 getan hat** (die Zelle von Hand korrigiert, was die Anwendung
der vom Betreiber mit `2e57ee3` ausgesprochenen Regel ist und keine neue Entscheidung); und **warum
er offen bleibt** (der Defekt sitzt in der GSD-Laufzeit ausserhalb dieses Repositories, nichts hier
kann ihn schliessen, und der naechste `mark-complete`- oder `revert-phase`-Zyklus erzeugt ihn erneut;
seine Schliessbedingung ist ein Lauf, in dem eine dekorierte Zeile unter `reverted` erscheint).

**Dann die eine Zelle korrigieren.** In `.planning/REQUIREMENTS.md` ausschliesslich die Statuszelle
der TRANS-06-Zeile von `Complete` auf `Gaps Found` setzen. Nichts anderes: keine zweite Zeile, keine
Checkbox, kein Requirement-Text, keine Ueberschrift, keine Spaltenbreite. Die Id-Zelle behaelt ihre
Dekoration — sie zu entfernen waere die bequeme Reparatur am falschen Ort und wuerde die
Release-Blocker-Auszeichnung des Registers beschaedigen, um einen Werkzeugdefekt zu umgehen.

**Zuletzt alle vierzehn Phase-2-Zeilen nachzaehlen.** Vierzehn Statuszellen lesen `Gaps Found`, keine
liest `Complete`, und die vierzehn zugehoerigen Checkboxen sind leer. Die Zahlen gehoeren in die
SUMMARY. Und ausdruecklich nachpruefen, dass die Buchhaltungsregression der Runde 6 geschlossen
geblieben ist: `.planning/ROADMAP.md` traegt genau eine Zahlenangabe fuer die ausgefuehrten Plaene
dieser Phase, und `.planning/STATE.md` sagt unter `## Current Position` dieselbe Zahl wie sein eigenes
Frontmatter. Beides wurde vom Betreiber bereits korrigiert; dieser Task **prueft nur nach und aendert
dort nichts**.
  </action>
  <acceptance_criteria>
    - Die SUMMARY zitiert die Ausgabe des Laufs gegen die Kopie: die undekorierte Kontroll-Id erscheint unter `reverted`, die dekorierte unter `unchanged`
    - Die SUMMARY zitiert die beiden Stellen der Werkzeugquelle woertlich — das Checkbox-Muster mit dem Fett-Wrapper und das Zeilen-Praedikat mit dem Gleichheitsvergleich — und nennt die Datei und die Funktion, aus der sie stammen
    - Das echte `.planning/REQUIREMENTS.md` wurde von dem Messlauf **nicht** beruehrt; die Kopie lag ausserhalb des Repositories
    - Genau ein weiterer `windows append` wurde abgesetzt; die SUMMARY nennt den Aufruf mit der zurueckgegebenen id
    - Der neue Eintrag traegt die gemessene Zahl der dekorierten Release-Blocker-Zeilen des ganzen Registers und listet ihre Ids
    - Der neue Eintrag traegt eine Schliessbedingung, die an einem Lauf pruefbar ist, und steht auf `open`
    - Alle vierzehn Phase-2-Statuszellen im Traceability-Register lesen `Gaps Found`; keine liest `Complete`
    - Alle vierzehn Phase-2-Checkboxen sind leer
    - Die Id-Zelle der korrigierten Zeile traegt ihre Dekoration unveraendert; der Diff an `.planning/REQUIREMENTS.md` besteht aus genau einer geaenderten Zeile
    - Die SUMMARY belegt, dass `.planning/ROADMAP.md` genau eine Zahlenangabe fuer die ausgefuehrten Plaene der Phase traegt und dass `.planning/STATE.md` unter `## Current Position` dieselbe Zahl nennt wie sein Frontmatter — nachgeprueft, nicht geaendert
    - `requirements-completed` in der SUMMARY ist leer
    - `git diff --quiet HEAD -- internal/ cmd/ web/` ist gruen
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^| .*| Phase 2 | Gaps Found |' .planning/REQUIREMENTS.md | grep -qx '14'</automated>
    <fails_when>Exit 1, weil nicht genau vierzehn Phase-2-Zeilen `Gaps Found` lesen. Dreizehn heisst, die dekorierte Zeile ist unkorrigiert geblieben — der Zustand, den dieser Task beheben soll. Mehr als vierzehn heisst, dass eine Zeile einer anderen Phase mitgeaendert wurde, was dieser Plan verbietet.</fails_when>
    <automated>grep -c '^| .*| Phase 2 | Complete |' .planning/REQUIREMENTS.md | grep -qx '0'</automated>
    <fails_when>Exit 1, weil mindestens eine Phase-2-Zeile weiterhin `Complete` liest, waehrend die Verifikation der Phase `gaps_found` traegt. Das ist woertlich die Aussage, die der Betreiber mit `2e57ee3` zurueckgenommen hat.</fails_when>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status --raw | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const l=JSON.parse(s).ledger;const ok=l.open_count===57&&l.waived_count===0&&l.fixed_count===16&&l.total_count===73;const ids=l.entries.map(x=>x.id).sort((a,b)=>a-b);const dense=ids.length===73&&ids.every((v,i)=>v===i+1);const last=l.entries.find(x=>x.id===73);process.exit(ok&&dense&&last&&last.status==="open"&&last.kind==="deviation"?0:1)})'</automated>
    <fails_when>Exit 1. Vier Bedingungen fuehren dorthin: die Zaehler stehen nicht auf `57/0/16/73`; die Ids sind nicht lueckenlos 1..73; der neue Eintrag ist nicht `open` (bei der Anlage `fixed` zu setzen ist der Fehler von 69, und `windows_enforce` sieht ihn dann beim Ship nicht); oder er traegt eine andere Art als `deviation`.</fails_when>
    <automated>git diff --quiet HEAD -- internal/ cmd/ web/</automated>
    <fails_when>Exit 1, weil ein Unterschied unter `internal/`, `cmd/` oder `web/` im Baum steht. Dieser Task misst an einer Kopie ausserhalb des Repositories und aendert genau eine Zelle in einem Register.</fails_when>
  </verify>
  <done>Der Werkzeugdefekt ist an der Quelle gelesen, an einer Kopie reproduziert und mit seiner gemessenen Reichweite als offener Eintrag geledgert; die eine Zeile, die kein Werkzeug erreicht hat, liest `Gaps Found` wie die dreizehn anderen; und die Buchhaltungsregression der Runde 6 ist als geschlossen nachgeprueft, nicht erneut angefasst.</done>
</task>

<task type="auto">
  <name>Task 3: Die Geschwister-Leser — jeder einzeln gemessen, und das Entscheidungsdokument dort korrigiert, wo die Messung ihm widerspricht</name>
  <reversibility rating="costly">Der Ledger-Eintrag ist nicht zurueckziehbar (siehe Task 1). Der Text muss deshalb vor dem `append` stimmen, und genau darum misst dieser Task jeden der drei Leser einzeln, statt die Aufzaehlung des Entscheidungsdokuments zu uebernehmen.</reversibility>
  <precondition>Die beiden Ledger-Eintraege aus Task 1 und Task 2 existieren, denn Eintrag 74 grenzt sich gegen 72 ab (`die Geschwister sind von 72 nicht gedeckt`). Pruefen mit `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status --pick ledger.total_count` — erwartet 73.</precondition>
  <read_first>
    - `internal/imagefactory/guard_drift_test.go`, `stringArrayLiteral` vollstaendig — der Ausdruck,
      der aus dem mit `regexp.QuoteMeta` maskierten Namen, einer beliebigen Zeichenfolge ohne
      Gleichheitszeichen und dem Klammerpaar besteht, und der Doc-Kommentar darueber. **Der Ausdruck
      hat weder einen Zeilenanfangs-Anker noch eine Wortgrenze hinter dem Namen** — beides gehoert
      gemessen, nicht angenommen.
    - `internal/httpapi/handlers/budget_drift_test.go:77-120` — der dortige Anker mit seinem
      Kommentar. **Hier ist besondere Sorgfalt verlangt:** das Entscheidungsdokument fuehrt diese
      Stelle als Traegerin desselben Praefix-Lochs, und beim Nachrechnen am Muster ist das nicht
      offensichtlich richtig. Miss es, und schreibe auf, was gemessen wurde.
    - `internal/imagefactory/warnings_test.go`, `TestWarningDetailsMatchTheUI` vollstaendig — die
      beiden `strings.Contains`-Pruefungen ueber ganze Dateien und die Schleife ueber
      `exportedWarningCodes`. Ebenso `exportedWarningCodes` selbst mit seinem Doc-Kommentar, der
      begruendet, warum die **Go**-Seite ueber `go/ast` gelesen wird und nicht per grep — genau die
      Unterscheidung, die auf der TypeScript-Seite fehlt.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md`,
      Abschnitt "Was keine der vier Varianten schliesst", Unterpunkt "Die Geschwister" — die
      Aufzaehlung, die dieser Task nachmisst, samt dem Satz, warum sie einen eigenen offenen Eintrag
      brauchen.
    - `.planning/WINDOWS.md`, Eintrag 71 — der Praezedenzfall fuer einen Eintrag, der mehrere
      Fundstellen in einem Text fuehrt, jede einzeln beurteilt, und der seine eigene Messgrenze
      ausschreibt.
  </read_first>
  <action>
**Jeden der drei Leser einzeln messen, bevor eine Zeile Text entsteht.** Der billigste Weg ist
derselbe, den der Verifier der Runde 6 und das Planen dieser Runde gegangen sind: die Muster woertlich
in eine wegwerfbare Sonde kopieren und gegen synthetische Quellen fahren. Am Ende ist die Sonde
entfernt und `git status` zeigt ausser den geplanten Aenderungen nichts.

Fuer jeden Leser zwei Fragen, und beide beantwortet mit einer Ausgabe statt mit einem Urteil:

- **Traegt er das Praefix-Loch aus Runde 5?** Erfuellt also ein Bezeichner, der den gesuchten nur als
  Praefix traegt, sein Muster? Miss es mit einem angehaengten Suffix am Namen.
- **Traegt er die lexikalische Blindheit?** Erfuellt also eine Deklaration, die nur in einem
  Kommentar oder einem Template-Literal lebt, sein Muster?

Erwartung aus dem Planen dieser Runde, ausdruecklich als **Erwartung und nicht als Ergebnis**: der
Leser der Installer-Namen in `guard_drift_test.go` traegt beides, weil sein Muster hinter dem Namen
eine beliebige Zeichenfolge ohne Gleichheitszeichen zulaesst und ueberhaupt keinen
Zeilenanfangs-Anker hat. Der Anker in `budget_drift_test.go` traegt das Praefix-Loch **nicht**,
weil er das Gleichheitszeichen unmittelbar hinter dem Namen verlangt — er traegt aber die
lexikalische Blindheit, weil sein Zeilenanfangs-Anker Einrueckung zulaesst und Kommentarzeichen nicht
kennt. Die beiden `strings.Contains`-Sweeps in `warnings_test.go` haben weder Anker noch Wortgrenze:
ein Satz oder ein Code, der nur in einem Kommentar der Zieldatei ueberlebt, erfuellt sie.

**Wird etwas anderes gemessen, gilt das Gemessene.** Und wo die Messung dem Entscheidungsdokument
widerspricht, sagt der Eintrag das ausdruecklich und benennt die Stelle — ein Ledger, der eine
Vermutung aus einem Planungsdokument uebernimmt, ist derselbe Defekt wie ein Waechter, der eine
Transkription fuer eine Garantie haelt.

**Dann Eintrag 74.** `--kind unmet-truth`, `--file internal/imagefactory/guard_drift_test.go`, ohne
`--line` — er zeigt auf den Symbolnamen des Installer-Namen-Lesers, und die beiden anderen
Fundstellen werden im Text mit Datei und Symbol benannt, wie Eintrag 71 es fuer seine vier
Reststellen vormacht. Text als ein durchgehender Absatz ohne Pipe-Zeichen und ohne Zeilenumbrueche,
eroeffnend mit `[from 02-31]`. Er sagt: **welche drei Leser** betroffen sind, je mit Datei und
Symbol; **was fuer jeden einzeln gemessen wurde**, mit den Ausgaben; **worin sich die Messung vom
Entscheidungsdokument unterscheidet**, falls sie es tut; **warum sie einen eigenen Eintrag bekommen**
(sie stillschweigend stehen zu lassen waere dieselbe Operation, die 69 falsch gemacht hat, und
Eintrag 72 deckt sie ausdruecklich nicht); **was ihn schliesst** (jeder der drei traegt entweder die
Verankerungs-Disziplin aus Plan 02-27 oder einen geschriebenen, gemessenen Ausschluss wie den aus
Plan 02-30 — eine Runde, die das fuer alle drei tut und je zwei Messungen protokolliert, schliesst
ihn); und **die Grenze dieser Messung** (drei Leser wurden geprueft und keine weiteren; ob der Baum
weitere traegt, hat diese Runde nicht festgestellt).

**Zuletzt den Ledger unabhaengig vom Werkzeug nachzaehlen** und die Endsignatur der Runde halten:
Frontmatter, Markdown-Tabelle und JSON-Block einzeln parsen, Status pro Id vergleichen, `58 open / 0
waived / 16 fixed / 74 total`, Ids 1..74 lueckenlos und doppelfrei. `windows status` bestaetigt
zusaetzlich `"ok": true`.
  </action>
  <acceptance_criteria>
    - Die SUMMARY fuehrt eine Tabelle mit drei Zeilen — je Leser Datei, Symbol, Praefix-Loch ja/nein mit gemessener Ausgabe, lexikalische Blindheit ja/nein mit gemessener Ausgabe
    - Wo die Messung dem Entscheidungsdokument widerspricht, sagt die SUMMARY es ausdruecklich und benennt die Stelle
    - Genau ein weiterer `windows append` wurde abgesetzt; die SUMMARY nennt den Aufruf mit der zurueckgegebenen id
    - Der neue Eintrag nennt alle drei Leser mit Datei und Symbol, traegt ihre gemessenen Ergebnisse, grenzt sich ausdruecklich gegen Eintrag 72 ab und schreibt seine eigene Messgrenze aus
    - Der neue Eintrag steht auf `open` und traegt eine Schliessbedingung, die an Messungen pruefbar ist
    - Die drei Repraesentationen des Ledgers sind unabhaengig vom Werkzeug nachgezaehlt und tragen `58 open / 0 waived / 16 fixed / 74 total`, ids 1..74 lueckenlos und doppelfrei, null Status-Abweichungen pro Id
    - `waived_count` ist 0, und in keinem Artefakt dieser Runde steht ein `overrides:`-Block
    - Die Sonde ist entfernt; `git status --porcelain` zeigt keine Datei ausserhalb der geplanten `files_modified` und der SUMMARY
    - `git diff --quiet HEAD -- internal/ cmd/ web/` ist gruen
  </acceptance_criteria>
  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status --raw | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const l=JSON.parse(s).ledger;const ok=l.open_count===58&&l.waived_count===0&&l.fixed_count===16&&l.total_count===74;const ids=l.entries.map(x=>x.id).sort((a,b)=>a-b);const dense=ids.length===74&&ids.every((v,i)=>v===i+1);const dup=new Set(ids).size===74;const last=l.entries.find(x=>x.id===74);const keep=[56,58,66,70].every(i=>{const e=l.entries.find(x=>x.id===i);return e&&e.status==="open"});process.exit(ok&&dense&&dup&&keep&&last&&last.status==="open"&&last.kind==="unmet-truth"?0:1)})'</automated>
    <fails_when>Exit 1. Sechs Bedingungen fuehren dorthin: die Endsignatur der Runde ist nicht `58/0/16/74`; die Ids sind nicht lueckenlos 1..74; eine Id kommt doppelt vor; 56, 58, 66 oder 70 ist nicht mehr `open`; der neue Eintrag ist nicht `open`; oder er traegt eine andere Art als `unmet-truth`.</fails_when>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status | grep -q '"ok": true'</automated>
    <fails_when>Exit 1, weil `windows status` nicht `"ok": true` meldet — Frontmatter, Markdown-Tabelle und JSON-Block sind dann nicht im Gleichschritt. Der wahrscheinlichste Weg ist wieder ein Pipe-Zeichen oder ein Zeilenumbruch im Beschreibungstext dieses Eintrags, der als einziger drei Dateipfade und drei Symbolnamen in einem Absatz fuehrt.</fails_when>
    <automated>git status --porcelain | grep -v '^ M .planning/' | grep -v '^?? .planning/phases/02-transport-seam-talossim-image-factory/02-3[01]-SUMMARY.md$' | grep -qc . && exit 1 || exit 0</automated>
    <fails_when>Exit 1, weil `git status --porcelain` nach dem Herausfiltern der `.planning/`-Aenderungen und der SUMMARYs noch mindestens eine Zeile ausgibt. Der wahrscheinlichste Fall ist eine liegengebliebene Sondendatei in einem der drei gemessenen Pakete — genau das, was der Verifier der Runde 6 mit `git status ist leer` als Abnahme seiner eigenen Sonde gefuehrt hat.</fails_when>
    <automated>git diff --quiet HEAD -- internal/ cmd/ web/</automated>
    <fails_when>Exit 1, weil ein Unterschied unter `internal/`, `cmd/` oder `web/` im Baum steht. Die drei Leser werden gemessen und nicht repariert — ihre Reparatur ist die Schliessbedingung dieses Eintrags und gehoert einer spaeteren Runde.</fails_when>
  </verify>
  <done>Die drei Geschwister-Leser sind einzeln gemessen, ihre Ergebnisse stehen in einem eigenen offenen Ledger-Eintrag samt seiner Messgrenze, wo die Messung dem Entscheidungsdokument widerspricht ist das ausgeschrieben, und der Ledger schliesst die Runde mit `58 open / 0 waived / 16 fixed / 74 total` in drei uebereinstimmenden Repraesentationen.</done>
</task>

</tasks>

## Artifacts this phase produces

| Kind | Symbol | Location |
|---|---|---|
| ledger entry | **Eintrag 72** — `unmet-truth`, `open`, ohne Zeilennummer, zeigt auf `refusedRangesDecl`; eroeffnet mit `AMENDS ENTRY 70, WHICH STAYS OPEN (no amend verb exists)`; traegt die sechspunktige Schliessbedingung, das Ausbleiben der Ratifikation und die fuenf verbleibenden Reste | `.planning/WINDOWS.md` |
| ledger entry | **Eintrag 73** — `deviation`, `open`; der gemessene Defekt an `requirements.revert-phase`, seine an der Quelle gelesene Ursache, seine Reichweite ueber alle dekorierten Release-Blocker-Zeilen und die Handkorrektur, die er begleitet | `.planning/WINDOWS.md` |
| ledger entry | **Eintrag 74** — `unmet-truth`, `open`, ohne Zeilennummer, zeigt auf `stringArrayLiteral`; die drei Geschwister-Leser je einzeln gemessen, mit ausgeschriebener Messgrenze | `.planning/WINDOWS.md` |
| ledger state | Endsignatur der Runde: `58 open / 0 waived / 16 fixed / 74 total`, Ids 1..74 lueckenlos und doppelfrei, `"ok": true`. **Naechste freie Ledger-Id: 75** | `.planning/WINDOWS.md` |
| register correction | die Statuszelle der TRANS-06-Zeile liest `Gaps Found` statt `Complete`; ihre Id-Zelle behaelt ihre Dekoration; alle vierzehn Phase-2-Zeilen lesen danach `Gaps Found` | `.planning/REQUIREMENTS.md` |
| document addendum | ein datierter, **angehaengter** Nachtrag: was Runde 7 auf einer nicht ratifizierten Empfehlung ausgefuehrt hat, welcher Plan welchen Teil traegt, dass der Statusblock unveraendert bleibt, und die Byte-Integritaet des entschiedenen Bereichs als Praefix-Hash mit dem erzeugenden Kommando | `02-DECISION-drift-guard-lexik.md` |
| measurement | der Lauf gegen die Kopie: undekorierte Kontroll-Id unter `reverted`, dekorierte unter `unchanged`; dazu die beiden woertlich zitierten Stellen der Werkzeugquelle | `02-31-SUMMARY.md` |
| measurement | die drei Geschwister-Leser, je mit Praefix-Loch und lexikalischer Blindheit als gemessene Ausgabe, und die Stelle, an der die Messung dem Entscheidungsdokument widerspricht | `02-31-SUMMARY.md` |
| measurement | die Nachpruefung, dass die Buchhaltungsregression der Runde 6 geschlossen geblieben ist — eine Zahlenangabe in ROADMAP, uebereinstimmende Zahlen in STATE | `02-31-SUMMARY.md` |
| id allocation | **Naechste freie Luecken-Id: G-02-31. Naechste freie Threat-Id: T-02-134.** | `02-31-SUMMARY.md` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/WINDOWS.md` → `/gsd-ship` | `workflow.windows_enforce` liest genau dieses Register und blockiert, solange `open_count > 0`. Ein Eintrag, der bei der Anlage `fixed` gesetzt wird, ist an dieser Grenze unsichtbar — er passiert das Tor, ohne dass jemand ihn abgewaehlt hat |
| `.planning/REQUIREMENTS.md` → der Leser, der den Projektstand daraus liest | das Traceability-Register ist die eine Stelle, an der ein Mensch oder ein Werkzeug ablesen soll, was fertig ist. Eine Zeile, die `Complete` liest, waehrend die Verifikation ihrer Phase `gaps_found` sagt, ist eine Falschaussage an genau der Stelle, an der niemand nachfragt |
| die Werkzeugkette → die handgeschriebene Aussage darunter | ein Werkzeug, das ein Register schreibt, ohne es zu lesen, ueberschreibt Entscheidungen, die ein Mensch getroffen hat. In Runde 6 hat es zu viel geschrieben; hier erreicht es zu wenig — dieselbe Grenze, zwei Richtungen |
| das Entscheidungsdokument → die Handlungen, die auf ihm ruhen | ein Dokument, dessen Statusblock `nicht ratifiziert` sagt, waehrend eine Runde auf ihm ausgefuehrt hat, ist nur dann ehrlich, wenn die Ausfuehrung im Dokument steht. Angehaengt, nicht eingearbeitet — sonst ist nicht mehr erkennbar, was entschieden und was nachgetragen wurde |

## STRIDE Threat Register

ASVS-Level 1, blockierend ab `high`. Ids T-02-132..T-02-133. Die Plaene 02-29 und 02-30 dieser Runde
vergeben T-02-126..T-02-131; die naechste freie Id nach diesem Plan ist T-02-134.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-132 | Repudiation | eine offene Eigenschaft wird als `fixed` verbucht und ist damit beim Ship unsichtbar; `/gsd-ship` laesst eine Luecke passieren, die niemand abgewaehlt hat, weil niemand sie sieht. Genau das ist mit Eintrag 69 passiert, und Eintrag 70 nennt es mechanisch statt stilistisch | high | mitigate | alle drei neuen Eintraege werden bei der Anlage `open` gesetzt und keiner wird gewaiv't; `overrides:` wird nirgends benutzt und `waived_count` bleibt 0. Ein maschineller Check ueber `windows status --raw` bestaetigt fuer jeden neuen Eintrag den Status, die Art und die Endsignatur, und dass 56, 58, 66 und 70 unberuehrt offen geblieben sind. Die drei Repraesentationen werden zusaetzlich unabhaengig vom Werkzeug nachgezaehlt |
| T-02-133 | Tampering | eine Werkzeugkette schreibt einen Requirement-Status, den ein Betreiber ausdruecklich zurueckgenommen hat, oder erreicht eine Zeile nicht, die sie erreichen muesste — und in beiden Faellen liest das Register danach etwas, das der Phasenstand nicht traegt. Gemessen: der einzige Release-Blocker dieser Phase las `Complete`, waehrend die Verifikation `gaps_found` sagt | high | mitigate | die Ursache wird an der Quelle gelesen und an einer Kopie ausserhalb des Repositories reproduziert, nicht aus der Beobachtung erschlossen; die betroffene Zelle wird von Hand nach der vom Betreiber ausgesprochenen Regel korrigiert; die Reichweite ueber alle dekorierten Release-Blocker-Zeilen wird gemessen und geledgert; und die Abnahme zaehlt beide Richtungen — vierzehn Zeilen `Gaps Found` und null Zeilen `Complete` |
| T-02-SC | Tampering | npm/pip/cargo installs | low | mitigate | dieser Plan fuegt kein Go-Modul und kein npm-Paket hinzu; es gibt keinen Installationsschritt und daher nichts zu pruefen. Geaendert werden der Ledger ueber `gsd-tools windows`, eine Zelle in einem Markdown-Register und ein angehaengter Abschnitt in einem Markdown-Dokument |
</threat_model>

<verification>
- `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` meldet `"ok": true`
- Die Uebereinstimmung der drei Ledger-Repraesentationen wird **unabhaengig vom Werkzeug** nachgezaehlt und gegen die Signatur `fm=58/0/16/74 md=58/16/74 js=58/16/74 ids=74/74` geprueft. Diese Pruefung wuerde heute eine andere Signatur liefern — eine Konsistenzpruefung, die auf einem unveraenderten Ledger besteht, prueft nichts
- Ein maschineller Check ueber `windows status --raw` bestaetigt: 56, 58, 66 und 70 sind `open`, 69 ist `fixed`, die drei neuen Eintraege sind `open`, der amendierende traegt keine Zeilennummer, und `waived_count` ist 0
- `grep -c '^| .*| Phase 2 | Complete |' .planning/REQUIREMENTS.md` ist 0 und `grep -c '^| .*| Phase 2 | Gaps Found |'` ist 14
- Der Diff an `.planning/REQUIREMENTS.md` besteht aus genau einer geaenderten Zeile
- `02-DECISION-drift-guard-lexik.md` traegt seinen Statusblock unveraendert; der Nachtrag ist angehaengt, und die SUMMARY nennt Byte-Zahl, Hash und das erzeugende Kommando des entschiedenen Bereichs
- `git diff --quiet HEAD -- internal/ cmd/ web/` ist gruen; dieser Plan schreibt keinen Code
- Die Sonden aus Task 2 und Task 3 sind rueckstandslos entfernt; `git status --porcelain` zeigt ausser den `.planning/`-Aenderungen und der SUMMARY nichts
- **Kein Go- und kein npm-Testlauf ist Teil dieser Abnahme.** Dieser Plan aendert keine Quelldatei; die Testlaeufe gehoeren den Plaenen 02-29 und 02-30. Was hier geprueft wird, ist, dass sie gruen sind — als Vorbedingung von Task 1 und nicht als Ergebnis dieses Plans
- `requirements-completed` in der SUMMARY ist leer, obwohl `requirements` alle vierzehn Ids fuehrt. Die beiden sind verschiedene Behauptungen, und ihre Verwechslung ist die Wurzel der Regression aus Runde 6
</verification>

<success_criteria>
- Ledger-Eintrag 72 steht als offener, amendierender Eintrag ohne Zeilennummer im Register, eroeffnet mit `AMENDS ENTRY 70, WHICH STAYS OPEN (no amend verb exists)` und zeigt auf den Symbolnamen `refusedRangesDecl` — womit 70s stale Zeiger auf Zeile 83 erledigt ist, ohne 70 zu bearbeiten
- Eintrag 72 traegt alle sechs Punkte der Schliessbedingung, Punkt 6 eingeschlossen, samt dem Satz, dass ein Eintrag, dessen Bedingung nur die schon bekannten Formen abfragt, sich selbst schliesst
- Eintrag 72 sagt ausdruecklich, dass die Richtung dieser Runde nicht ratifiziert ist, dass Variante A punktgleich lag, und was ein Wechsel auf sie kostet
- Eintrag 72 benennt die fuenf verbleibenden Reste einzeln — die Ableitung, die eine tragende Verbindung, den `TrimSpace`-Rest, die bewusst nicht geschriebenen Hinweiszeilen und die 2046 nie verglichenen Codepoints
- Eintrag 70 ist woertlich unveraendert und bleibt `open`; Eintrag 69 bleibt `fixed`; Eintrag 56 wird ausdruecklich nicht ueberholt; **G-02-9 ist offen** und die Eintraege 58 und 66 tragen ihn unberuehrt
- Ledger-Eintrag 73 traegt den an der Quelle gelesenen und an einer Kopie reproduzierten Werkzeugdefekt mit seiner gemessenen Reichweite und bleibt `open`
- Alle vierzehn Phase-2-Zeilen im Traceability-Register lesen `Gaps Found`; keine liest `Complete`; die korrigierte Zeile behaelt ihre Dekoration
- Ledger-Eintrag 74 traegt die drei einzeln gemessenen Geschwister-Leser mit ausgeschriebener Messgrenze und bleibt `open`
- Der Ledger schliesst die Runde mit `58 open / 0 waived / 16 fixed / 74 total` in drei uebereinstimmenden Repraesentationen, Ids 1..74 lueckenlos und doppelfrei
- **`overrides:` wird nirgends benutzt und `waived_count` bleibt 0** — dieser Rest wird nicht angenommen, sondern offen gefuehrt
- `02-DECISION-drift-guard-lexik.md` traegt einen angehaengten, datierten Nachtrag und einen unveraenderten Statusblock
- Dieser Plan schreibt keinen Code, und keine Zeile in ihm behauptet, G-02-29 sei geschlossen
</success_criteria>

<output>
Create `.planning/phases/02-transport-seam-talossim-image-factory/02-31-SUMMARY.md` when done
</output>

