---
phase: 02-transport-seam-talossim-image-factory
plan: 29
type: execute
wave: 1
depends_on: []
gap_closure: true

# gap_ids ist die maschinenlesbare Behauptung "dieser Plan schliesst diese Luecken".
#
# **Die Id-Entscheidung, ausdruecklich getroffen statt implizit gelassen.** `02-VERIFICATION.md`
# Runde 6 fuehrt G-02-25 unter `re_verification.gaps_closed` als GESCHLOSSEN und `gaps_remaining`
# als leer. Der Befund von Runde 6, `gaps[0]`, ist damit KEIN Rest von G-02-25, sondern neu — er
# traegt zwei sachlich verschiedene Defekte in einer Wahrheitszeile, und die beiden bekommen
# getrennte Nummern, weil sie getrennte Wurzeln, getrennte Plaene und getrennte Schicksale haben:
#
#   G-02-28  ==  02-VERIFICATION.md Runde 6, gaps[0], die SPIEGEL-Haelfte   (rundenlokal G6-1b)
#                ==  02-REVIEW.md Runde 6, WR-02
#                Wurzel: `whole` wird ueber die ganze Quelle gezaehlt und beantwortet danach zwei
#                verschiedene Fragen. IN RUNDE 6 NEU ENTSTANDEN. Dieser Plan schliesst ihn.
#
#   G-02-29  ==  02-VERIFICATION.md Runde 6, gaps[0], die ANKER-Haelfte     (rundenlokal G6-1a)
#                ==  02-REVIEW.md Runde 6, WR-01
#                Wurzel: der Anker kennt keinen lexikalischen Kontext und bindet an ein Literal
#                statt an einen Wert. Gehoert Plan 02-30 und 02-31 und wird von Runde 7 NICHT
#                geschlossen — siehe `02-DECISION-drift-guard-lexik.md`.
#
# Die naechste freie Nummer nach dieser Runde ist G-02-31 (G-02-30 gehoert Plan 02-31).
gap_ids: [G-02-28]
gap_id_mapping:
  - gap_id: G-02-28
    verification_round: 6
    round_local_label: G6-1b
    verification_status: partial
    review_finding: "WR-02 (Spiegel-Defekt), WR-03 (eine Meldung fuer zwei Ursachen), WR-04 (drei trivial erfuellte Tabellenzeilen), WR-05 (Doc-Kommentar am falschen Symbol)"
    truth: "A guard reports a pass only when it measured the property it is named for — und seine Meldung nennt die Ursache, die sie festgestellt hat, nicht eine andere"
    id_decision: "neue Id — der Defekt ist in Runde 6 durch Plan 02-27 entstanden und ist kein Rest von G-02-25, das Runde 6 als geschlossen fuehrt"

files_modified:
  - internal/imagefactory/guard_drift_test.go
# `web/src/routes/images.tsx` steht bewusst NICHT hier und wird von diesem Plan auch nicht
# voruebergehend angefasst. Alle Belege dieses Plans laufen ueber synthetische Quellen in der
# Testdatei; es gibt keinen Lebendbeleg, der eine Umbenennung am echten Baum braeuchte. Die
# Regressionssperre gegen die Praefix-Umbenennung ist als Tabellenzeile bereits kodiert (Plan
# 02-27) und bleibt woertlich erhalten.
autonomous: true
requirements: [FACT-01, FACT-06]
# Die uebrigen zwoelf Phasen-Requirements erscheinen in den `requirements`-Frontmattern der Plaene
# 02-01…02-26; `02-VERIFICATION.md` Runde 6, Abschnitt 6 stellt phasenweit fest: "Orphaned
# requirements: keine." Sie hier zu wiederholen waere eine Behauptung, die dieser Plan nicht
# einloest. Plan 02-31 dieser Runde traegt alle vierzehn, weil sein Task 2 die Statuszeile jeder
# einzelnen im Traceability-Register prueft.

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Die Leer-Meldung und die Abschneide-Meldung werden durch die FORM DES RUMPFES unterschieden und nicht durch eine Zaehlung ueber die Datei. Eine wirklich leere `const REFUSED_RANGES … = []` bekommt die Leer-Meldung, auch wenn irgendwo sonst in der Datei ein eintragsfoermiges Literal steht. Beim Planen am echten Ausdruck gemessen: heute liefert genau dieser Fall `cut short before its first entry, but 1 entries are present in the source as a whole` — eine Klammer, die es nicht gibt. (Punkt 4 der Schliessbedingung von Ledger-Eintrag 72.)"
    - "Die Abschneide-Meldung ZEIGT ihren Beleg, statt ihn zu behaupten: sie zitiert den geschnittenen Rumpf mit `%q`. Der Satz \"The declaration is NOT empty\", der heute aus einer Dateizaehlung erschlossen wird, verschwindet mit der Zaehlung, aus der er erschlossen war."
    - "`whole` beantwortet nur noch EINE Frage — steht ein eintragsfoermiges Literal ausserhalb der Deklaration. Die Zaehlpruefung `whole != len(matches)` bleibt woertlich erhalten; nur ihre zweite, geliehene Aufgabe entfaellt."
    - "Eine Bedingung, eine Ursache, eine Meldung: `to > utf8.MaxRune` und `from > to` sind zwei Zweige mit zwei Texten. Die invertierte Range wird nicht laenger mit einer verlustbehafteten Konvertierung erklaert, die bei ihr nicht stattfindet — beide Grenzen von `{0x001f, 0x0000}` sind darstellbar. (02-REVIEW WR-03.)"
    - "Keine Tabellenzeile prueft mehr eine Zeichenkette, die jeder Fehlerausgang der Funktion traegt. Die drei Zeilen mit `wantErr: \"REFUSED_RANGES\"` pinnen je eine unterscheidende Teilzeichenkette des Kein-Anker-Ausgangs; `wantErr` wird `[]string` wie in den beiden anderen Tabellen. (02-REVIEW WR-04.)"
    - "Der Waechter stellt fest, wie viele Deklarationen er gefunden hat, statt schweigend die erste zu nehmen. Beim Planen gemessen: eine eingerueckte innere `const REFUSED_RANGES` vor der echten liefert heute `1 entries inside the REFUSED_RANGES declaration, 7 in the source as a whole` — fail closed mit falscher Ursache; eine leere innere vor der echten liefert nach der Rumpfform-Korrektur `carries no entries`, waehrend die echte Tabelle sechs Eintraege traegt."
    - "`go doc` zeigt die Begruendung der Anker-Entscheidung unter dem Symbol, das sie betrifft. Der 49-zeilige Block vor `var refusedRangesEntry` wird geteilt; `var refusedRangesDecl` bekommt seinen eigenen Doc-Kommentar. (02-REVIEW WR-05, in Runde 6 um 25 Zeilen gewachsen.)"
    - "Die Regressionssperren der Runden 4, 5 und 6 bleiben woertlich erhalten und bleiben gruen: die praefixierte Umbenennung (`REFUSED_RANGES_LEGACY`) ist rot, die Deklaration hinter `//` ist rot, ein Eintrag mit einer Grenze oberhalb `utf8.MaxRune` ist rot. (Punkte 1, 2 und 3 der Schliessbedingung von Ledger-Eintrag 72.)"
    - "`TestBrowserRefusalSetEqualsTheServers` misst weiterhin dasselbe und bleibt gruen; die Ablehnungsmenge wird auf keiner der beiden Seiten angefasst. Rumpf- wie Dateizahl bleiben 6."
    - "Jede neue Tabellenzeile wurde rot gesehen, bevor sie gruen war, und die Rot-Ausgabe steht woertlich in der SUMMARY. Fuer die drei umgestellten `wantErr`-Zeilen, die die Eigenschaft heute schon erfuellen, wird stattdessen die unterscheidende Teilzeichenkette einmal aus der Meldung entfernt, die Zeilen rot gesehen und die Aenderung zurueckgenommen."
  prohibitions:
    - statement: "Die Ablehnungsmenge selbst nicht aendern — weder `REFUSED_RANGES` in `web/src/routes/images.tsx` noch `NotRepresentableReason` in `internal/imagefactory/schematicid.go`. `02-VERIFICATION.md` Runde 6 hat neu gemessen, dass heute nichts maskiert ist: das Loeschen des `U+FEFF`-Eintrags laesst den Waechter weiterhin scheitern. Es geht um das kuenftige Verhalten des Waechters."
      status: unresolved
    - statement: "`web/src/routes/images.tsx` wird von diesem Plan WEDER netto NOCH voruebergehend veraendert. Anders als Plan 02-27 braucht dieser Plan keinen Lebendbeleg: alle sechs neuen Eigenschaften sind an synthetischen Quellen messbar, und die Umbenennungssperre ist bereits als Tabellenzeile kodiert. `git diff --stat -- web/` muss ueber den ganzen Plan leer bleiben."
      status: unresolved
    - statement: "Keine Tabellenzeile wird entfernt oder umbenannt, um einen Fehlschlag loszuwerden. Die sechs Zeilen von `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration`, die zwei von `TestBrowserRefusalGuardRefusesAnEntryItCannotRead` und die drei von `TestBrowserRefusalGuardRefusesABoundItCannotRepresent` bleiben erhalten; dieser Plan aendert `wantErr`-Werte und fuegt Zeilen hinzu, er streicht keine. Ein Tabellentest, dessen Zeilen unbemerkt verschwinden, ist derselbe Defekt wie ein Waechter, der nicht findet, was er bewacht."
      status: unresolved
    - statement: "Kein neuer Pfad in `parseBrowserRefusalRanges` darf einen Eintrag oder eine Deklaration ueberspringen, ohne einen Fehler zurueckzugeben. `continue` ohne Fehlerrueckgabe ist die Eigenschaft, die diese Luecke seit vier Runden traegt."
      status: unresolved
    - statement: "Kein `stripNonCode`, kein Lexer, kein Parser, kein Codegen. Das sind die Varianten A, C und D aus `02-DECISION-drift-guard-lexik.md`; die Empfehlung ist B, und die lexikalische Frage gehoert Plan 02-30 dieser Runde. Dieser Plan enthaelt ausschliesslich die Korrekturen, die `02-DECISION-drift-guard-lexik.md` unter \"Was in jedem Fall gilt\" als richtungsunabhaengig fuehrt."
      status: unresolved
    - statement: "Weder Kommentar noch SUMMARY dieses Plans darf G-02-29 (die Anker-Blindheit) beruehren, geschweige denn als geschlossen melden. Dieser Plan schliesst G-02-28, den Spiegel-Defekt. Genau die Reihenfolge zu verletzen — ein Plan meldet eine allquantifizierte Eigenschaft als erledigt, die eine Verifikationsrunde feststellen muss — war der Befund von Runde 5 an Ledger-Eintrag 69."
      status: unresolved
    - statement: "**G-02-9 bleibt offen.** Kein Kommentar und keine SUMMARY-Zeile dieses Plans darf ihn als geschlossen melden. Die Ledger-Eintraege 56, 58, 66 und 70 bleiben unberuehrt; dieser Plan schreibt `.planning/WINDOWS.md` nicht — der Ledger gehoert Plan 02-31."
      status: unresolved
    - statement: "`requirements-completed` in der SUMMARY bleibt LEER. Die Phase traegt `gaps_found`; genau ein `requirements-completed`-Feld aus zwei SUMMARYs der Runde 6 hat die Buchhaltungsregression ausgeloest, die Runde 6 als `gaps[1]` gefuehrt hat und die der Betreiber mit `b7a1ab9` zurueckgenommen hat. Diese Runde unterbietet lieber, als noch einmal zu ueberschreiben."
      status: unresolved
  artifacts:
    - path: "internal/imagefactory/guard_drift_test.go"
      provides: "die Rumpfform-Unterscheidung zwischen Leere und Abschneiden, zwei getrennte Bound-Zweige, unterscheidende `wantErr`-Teilzeichenketten als `[]string`, die Duplikatspruefung ueber die Zahl der gefundenen Deklarationen und den geteilten Doc-Kommentar"
      contains: "strings.TrimSpace"
  key_links:
    - from: "internal/imagefactory/guard_drift_test.go"
      to: "web/src/routes/images.tsx"
      via: "der Waechter liest die Deklaration `REFUSED_RANGES` (Zeile 105) aus der Quelle, weil ein Browser kein Go-Symbol importieren kann. Diese Naht wird von einem regulaeren Ausdruck ueberquert und nicht von einem Typsystem. Dieser Plan aendert an der Naht nichts — er sorgt dafuer, dass die Meldungen ueber sie die Ursache nennen, die sie festgestellt haben"
      pattern: "REFUSED_RANGES"
    - from: "internal/imagefactory/guard_drift_test.go"
      to: "strings"
      via: "`strings.TrimSpace(body)` ist die unterscheidende Groesse, die `whole` als Unterscheider ersetzt: die Form des Rumpfes und nicht eine Zaehlung ueber die Datei. `strings` ist bereits importiert und wird heute fuer `strings.Contains` und `strings.Join` benutzt"
      pattern: "strings.TrimSpace"
    - from: "internal/imagefactory/guard_drift_test.go"
      to: "unicode/utf8"
      via: "`utf8.MaxRune` bleibt die Schranke des Ueberlauf-Zweigs; der invertierte Zweig kommt ohne sie aus, und genau das ist WR-03s Punkt"
      pattern: "utf8.MaxRune"
---

<objective>
Der Waechter nennt die Ursache, die er festgestellt hat — nicht eine andere, die er aus einer
Dateizaehlung erschliesst.

Purpose: Runde 6 hat geliefert, was Runde 5 bestellt hatte, und dabei einen Defekt neu erzeugt.
`whole` wird bei `guard_drift_test.go:300` ueber die **ganze Quelle** gezaehlt, und dieselbe Zahl
beantwortet danach zwei verschiedene Fragen: *"war der Rumpf abgeschnitten?"* (`:307`) und *"steht
ein Literal ausserhalb der Deklaration?"* (`:329`). Beim Planen am echten Ausdruck nachgemessen,
nicht aus `02-VERIFICATION.md` uebernommen: eine wirklich leere `const REFUSED_RANGES: readonly
RefusedRange[] = []` neben irgendeiner zweiten eintragsfoermigen Tabelle liefert heute
`E3 cut short before its first entry, but 1 entries are present in the source as a whole` — die
Aufforderung, nach einer Klammer zu suchen, die es nicht gibt. Ohne die zweite Tabelle greift die
richtige Leer-Meldung. Eine falsche Ursache ist gegen eine andere getauscht worden.

**Verschaerfend, und im Register dieser Phase das Eigentliche:** die Meldung behauptet einen Satz,
den der Code nie feststellt — *"The declaration is NOT empty"* wird aus einer Dateizaehlung
erschlossen, nicht aus einer Eigenschaft der Deklaration. Das ist woertlich die Gattung Defekt, die
diese Phase seit Runde 3 an jedem anderen Artefakt korrigiert.

**Der Fix steht nicht zur Debatte und ist in allen vier Varianten von
`02-DECISION-drift-guard-lexik.md` derselbe:** die unterscheidende Groesse ist die **Form des
Rumpfes**, nicht eine Zaehlung ueber die Datei. Dieser Plan ist deshalb **richtungsunabhaengig** — er
haelt auch dann, wenn der Betreiber statt der empfohlenen Variante B die punktgleiche Variante A
waehlt. Er enthaelt genau das, was das Entscheidungsdokument unter *"Was in jedem Fall gilt"* fuehrt.

Drei weitere Befunde des Runde-6-Reviews liegen in derselben Funktion, kosten wenig und haben
dieselbe Wurzel — eine Aussage, die mehr behauptet, als sie feststellt. **WR-03:** eine Bedingung mit
zwei Ursachen und einer Meldung; fuer `{ from: 0x001f, to: 0x0000 }` sind beide Grenzen einwandfrei
darstellbar, und die Meldung erklaert dem Operator eine verlustbehaftete Konvertierung, die dort
nicht stattfindet. **WR-04:** drei Tabellenzeilen pruefen `wantErr: "REFUSED_RANGES"` — eine
Zeichenkette, die **jeder** Fehlerausgang dieser Funktion in seinen Text formatiert; die Zeilen
pruefen nach `err != nil` nichts weiter. **WR-05:** der 49-zeilige Doc-Block dokumentiert godoc-seitig
`refusedRangesEntry`, waehrend `refusedRangesDecl` gar keinen Kommentar hat — und Runde 6 hat 25
Zeilen in den falschen Block hineingeschrieben.

**Und ein siebter Fall, beim Planen gefunden und in keinem Dokument dieser Phase notiert.** Der Anker
nimmt die **erste** Deklaration, die er findet, und fragt nie, ob es eine zweite gibt. Gemessen: eine
eingerueckte innere `const REFUSED_RANGES` vor der echten liefert `1 entries inside the
REFUSED_RANGES declaration, 7 in the source as a whole` — fail closed, aber mit der Ursache eines
anderen Defekts; und eine **leere** innere vor der echten liefert nach der Rumpfform-Korrektur
`carries no entries`, waehrend die echte Tabelle sechs Eintraege traegt. Das ist eine dritte falsche
Ursache, sie entsteht teilweise erst durch die Korrektur dieses Plans, und sie ist mit einer Zeile zu
beseitigen.

Output: eine Funktion, deren sieben Fehlerausgaenge sieben verschiedene Ursachen nennen, von denen
keine aus einer Zaehlung erschlossen ist; drei neue Tabellenzeilen, die je eine dieser Ursachen
festnageln; `wantErr` als `[]string` mit unterscheidenden Teilzeichenketten; und ein Doc-Kommentar,
der unter dem Symbol steht, das er beschreibt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-VERIFICATION.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-REVIEW.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-27-PLAN.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-27-SUMMARY.md
</context>

## Recorded decisions

**Die Richtung dieser Runde ist VORGELEGT, NICHT RATIFIZIERT.** Siehe `## Flagged Assumptions`
weiter unten. Dieser Plan ist von der Richtungsfrage **nicht betroffen**: alle sechs Aenderungen
stehen in `02-DECISION-drift-guard-lexik.md` unter *"Was in jedem Fall gilt"* bzw. im eigenen
Abschnitt *"Der Spiegel-Defekt der Runde 6 — unabhaengig von der Richtungsfrage"*. Waehlt der
Betreiber Variante A statt der empfohlenen Variante B, bleibt dieser Plan unveraendert gueltig; nur
Plan 02-30 wuerde neu geplant.

**Alles hier Behauptete wurde beim Planen gemessen, nicht uebernommen.** Die Regexe
`refusedRangesDecl`, `refusedRangesEntry` und `browserRefusalRange` wurden woertlich aus
`internal/imagefactory/guard_drift_test.go` in ein eigenstaendiges Go-Programm kopiert, zusammen mit
dem Kontrollfluss von `parseBrowserRefusalRanges`, und gegen die echte `web/src/routes/images.tsx`
sowie gegen elf synthetische Quellen gefahren. Ausgaben, auf die dieser Plan sich stuetzt:

| Quelle | heutiger Ausgang | nach der Rumpfform-Korrektur |
|---|---|---|
| echte `images.tsx` | `ranges=6 whole=6 err=nil` | unveraendert `ranges=6` |
| `= []` allein | `declared but carries no entries` | unveraendert |
| `= []` neben einer zweiten Tabelle | **`cut short before its first entry, but 1 entries are present in the source as a whole`** | `declared but carries no entries` |
| Rumpf durch `]` im Kommentar abgeschnitten | `cut short …, but 2 entries are present …` | `cut short …; body is "\n  // see the table in RefusedRange["` |
| Rumpf besteht nur aus einem Kommentar | `declared but carries no entries` | **`cut short …; body is "\n  // nothing yet\n"`** (Verhaltensaenderung, siehe unten) |
| eingerueckte innere Deklaration vor der echten | `1 entries inside the … declaration, 7 in the source as a whole` | unveraendert, bis Task 3 greift |
| leere innere Deklaration vor der echten | `cut short …, but 2 entries …` | `declared but carries no entries` (falsch), bis Task 3 greift |

Ebenfalls gemessen: `refusedRangesDecl.FindAllStringSubmatch` findet in beiden
Doppel-Deklarations-Quellen **2** Treffer und in der echten `images.tsx` genau **1**. Die
Duplikatspruefung aus Task 3 ist an der echten Datei also nicht falsch-rot.

**Die eine Verhaltensaenderung, die dieser Plan bewusst in Kauf nimmt, und ihr Preis.**
`strings.TrimSpace(body) == ""` haelt einen Rumpf aus `// …` oder `/* */` fuer nicht leer und nimmt
den Abschneide-Zweig; heute bekommt genau dieser Fall die Leer-Meldung. Das ist dieselbe **Gattung**
Defekt, innerhalb der Aenderung, die diese Gattung beseitigen soll — kleiner als der Defekt, den sie
ersetzt, weil die Meldung jetzt ihren Beleg **zeigt** (`%q` des Rumpfes) statt ihn zu behaupten, aber
nicht null. Er wird deshalb nicht in eine Fussnote geschrieben, sondern von einer eigenen
Tabellenzeile festgenagelt (Task 1) und von Plan 02-31 in Ledger-Eintrag 72 aufgenommen.

**Die `must_haves.prohibitions` sind descriptor-los** (kein `check_kind`, `check_target`,
`check_rule`, `check_violation_fixture`, `check_clean_fixture`) und disponieren damit als
**flagged-unverified**, nicht als auto-dismissed. Es gibt in diesem Lauf keinen Menschen, der einen
Check verdrahtet; ein erfundener Check-Pfad waere schlimmer als keiner. Kein Kandidat wurde
automatisch verworfen. Zwei Kandidaten der Recall-Stufe sind als Kanon-Verweis fallengelassen und
hier als Brotkrume vermerkt statt gemuenzt: Pfad-Traversal beim Lesen von
`../../web/src/routes/images.tsx` (OWASP-Kanon; der Pfad ist eine Konstante) und ReDoS im Anker
(Kanon; das Muster ist linear und laeuft ueber eine Quelldatei fester Groesse im Test).

## API coverage

`.planning/phases/02-transport-seam-talossim-image-factory/COVERAGE.md` traegt die Matrix dieser
Phase und ist von diesem Plan unberuehrt. Dieser Plan aendert eine Testdatei; er spricht mit keinem
externen Dienst, fuegt keinen Aufruf hinzu und entfernt keinen.

<assumption_delta_decision>
- **Primaeres Substantiv:** unveraendert `REFUSED_RANGES` als die eine Deklaration, aus der die
  Ablehnungsmenge des Browsers gelesen wird. Dieser Plan schafft ausdruecklich **kein** zweites
  Vorkommen — im Gegenteil, Task 3 macht die Existenz genau einer erzwingbar.
- **Entscheidung:** `no-change`. Dieser Plan fuehrt keinen zweiten Fall ein; er trennt drei Ursachen,
  die heute in zwei Meldungen zusammenliegen.
- Ein Begleit-Invariantentest ist nicht noetig: die Duplikatspruefung aus Task 3 **ist** der Test,
  der rot wird, sobald eine zweite Deklaration desselben Bezeichners entsteht.
</assumption_delta_decision>

## Flagged Assumptions

### 1. Die Richtung dieser Runde ist nicht ratifiziert

`02-DECISION-drift-guard-lexik.md` traegt im Statusblock `offen — vorgelegt, nicht ratifiziert`. Sie
wurde von einem autonomen Lauf am 2026-09-05 erarbeitet — vier Entwuerfe, je vier Linsen — und
empfiehlt **Variante B**; **Variante A liegt punktgleich daneben, 7,0 gegen 7,0**. Der Verifier hat
die Frage ausdruecklich dem Betreiber zugewiesen (`02-VERIFICATION.md` Human-Verification-Punkt 1).
Diese Phase hat den Praezedenzfall in beide Richtungen: `02-DECISION-probe-budget.md` wurde in
Sitzung ratifiziert und Runde 4 war bis dahin blockiert; `02-DECISION-schematic-identity.md` wurde
vom Ausfuehrenden selbst aufgeloest und traegt das im Text.

**Was das fuer diesen Plan bedeutet:** nichts. Er ist richtungsunabhaengig, und das ist der Grund,
warum Runde 7 ihn als **eigenen Plan in Welle 1** fuehrt statt ihn mit der Verengung zu vermischen.
Waehlt der Betreiber Variante A, wird **nur Plan 02-30 verworfen und neu geplant**; dieser Plan
bleibt, und Plan 02-31 verliert nur die Abschnitte von Eintrag 72, die B betreffen. Der Wechsel
kostet den Betreiber weiterhin einen Satz.

### 2. Der spezifikationslose Probe-Fallback lief und hat nichts klassifiziert

Diese Phase hat keine `SPEC.md`, also `EDGE_ABSENT=1` und `PROHIB_ABSENT=1`. Die deterministische
Edge-Probe lief ueber alle vierzehn Requirement-Ids und lieferte
`{"applicable":14,"resolved":0,"unresolved":14,"byVerification":{"explicit":0,"backstop":0}}` — jede
Zeile `unresolved`, dreizehn davon `category: unclassified` mit `probe: "unclassified — review
manually"`. **Es wurde nichts klassifiziert und nichts aufgeloest.** Nach §C gilt: (Zahl der von der
Probe aufgeworfenen Punkte) == (Zahl der in `must_haves` uebernommenen + Zahl der hier als markierte
Annahme sichtbar gemachten). 14 == 0 + 14. **Keine stille Auslassung, keine automatische
Verwerfung.**

| requirement | category | status | Disposition in diesem Plan |
|---|---|---|---|
| FACT-01 | unclassified | unresolved | **live** — der Waechter bewacht die Naht zwischen der Eingabepruefung des Browsers und der Ablehnungsmenge des Servers. Dieser Plan korrigiert seine Diagnosen; er loest die Requirement-Zeile nicht auf |
| FACT-06 | unclassified | unresolved | **live** — die vorberechnete Id ruht auf `NotRepresentableReason`, der Menge hinter dem Codepoint-Sweep. Dieser Plan aendert sie nicht; er loest die Requirement-Zeile nicht auf |
| FOUND-12 | unclassified | unresolved | von 02-07 abgedeckt; von dieser Runde unberuehrt |
| TRANS-01 | concurrency | unresolved | von 02-01 abgedeckt; von dieser Runde unberuehrt. **Nicht** auf `backstop` promoviert — dieser Plan fasst die Transport-Naht nicht an, und ein Backstop-Marker waere eine Zusicherung, die kein Task einloest |
| TRANS-02 | unclassified | unresolved | von 02-01 abgedeckt; von dieser Runde unberuehrt |
| TRANS-03 | unclassified | unresolved | von 02-05 abgedeckt; von dieser Runde unberuehrt |
| TRANS-04 | unclassified | unresolved | von 02-05 abgedeckt; von dieser Runde unberuehrt |
| TRANS-05 | unclassified | unresolved | Transport-Haelfte belegt; UI-Haelfte in `02-VERIFICATION.md` `deferred` nach Phase 3 |
| TRANS-06 | unclassified | unresolved | von 02-01/02-03/02-08 abgedeckt. **Seine Statuszeile im Traceability-Register ist falsch** — siehe Plan 02-31, G-02-30 |
| TRANS-07 | unclassified | unresolved | von 02-03 abgedeckt; von dieser Runde unberuehrt |
| FACT-02 | unclassified | unresolved | von 02-02/02-22/02-25 abgedeckt; von dieser Runde unberuehrt |
| FACT-03 | unclassified | unresolved | von 02-04/02-23 abgedeckt; von dieser Runde unberuehrt |
| FACT-04 | unclassified | unresolved | von 02-06 abgedeckt; von dieser Runde unberuehrt |
| FACT-05 | unclassified | unresolved | von 02-04 abgedeckt; von dieser Runde unberuehrt |

### 3. Zwei Nicht-Gap-Befunde des Runde-6-Reviews bleiben ausdruecklich stehen

`IN-02` (die Begruendung fuer `ParseUint(…, 16, 32)` gilt nicht ueber acht Hexziffern), `IN-03`
(mehrzeilige Annotation und `let` werden als "keine Deklaration" gemeldet) und `IN-04` (`0x10FFFF`
gegen `utf8.MaxRune`) werden von diesem Plan **nicht** angefasst. Sie stehen unter
`02-VERIFICATION.md` Human-Verification-Punkt 6, einer ausdruecklichen Policy-Entscheidung des
Betreibers, und sie hier stillschweigend mitzunehmen waere dieselbe Ueberschreitung wie sie
stillschweigend fallenzulassen. `IN-01` (zwei Fundorte fuer `%d entries`) entfaellt als Nebenwirkung,
weil die neue Abschneide-Meldung keine Zahl mehr traegt; das gehoert in die SUMMARY, damit die
Policy-Liste des Betreibers um einen Punkt kuerzer wird und der Grund nachlesbar ist.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Die Form des Rumpfes entscheidet, ob die Deklaration leer ist — ein Pfad von der Fixture bis zur Meldung, die ihren Beleg zeigt</name>
  <reversibility rating="reversible">Die Aenderung liegt vollstaendig in einer Testdatei und aendert kein Produktionsverhalten. Sie zurueckzunehmen ist eine Umkehrung des Diffs; es entstehen keine Daten, keine Migration und kein Zustand, der sie ueberdauern koennte.</reversibility>
  <read_first>
    - `internal/imagefactory/guard_drift_test.go:224-300` — `parseBrowserRefusalRanges` bis
      einschliesslich der Zeile, die `whole` zaehlt. Insbesondere: der Deklarations-Schnitt und sein
      Fehlertext, die Lesbarkeitsschleife und ihr Satz "an entry it skips is a codepoint it reports
      agreement about without having compared it", und der Kommentar unmittelbar vor der Zaehlung von
      `whole`, der begruendet, warum sie vor dem Leer-Zweig steht — **genau diese Begruendung wird
      von diesem Task hinfaellig und muss mit ersetzt werden.**
    - `internal/imagefactory/guard_drift_test.go:301-345` — die drei Zweige, um die es geht: der
      erklaerende Kommentar "With the count in hand the three causes are three messages, and none of
      them claims another's", der Abschneide-Zweig, der Leer-Zweig und die Zaehlpruefung
      `whole != len(matches)`. **Der Abschneide-Zweig und der Leer-Zweig sind die Stelle, die dieser
      Task ersetzt; die Zaehlpruefung bleibt woertlich stehen.**
    - `internal/imagefactory/guard_drift_test.go:396-455` —
      `TestBrowserRefusalGuardRefusesAnEntryItCannotRead` vollstaendig, als Vorlage fuer die
      `wantErr []string`-Form mit mehreren Teilzeichenketten je Zeile und fuer die Auswertung, die
      ueber die Teilzeichenketten schleift. Diese Tabelle wird von diesem Task **nicht** erweitert.
    - `internal/imagefactory/guard_drift_test.go:455-510` —
      `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration` vollstaendig: die fuenf
      synthetischen Quellen `renamed`, `absent`, `polluted`, `prefixed` und `truncated`, die sechs
      Tabellenzeilen und die Auswertung, insbesondere dass `wantErr` heute ein `string` ist und dass
      eine Zeile ohne `wantErr` die Anzahl `wantRanges` prueft. **Der Feldtyp `wantErr string` ist die
      Stelle, die dieser Task auf `[]string` zieht.**
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-REVIEW.md:188-266` — WR-02
      vollstaendig, mit der gemessenen Fehlausgabe und der Begruendung, warum ein Tausch der falschen
      Ursache kein Wegfall ist.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md`,
      Abschnitt "Der Spiegel-Defekt der Runde 6 — unabhaengig von der Richtungsfrage" — die
      vorgeschlagene Form (`strings.TrimSpace(body) == ""`, die Meldung zitiert `body` mit `%q`), die
      Anweisung, das `wantErr` der bestehenden `truncated`-Zeile zu verkuerzen, und die ausdrueckliche
      Benennung des Restes, den `TrimSpace` uebriglaesst.
  </read_first>
  <behavior>
    - Eine Quelle mit `const REFUSED_RANGES: readonly RefusedRange[] = []` und einer zweiten,
      eintragsfoermigen Tabelle daneben liefert die **Leer**-Meldung — heute liefert sie die
      Abschneide-Meldung mit `1 entries are present in the source as a whole`.
    - Eine Quelle mit `= []` und ohne zweite Tabelle liefert weiterhin die Leer-Meldung.
    - Eine Quelle, deren Rumpf durch ein `]` in einem Kommentar vor dem ersten Eintrag abgeschnitten
      wird, liefert die Abschneide-Meldung, und deren Text zitiert den geschnittenen Rumpf woertlich.
    - Eine Quelle, deren Rumpf nur aus einem Kommentar besteht, liefert die Abschneide-Meldung mit dem
      zitierten Rumpf — heute liefert sie die Leer-Meldung. Das ist eine bewusste
      Verhaltensaenderung, und die Zeile, die sie festnagelt, ist der Beleg dafuer, dass sie bemerkt
      wurde.
    - Die echte `web/src/routes/images.tsx` liefert unveraendert sechs Bereiche und keinen Fehler.
    - Die Zaehlpruefung `whole != len(matches)` verhaelt sich unveraendert; die Zeile
      `an entry-shaped literal outside the declaration` bleibt gruen.
  </behavior>
  <files>internal/imagefactory/guard_drift_test.go</files>
  <action>
Zuerst die beiden Tabellenzeilen, und beide muessen rot laufen, solange `whole` die Unterscheidung
traegt.

**Der Feldtyp.** In `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration` das Feld `wantErr`
von `string` auf `[]string` ziehen und die Auswertung entsprechend anpassen: der Zweig
"kein Fehler erwartet" haengt jetzt an `len(tc.wantErr) == 0`, und die Pruefung schleift ueber die
Teilzeichenketten, genau wie es `TestBrowserRefusalGuardRefusesAnEntryItCannotRead` bereits tut. Die
fuenf bestehenden Zeilen mit einem Wert bekommen einen Ein-Element-Slice; ihr Inhalt bleibt in diesem
Task unveraendert — die unterscheidenden Teilzeichenketten sind Task 2.

**Die neue synthetische Quelle `emptyBesideASecondTable`.** Eine wirklich leere
`const REFUSED_RANGES: readonly RefusedRange[] = []` und darunter eine zweite, andersnamige Tabelle
mit genau einem eintragsfoermigen Literal. Ihr Kommentar nennt die heute gemessene Fehlausgabe
woertlich, damit der Zweck der Zeile nicht verlorengeht, und er sagt, worin der Unterschied zur
bestehenden `truncated`-Quelle besteht: dort gibt es die Klammer wirklich, hier nicht.

**Die neue synthetische Quelle `commentOnlyBody`.** Eine Deklaration, deren Rumpf aus genau einer
Kommentarzeile besteht und die keine zweite Tabelle daneben hat. Ihr Kommentar sagt aus, dass diese
Zeile den **Rest** festnagelt, den die Rumpfform-Unterscheidung uebriglaesst: ein Rumpf aus Kommentar
oder Leerraum-mit-Zeichen ist fuer `strings.TrimSpace` nicht leer, also nimmt er den Abschneide-Zweig,
obwohl er nichts abgeschnitten hat. Er sagt weiter, warum das trotzdem besser ist als der Zustand
davor: die Meldung zeigt den Rumpf, den sie gelesen hat, statt eine Eigenschaft zu behaupten, die sie
nicht festgestellt hat.

**Die beiden neuen Tabellenzeilen.** Namen exakt
`a genuinely empty declaration beside a second table` (Quelle `emptyBesideASecondTable`) und
`a body carrying only a comment` (Quelle `commentOnlyBody`). Die erste erwartet die Teilzeichenkette
des Leer-Ausgangs, die zweite die Teilzeichenkette des Abschneide-Ausgangs **plus** das Fragment des
zitierten Rumpfes. Beide jetzt fahren und beide rot sehen; die Ausgaben gehoeren woertlich in die
SUMMARY, denn sie sind der Beweis, dass die Zeilen die Eigenschaft pruefen, fuer die sie geschrieben
sind.

**Dann die Unterscheidung selbst.** Den Abschneide-Zweig und den Leer-Zweig durch **einen** Zweig
`len(matches) == 0` ersetzen, in dem `strings.TrimSpace(body) == ""` die beiden Ursachen trennt: ist
der Rumpf nach dem Trimmen leer, greift die Leer-Meldung woertlich in ihrer heutigen Fassung; sonst
greift die Abschneide-Meldung, und die zitiert den Rumpf mit `%q` statt eine Zahl aus der Datei zu
nennen. Der Satz, der die Deklaration als nicht leer bezeichnet, entfaellt zusammen mit der Zaehlung,
aus der er erschlossen war.

**Die Zaehlung von `whole` wandert an ihre einzige verbleibende Verwendungsstelle** — unmittelbar vor
die Zaehlpruefung `whole != len(matches)` — und ihr Kommentar wird ersetzt. Er sagt jetzt, dass
`whole` genau eine Frage beantwortet (steht ein eintragsfoermiges Literal ausserhalb der
Deklaration), und er sagt, was der frueheren Fassung fehlte: dieselbe Zahl hat zwei Fragen
beantwortet, und die zweite Antwort war fuer eine wirklich leere Deklaration falsch.

**Den erklaerenden Kommentar ueber den drei Zweigen anpassen.** Er behauptet heute "With the count in
hand the three causes are three messages" — die Zahl ist jetzt nicht mehr in der Hand, und die
unterscheidende Groesse ist die Rumpfform. Der Kommentar nennt beide Ursachen mit ihrer eigenen
Begruendung und nennt ausdruecklich den Rest, den die neue Unterscheidung laesst.

**Das `wantErr` der bestehenden `truncated`-Zeile verkuerzen.** Es nennt heute die Zahl der Eintraege
in der ganzen Quelle; die Meldung traegt diese Zahl nicht mehr. Neu pinnt die Zeile die
Abschneide-Teilzeichenkette und das Fragment des zitierten Rumpfes, das den Kommentar sichtbar macht,
der die Klammer traegt.

**Zuletzt beide neuen Zeilen gruen sehen, die sechs bestehenden Zeilen gruen sehen und
`TestBrowserRefusalSetEqualsTheServers` unveraendert gruen sehen.** Die Rumpf- und Dateizahlen gegen
die echte Datei bleiben 6 und 6; halte beide in der SUMMARY fest.
  </action>
  <acceptance_criteria>
    - `internal/imagefactory/guard_drift_test.go` enthaelt `strings.TrimSpace(body)` woertlich — die unterscheidende Groesse ist die Form des Rumpfes
    - Die Tabelle von `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration` deklariert `wantErr` als `[]string`, und alle acht Zeilen (sechs bestehende plus zwei neue) tragen einen Slice
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration$' -count=1 -v` gibt eine Zeile `--- PASS: …/a_genuinely_empty_declaration_beside_a_second_table` aus
    - Dieselbe Ausgabe gibt eine Zeile `--- PASS: …/a_body_carrying_only_a_comment` aus
    - Dieselbe Ausgabe enthaelt genau acht `--- PASS:`-Zeilen auf Untertest-Ebene und keine `--- FAIL:`-Zeile
    - Die sechs bestehenden Zeilennamen (`the real route`, `the declaration renamed out of existence`, `the declaration absent`, `an entry-shaped literal outside the declaration`, `the declaration renamed to a prefixed name`, `the body cut short by a bracket in a comment`) sind unveraendert vorhanden und bestehen weiterhin
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalSetEqualsTheServers$' -count=1 -v` gibt `--- PASS: TestBrowserRefusalSetEqualsTheServers` aus, ohne dass dieser Test veraendert wurde
    - **Rot-vor-gruen:** die SUMMARY zitiert die Fehlausgabe beider neuer Zeilen gegen den unveraenderten Kontrollfluss — fuer die erste die Abschneide-Meldung mit `1 entries are present in the source as a whole`, fuer die zweite die Leer-Meldung
    - Die SUMMARY nennt die beiden gemessenen Anzahlen gegen die echte `web/src/routes/images.tsx` (Treffer im Deklarationsrumpf und Treffer in der ganzen Datei) und dass beide 6 sind
    - Der Kommentar ueber der Zaehlung von `whole` sagt, dass sie genau eine Frage beantwortet, und benennt, dass sie vorher zwei beantwortet hat
    - Der Kommentar der Quelle `commentOnlyBody` benennt den Rest, den `strings.TrimSpace` uebriglaesst, und warum die Aenderung ihn trotzdem wert ist
    - `git diff --stat -- web/` ist leer
    - `gofmt -l ./internal` gibt nichts aus
  </acceptance_criteria>
  <verify>
    <automated>grep -Fc 'strings.TrimSpace(body)' internal/imagefactory/guard_drift_test.go | grep -qv '^0$'</automated>
    <fails_when>Exit 1 — die Datei enthaelt den Ausdruck nicht woertlich, die Unterscheidung haengt also weiterhin an einer Zaehlung ueber die Datei statt an der Form des Rumpfes. Das ist die Messung selbst und nicht ihre Beschreibung: solange dieser Ausdruck fehlt, bekommt eine wirklich leere Deklaration neben einer zweiten Tabelle die Aufforderung, nach einer Klammer zu suchen, die es nicht gibt.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration$' -count=1 -v 2>&1 | grep -q '^    --- PASS: TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration/a_genuinely_empty_declaration_beside_a_second_table'</automated>
    <fails_when>Exit 1 der Pipeline. Drei Wege fuehren dorthin, und der erste ist der Grund fuer diese Form: die Untertest-Zeile existiert nicht, weil die Tabellenzeile nie angelegt wurde — ein `-run`-Filter, der nichts trifft, endet in Go fuer sich allein mit 0 und waere genau die Klasse Defekt, die diese Phase seit vier Runden schliesst. Sonst: das Paket uebersetzt nicht, oder die Zeile ist `--- FAIL:`, weil die Leer-Meldung nicht greift und stattdessen die Abschneide-Meldung mit `1 entries are present in the source as a whole` zurueckkommt.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration$' -count=1 -v 2>&1 | grep -c '^    --- PASS: ' | grep -qx '8'</automated>
    <fails_when>Exit 1, weil die Zahl der bestandenen Untertests nicht 8 ist. Weniger heisst, dass eine Zeile fehlt oder rot ist — eine still verschwundene Tabellenzeile ist derselbe Defekt wie ein Waechter, der nicht findet, was er bewacht, eine Ebene hoeher. Mehr heisst, dass dieser Task ueber seinen Auftrag hinaus Zeilen angelegt hat, was die Abnahme der Nachbartasks verschiebt.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalSetEqualsTheServers$' -count=1 -v 2>&1 | grep -q '^--- PASS: TestBrowserRefusalSetEqualsTheServers '</automated>
    <fails_when>Exit 1 ohne `--- PASS:`-Zeile fuer diesen Test. Weil Runde 6 neu gemessen hat, dass die beiden Ablehnungsmengen heute uebereinstimmen, kann ein Fehlschlag hier nur an diesem Task liegen: der geschnittene Rumpf traegt nicht mehr sechs Eintraege — dann nennt die Ausgabe eine Liste abweichender Codepoints — oder der neue Zweig greift faelschlich auf der echten Datei, dann nennt sie die Abschneide-Meldung mit dem zitierten Rumpf.</fails_when>
    <automated>git diff --quiet HEAD -- web/</automated>
    <fails_when>Exit 1, weil `git diff --quiet HEAD -- web/` einen Unterschied meldet — es steht also eine Aenderung unter `web/` im Baum. Dieser Plan darf `web/src/routes/images.tsx` weder netto noch voruebergehend anfassen; ein Diff dort heisst, dass der Waechter an die Deklaration angepasst wurde statt umgekehrt.</fails_when>
  </verify>
  <done>Die Leer- und die Abschneide-Meldung werden durch `strings.TrimSpace(body)` unterschieden, die Abschneide-Meldung zitiert den Rumpf, den sie gelesen hat, `whole` beantwortet nur noch die Frage nach dem Literal ausserhalb der Deklaration, und beide neuen Tabellenzeilen waren vorher rot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eine Bedingung, eine Ursache, eine Meldung — und keine Tabellenzeile, die jeder Fehlschlag erfuellt</name>
  <reversibility rating="reversible">Ein aufgeteilter `if` und geaenderte Erwartungswerte in einer Testdatei. Kein Produktionsverhalten, kein Zustand, kein Datenformat.</reversibility>
  <read_first>
    - `internal/imagefactory/guard_drift_test.go` in dem Zustand, den Task 1 hinterlassen hat —
      insbesondere `parseBrowserRefusalRanges` mit der neuen Rumpfform-Unterscheidung und die
      Tabelle mit `wantErr []string`.
    - `internal/imagefactory/guard_drift_test.go:346-394` — die Bound-Konvertierung vollstaendig: die
      beiden `strconv.ParseUint`-Aufrufe, der lange Kommentar, der die Semantik-gegen-Syntax-Trennung
      und die Bitbreite 32 begruendet, und die Bedingung `to > utf8.MaxRune || from > to` mit ihrer
      Meldung. **Diese eine Bedingung ist die Stelle, die dieser Task in zwei Zweige teilt.**
    - `internal/imagefactory/guard_drift_test.go:230-245` — der Fehlertext des
      Deklarations-Schnitts vollstaendig. Er ist der einzige Ausgang, der die Formulierung ueber den
      Praefix traegt, und er ist damit die unterscheidende Teilzeichenkette, die die drei schwachen
      Zeilen bekommen. **Beim Umstellen woertlich aus dem Quelltext uebernehmen, nicht aus diesem
      Plan abschreiben** — die Meldung ist ueber mehrere Formatierungsargumente verteilt.
    - `internal/imagefactory/guard_drift_test.go:456-514` — `TestBrowserRefusalGuardRefusesABoundItCannotRepresent`
      vollstaendig: die drei synthetischen Quellen mit den in Runde 5 gemessenen Go-Werten in ihren
      Kommentaren, und die drei Tabellenzeilen mit ihren `wantErr`-Slices. **Die Zeile
      `an inverted range` ist die, deren Erwartung dieser Task umstellt.**
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-REVIEW.md:267-366` — WR-03 und
      WR-04 vollstaendig, mit der Auszaehlung der sechs Fehlerausgaenge und der Feststellung, dass
      jeder von ihnen den Bezeichner in seinen Text formatiert, sowie den vorgeschlagenen
      Formulierungen fuer die beiden getrennten Bound-Meldungen.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-27-SUMMARY.md`, Abschnitt
      `key-decisions` — die Regel, die dieser Task nachtraegt: der statische Text einer Fehlermeldung
      enthaelt keine Literale, die eine Tabellenzeile als Teilzeichenkette prueft, sonst ist die Zeile
      trivial erfuellt und prueft die Meldung nicht mehr.
  </read_first>
  <behavior>
    - Ein Eintrag, dessen obere Grenze groesser als `utf8.MaxRune` ist, liefert eine Meldung, die von
      einer nicht darstellbaren **oberen Grenze** spricht und die verlustbehaftete Konvertierung
      erklaert.
    - Ein Eintrag, dessen untere Grenze ueber der oberen liegt, liefert eine **andere** Meldung: sie
      sagt, dass beide Grenzen darstellbar sind und die Range trotzdem nichts abdeckt, weil der Sweep
      aufwaerts laeuft. Sie erwaehnt `utf8.MaxRune` nicht.
    - Beide Meldungen nennen weiterhin beide Grenzen woertlich, wie die drei Tabellenzeilen es
      pruefen.
    - Die drei Zeilen `the declaration renamed out of existence`, `the declaration absent` und
      `the declaration renamed to a prefixed name` scheitern kuenftig, wenn die Funktion einen
      **anderen** Fehler zurueckgibt als den Kein-Anker-Ausgang — heute sind sie fuer jeden der
      sieben Fehlerausgaenge erfuellt.
    - Die echte `web/src/routes/images.tsx` loest keinen der beiden Bound-Zweige aus; ihr Eintrag
      `{ from: 0xfffe, to: 0x10ffff }` bleibt zulaessig.
  </behavior>
  <files>internal/imagefactory/guard_drift_test.go</files>
  <action>
**Zuerst die drei Erwartungen umstellen, und die Rot-Messung dafuer herstellen.**

Die Zeile `an inverted range` in `TestBrowserRefusalGuardRefusesABoundItCannotRepresent` erwartet
heute neben den beiden Grenzen die Formulierung ueber die nicht darstellbare Grenze. Sie bekommt
stattdessen die Teilzeichenkette der neuen, eigenen Inversions-Meldung. Jetzt fahren und rot sehen —
die heutige Meldung traegt sie nicht. Die Ausgabe gehoert in die SUMMARY.

Die beiden anderen Zeilen dieser Tabelle bekommen die praezisere Teilzeichenkette des
Ueberlauf-Zweigs statt der heutigen, die beide Zweige treffen wuerde.

Die drei Zeilen `the declaration renamed out of existence`, `the declaration absent` und
`the declaration renamed to a prefixed name` in
`TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration` bekommen je die Teilzeichenkette, die
**nur** der Kein-Anker-Ausgang traegt: der Satzanfang, der sagt, dass keine Deklaration als
Array-Literal gefunden wurde. Die Praefix-Zeile bekommt zusaetzlich den Satz ueber den Namen, der den
gesuchten nur als Praefix traegt — sie traegt die Kernbehauptung von Plan 02-27 und soll sie
ausdruecklich pinnen. **Beide Teilzeichenketten woertlich aus dem Fehlertext im Quelltext
uebernehmen.**

Diese drei Zeilen koennen nicht rot-vor-gruen laufen, weil die Eigenschaft, die sie neu pinnen, heute
schon gilt; was fehlt, ist die Pinnung. Der Fail-first-Ersatz ist deshalb ausdruecklich
vorgeschrieben: die unterscheidende Teilzeichenkette einmal aus dem Fehlertext des
Deklarations-Schnitts entfernen, die drei Zeilen rot sehen, die Ausgabe in die SUMMARY schreiben und
die Aenderung zurueckziehen. Ohne diesen Schritt behauptet die Umstellung eine Wirkung, die niemand
gemessen hat — und genau das ist die Gattung Befund, an der diese Phase seit Runde 3 arbeitet.

**Dann die Bedingung teilen.** Aus der einen Bedingung werden zwei aufeinanderfolgende Zweige. Der
erste prueft, ob die obere Grenze `utf8.MaxRune` ueberschreitet, und behaelt die heutige Erklaerung
ueber die verlustbehaftete `rune`-Konversion; sein Kopfsatz spricht ausdruecklich von der **oberen**
Grenze. Der zweite prueft, ob die untere Grenze ueber der oberen liegt, und bekommt seine **eigene**
Ursache: beide Grenzen sind darstellbar, und die Range deckt trotzdem nichts ab, weil der Sweep in
`TestBrowserRefusalSetEqualsTheServers` von `rune(0)` aufwaerts laeuft und eine invertierte Range nie
betritt. Beide Zweige schliessen mit dem Satz, den die Lesbarkeitsschleife bereits fuehrt und den die
heutige Meldung ebenfalls fuehrt: ein Eintrag, den der Waechter uebergeht, ist ein Codepoint, ueber
den er Uebereinstimmung meldet, ohne verglichen zu haben.

Den langen Kommentar oberhalb der Bedingung um den Punkt ergaenzen, den WR-03 aufmacht: derselbe Plan,
der drei Zeilen darueber "none of them claims another's" als Prinzip formuliert hat, hat es an dieser
einen Stelle nicht eingehalten, und die Testzeile hat die unzutreffende Formulierung festgenagelt.

**Zuletzt alle drei Tabellen einzeln fahren und die Untertestzahlen pruefen** — acht, zwei und drei.
Die Zahl gehoert geprueft und nicht nur der Zustand des Elterntests, weil ein Tabellentest mit leerer
Tabelle gruen ist.
  </action>
  <acceptance_criteria>
    - `parseBrowserRefusalRanges` enthaelt zwei getrennte Zweige: einen, dessen Bedingung ausschliesslich die obere Grenze gegen `utf8.MaxRune` prueft, und einen, dessen Bedingung ausschliesslich die untere gegen die obere prueft. Die Datei enthaelt keine Bedingung mehr, die beide Vergleiche mit einem logischen Oder verbindet
    - Die Meldung des Inversions-Zweigs erwaehnt `utf8.MaxRune` nicht und nennt beide Grenzen woertlich
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesABoundItCannotRepresent$' -count=1 -v` gibt drei `--- PASS:`-Untertestzeilen und keine `--- FAIL:`-Zeile aus
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration$' -count=1 -v` gibt acht `--- PASS:`-Untertestzeilen und keine `--- FAIL:`-Zeile aus
    - Keine `wantErr`-Zeile der Datei besteht mehr aus einem einzelnen Element, das nur der Bezeichner ist; jede prueft eine Teilzeichenkette, die mindestens ein Wort ausserhalb des Bezeichners enthaelt
    - **Rot-vor-gruen fuer die Inversions-Zeile:** die SUMMARY zitiert deren Fehlausgabe gegen die ungeteilte Bedingung
    - **Fail-first-Ersatz fuer die drei Deklarations-Zeilen:** die SUMMARY zitiert die Rot-Ausgabe, die entsteht, wenn die unterscheidende Teilzeichenkette einmal aus dem Fehlertext entfernt wird, und belegt, dass die Aenderung zurueckgenommen wurde
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalSetEqualsTheServers$' -count=1 -v` gibt `--- PASS:` aus; der Eintrag `{ from: 0xfffe, to: 0x10ffff }` der echten Datei wird von keinem der beiden neuen Zweige abgelehnt
    - `git diff --stat -- web/` ist leer
    - `gofmt -l ./internal` gibt nichts aus
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesABoundItCannotRepresent$' -count=1 -v 2>&1 | grep -c '^    --- PASS: ' | grep -qx '3'</automated>
    <fails_when>Exit 1, weil die Zahl der bestandenen Untertests nicht 3 ist. Der wahrscheinliche Weg dorthin ist die Zeile `an inverted range`: sie erwartet jetzt die Formulierung des eigenen Inversions-Zweigs, und solange die Bedingung ungeteilt ist, kommt die Ueberlauf-Meldung zurueck und die Zeile ist `--- FAIL:`. Weniger als 3 heisst ausserdem, dass eine Zeile entfernt wurde, was dieser Plan verbietet.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration$' -count=1 -v 2>&1 | grep -c '^    --- PASS: ' | grep -qx '8'</automated>
    <fails_when>Exit 1, weil die Zahl der bestandenen Untertests nicht 8 ist. Nach der Umstellung der drei Deklarations-Zeilen ist der Weg dorthin: die aus dem Quelltext uebernommene Teilzeichenkette stimmt nicht woertlich mit der Meldung ueberein — die Meldung ist ueber mehrere Formatierungsargumente verteilt, und ein aus diesem Plan abgeschriebener Wortlaut trifft sie nicht.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalSetEqualsTheServers$' -count=1 -v 2>&1 | grep -q '^--- PASS: TestBrowserRefusalSetEqualsTheServers '</automated>
    <fails_when>Exit 1 ohne `--- PASS:`-Zeile. Der einzige Weg dorthin, den dieser Task oeffnen kann, ist ein zu scharfer Ueberlauf-Zweig: der Eintrag `{ from: 0xfffe, to: 0x10ffff }` der echten Datei liegt genau auf `utf8.MaxRune`, und ein Vergleich mit `>=` statt `>` wuerde ihn ablehnen. Die Ausgabe nennt dann die Bound-Meldung mit `from 0xfffe to 0x10ffff`.</fails_when>
    <automated>go build ./... && go vet ./... && go test ./internal/imagefactory/... -count=1</automated>
    <fails_when>Nicht-Null-Exit, oder `FAIL` in der Ausgabe. `go vet` faellt insbesondere ueber eine Formatierungs-Direktive, deren Argumentzahl nach dem Teilen der Bedingung nicht mehr stimmt.</fails_when>
  </verify>
  <done>Ueberlauf und Inversion sind zwei Zweige mit zwei Ursachen, die Inversions-Meldung nennt `utf8.MaxRune` nicht, keine Tabellenzeile ist mehr von jedem Fehlerausgang erfuellt, und beide Umstellungen sind rot gemessen worden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Der Waechter zaehlt die Deklarationen, die er gefunden hat — und der Doc-Kommentar steht unter dem Symbol, das er beschreibt</name>
  <reversibility rating="reversible">Eine zusaetzliche Pruefung und eine Leerzeile in einer Testdatei. Kein Produktionsverhalten, kein Zustand.</reversibility>
  <precondition>Die Rumpfform-Unterscheidung aus Task 1 steht, denn die zweite neue Tabellenzeile dieses Tasks misst gegen die Meldung, die erst dadurch entsteht. Pruefen mit `grep -Fc 'strings.TrimSpace(body)' internal/imagefactory/guard_drift_test.go` — erwartet mindestens 1. Ist sie nicht da, ist die erwartete Rot-Ausgabe eine andere und der Fail-first-Beleg dieses Tasks waere falsch etikettiert.</precondition>
  <read_first>
    - `internal/imagefactory/guard_drift_test.go` in dem Zustand, den Task 2 hinterlassen hat.
    - `internal/imagefactory/guard_drift_test.go:57-110` — der zusammenhaengende Kommentarblock
      vollstaendig, `var refusedRangesEntry` und `var refusedRangesDecl`. **Die Bruchstelle liegt
      dort, wo der Satz ueber die Abschneide-Diagnose endet und der Satz beginnt, der
      `refusedRangesEntry` beschreibt; genau dort fehlt die Leerzeile.** Beide Haelften sind
      inhaltlich vollstaendig und muessen nicht umgeschrieben, sondern nur vor ihre jeweilige
      Deklaration gezogen werden.
    - `internal/imagefactory/guard_drift_test.go:224-235` — der Deklarations-Schnitt:
      `refusedRangesDecl.FindStringSubmatch(source)` und der Umgang mit dem Nil-Fall. **Der Aufruf,
      der genau einen Treffer liefert und nie fragt, ob es einen zweiten gibt, ist die Stelle, die
      dieser Task ersetzt.**
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-REVIEW.md:367-409` — WR-05
      vollstaendig, mit der Feststellung, dass 25 der 49 Kommentarzeilen in Runde 6 in den falschen
      Block geschrieben wurden, und der vorgeschlagenen Aufteilung.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-PATTERNS.md`, Abschnitt
      `### Comment register` — die Konvention dieses Codebestands: ein Doc-Kommentar nennt die
      Entscheidung und ihre Gegenprobe, nicht den Mechanismus. Der neue Kommentar der
      Duplikatspruefung wird daran gemessen.
  </read_first>
  <behavior>
    - Eine Quelle mit zwei Deklarationen desselben Bezeichners — eine eingerueckte innere vor der
      aeusseren — liefert einen Fehler, der die **Zahl** der gefundenen Deklarationen nennt. Heute
      liefert sie die Zaehlpruefung mit `1 entries inside the REFUSED_RANGES declaration, 7 in the
      source as a whole`, also die Ursache eines anderen Defekts.
    - Eine Quelle, deren innere Deklaration leer ist und deren aeussere die echte Tabelle traegt,
      liefert denselben Fehler. Heute liefert sie nach Task 1 die Leer-Meldung, waehrend die echte
      Tabelle sechs Eintraege traegt.
    - Die echte `web/src/routes/images.tsx` traegt genau eine Deklaration und loest die Pruefung nicht
      aus. Beim Planen gemessen: `FindAllStringSubmatch` liefert dort 1 Treffer.
    - Die Pruefung greift **vor** dem Lesen des Rumpfes, damit die Ursache nicht von einer spaeteren
      Pruefung ueberschrieben wird.
    - `go doc` zeigt einen Doc-Kommentar an `refusedRangesEntry` und einen eigenen an
      `refusedRangesDecl`.
  </behavior>
  <files>internal/imagefactory/guard_drift_test.go</files>
  <action>
**Zuerst die beiden Tabellenzeilen, und beide muessen rot laufen.**

Zwei neue synthetische Quellen in
`TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration`. Die erste, `shadowedByAnInnerTable`,
traegt eine in eine Funktion eingerueckte Deklaration des bewachten Bezeichners mit einem
eintragsfoermigen Literal, und darunter die echte, aeussere Deklaration mit zwei Eintraegen. Die
zweite, `shadowedByAnEmptyInnerTable`, traegt dieselbe Struktur, wobei die innere Deklaration leer
ist. Beide Kommentare nennen die heute gemessene Fehlausgabe woertlich und sagen, warum sie die
falsche Ursache ist: der Anker steht auf `^` mit vorangehendem Leerraum, also erfuellt auch eine
eingerueckte Deklaration ihn, und der Waechter nimmt schweigend die erste. Der Kommentar der zweiten
Quelle sagt zusaetzlich, dass ihre falsche Ursache erst durch Task 1 dieses Plans entstanden ist —
das gehoert dazu, weil es die Begruendung dafuer ist, dass diese Pruefung in dieselbe Runde gehoert.

Die beiden Tabellenzeilen heissen exakt `two declarations, an indented inner one first` und
`an empty inner declaration before the real one`. Beide erwarten die Teilzeichenkette der neuen
Duplikat-Meldung. Jetzt fahren, beide rot sehen, beide Ausgaben in die SUMMARY.

**Dann die Pruefung.** Den Deklarations-Schnitt von einem Aufruf, der einen Treffer liefert, auf
einen umstellen, der alle Treffer liefert. Kein Treffer bleibt der bestehende Kein-Anker-Ausgang mit
seinem unveraenderten Text. Mehr als ein Treffer wird ein **eigener** Fehlerausgang, dessen Meldung
die Zahl nennt und sagt, warum die Mehrdeutigkeit ein Fehlschlag und keine stille Auswahl ist: der
Waechter vergleicht Mengen, und eine von zweien zu lesen heisst, ueber die andere Uebereinstimmung zu
melden, ohne sie verglichen zu haben — derselbe Satz, den die Lesbarkeitsschleife und die
Bound-Zweige bereits fuehren. Genau ein Treffer laeuft weiter wie bisher; der Rumpf ist die erste
Gruppe dieses einen Treffers.

Der Kommentar an dieser Pruefung nennt die Entscheidung und ihre Gegenprobe, nicht den Mechanismus —
das ist die Konvention dieses Codebestands. Die Entscheidung: der Waechter waehlt nicht, sondern
verlangt Eindeutigkeit. Die Gegenprobe: eine eingerueckte Zweitdeklaration ist nicht theoretisch, sie
ist die Form, in der eine Tabelle waehrend eines Refactors kurzzeitig doppelt existiert, und der
frueher greifende Zaehlvergleich hat sie mit der Ursache eines anderen Defekts erklaert.

**Dann WR-05.** Eine Leerzeile an der Bruchstelle des Kommentarblocks einziehen und die beiden
Haelften vor ihre jeweiligen Deklarationen ziehen, sodass der Block ueber die
Deklarations-Ausschneidung unmittelbar vor `var refusedRangesDecl` steht und der Block ueber das
Eintrags-Literal unmittelbar vor `var refusedRangesEntry`. Beide Haelften bleiben inhaltlich
unveraendert; die Begruendung der Anker-Entscheidung aus Runde 6, 25 Zeilen davon in dieser Runde
gewachsen, wandert damit unter das Symbol, das sie betrifft.

**Zuletzt die Tabelle fahren und zehn bestandene Untertests sehen.**
  </action>
  <acceptance_criteria>
    - `parseBrowserRefusalRanges` ruft eine Methode auf, die **alle** Treffer des Deklarations-Ausdrucks liefert, und verzweigt auf deren Anzahl. Die Datei enthaelt keinen `FindStringSubmatch`-Aufruf auf `refusedRangesDecl` mehr
    - Die Duplikat-Meldung nennt die Zahl der gefundenen Deklarationen und den Bezeichner
    - Die Pruefung steht vor dem Lesen des Rumpfes; keine der beiden neuen Tabellenzeilen bekommt eine Rumpf- oder Zaehl-Meldung
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration$' -count=1 -v` gibt eine Zeile `--- PASS: …/two_declarations,_an_indented_inner_one_first` aus
    - Dieselbe Ausgabe gibt eine Zeile `--- PASS: …/an_empty_inner_declaration_before_the_real_one` aus
    - Dieselbe Ausgabe enthaelt genau zehn `--- PASS:`-Zeilen auf Untertest-Ebene und keine `--- FAIL:`-Zeile
    - Die Zeile unmittelbar vor `var refusedRangesDecl = regexp.MustCompile(` ist eine Kommentarzeile
    - Die Zeile unmittelbar vor `var refusedRangesEntry = regexp.MustCompile(` ist eine Kommentarzeile
    - **Rot-vor-gruen:** die SUMMARY zitiert die Fehlausgabe beider neuer Zeilen gegen den Zustand vor der Pruefung — fuer die erste die Zaehlpruefung mit `1 entries inside the REFUSED_RANGES declaration, 7 in the source as a whole`, fuer die zweite die Leer-Meldung
    - Die SUMMARY belegt, dass die echte `web/src/routes/images.tsx` genau eine Deklaration traegt, und nennt die gemessene Zahl
    - `git diff --stat -- web/` ist leer
    - `gofmt -l ./internal` gibt nichts aus
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration$' -count=1 -v 2>&1 | grep -c '^    --- PASS: ' | grep -qx '10'</automated>
    <fails_when>Exit 1, weil die Zahl der bestandenen Untertests nicht 10 ist. Weniger heisst, eine der beiden neuen Zeilen fehlt oder ist rot — im zweiten Fall greift die Duplikatspruefung nicht vor dem Lesen des Rumpfes und die Ausgabe nennt statt der Zahl der Deklarationen entweder den Zaehlvergleich oder die Leer-Meldung.</fails_when>
    <automated>awk '/^var refusedRangesDecl = regexp.MustCompile\(/{print prev} {prev=$0}' internal/imagefactory/guard_drift_test.go | grep -q '^//'</automated>
    <fails_when>Exit 1, weil die Zeile unmittelbar vor der Deklaration von `refusedRangesDecl` keine Kommentarzeile ist. Dann haengt die Begruendung der Anker-Entscheidung godoc-seitig weiterhin unter dem Nachbarsymbol — WR-05 ist unveraendert offen und in dieser Runde noch einmal gewachsen.</fails_when>
    <automated>awk '/^var refusedRangesEntry = regexp.MustCompile\(/{print prev} {prev=$0}' internal/imagefactory/guard_drift_test.go | grep -q '^//'</automated>
    <fails_when>Exit 1, weil die Zeile unmittelbar vor der Deklaration von `refusedRangesEntry` keine Kommentarzeile ist. Zusammen mit der vorigen Pruefung heisst das: der 49-zeilige Block ist nicht geteilt worden, sondern nur verschoben, und eines der beiden Symbole steht weiterhin ohne Doc-Kommentar da.</fails_when>
    <automated>go build ./... && go vet ./... && go test ./internal/imagefactory/... -count=1</automated>
    <fails_when>Nicht-Null-Exit, oder `FAIL` in der Ausgabe. Nach diesem Task ist der wahrscheinlichste Weg dorthin eine Duplikatspruefung, die auf der echten `web/src/routes/images.tsx` greift — dann faellt `TestBrowserRefusalSetEqualsTheServers` mit der Duplikat-Meldung statt mit einer Codepoint-Liste, und die Zahl in dieser Meldung ist der Beleg.</fails_when>
    <automated>git diff --quiet HEAD -- web/</automated>
    <fails_when>Exit 1, weil `git diff --quiet HEAD -- web/` einen Unterschied meldet — es steht also eine Aenderung unter `web/` im Baum. Dieser Plan darf `web/src/routes/images.tsx` weder netto noch voruebergehend anfassen.</fails_when>
  </verify>
  <done>Der Waechter verlangt genau eine Deklaration und sagt es, wenn er mehrere findet; die beiden neuen Zeilen waren vorher rot mit je einer anderen falschen Ursache; und beide Regexp-Deklarationen tragen ihren eigenen Doc-Kommentar.</done>
</task>

</tasks>

## Artifacts this phase produces

| Kind | Symbol | Location |
|---|---|---|
| control-flow change | die Unterscheidung zwischen Leere und Abschneiden laeuft ueber `strings.TrimSpace(body)` statt ueber `whole`; `whole` beantwortet nur noch die Frage nach dem Literal ausserhalb der Deklaration | `internal/imagefactory/guard_drift_test.go` |
| error case | Rumpf vor dem ersten Eintrag abgeschnitten — die Meldung zitiert den gelesenen Rumpf mit `%q` statt eine Dateizaehlung zu nennen | `internal/imagefactory/guard_drift_test.go` |
| error case | obere Grenze oberhalb `utf8.MaxRune`, mit der Erklaerung der verlustbehafteten `rune`-Konversion und ohne den Inversionsfall | `internal/imagefactory/guard_drift_test.go` |
| error case | invertierte Range (`from > to`) mit eigener Ursache: beide Grenzen darstellbar, der Sweep laeuft aufwaerts | `internal/imagefactory/guard_drift_test.go` |
| error case | mehr als eine Deklaration des bewachten Bezeichners in der Quelle, mit der Zahl der gefundenen Deklarationen im Text | `internal/imagefactory/guard_drift_test.go` |
| type change | `wantErr` in `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration` wird `[]string`, wie in den beiden anderen Tabellen | `internal/imagefactory/guard_drift_test.go` |
| test row | `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration/a genuinely empty declaration beside a second table` (Zeile 7) | `internal/imagefactory/guard_drift_test.go` |
| test row | `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration/a body carrying only a comment` (Zeile 8) | `internal/imagefactory/guard_drift_test.go` |
| test row | `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration/two declarations, an indented inner one first` (Zeile 9) | `internal/imagefactory/guard_drift_test.go` |
| test row | `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration/an empty inner declaration before the real one` (Zeile 10) | `internal/imagefactory/guard_drift_test.go` |
| test source | die synthetischen Quellen `emptyBesideASecondTable`, `commentOnlyBody`, `shadowedByAnInnerTable`, `shadowedByAnEmptyInnerTable` | `internal/imagefactory/guard_drift_test.go` |
| doc placement | `var refusedRangesEntry` und `var refusedRangesDecl` tragen je einen eigenen Doc-Kommentar; der 49-zeilige Block ist geteilt | `internal/imagefactory/guard_drift_test.go` |
| measurement | Rot-Ausgaben der vier neuen Zeilen und der Fail-first-Ersatz fuer die drei umgestellten `wantErr`-Zeilen | `02-29-SUMMARY.md` |
| measurement | Rumpf- und Dateizahl gegen die echte `images.tsx` (6 / 6) und die Zahl der dort gefundenen Deklarationen (1) | `02-29-SUMMARY.md` |
| measurement | die Feststellung, dass `IN-01` (zwei Fundorte fuer `%d entries`) als Nebenwirkung entfaellt, mit Begruendung | `02-29-SUMMARY.md` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `web/src/routes/images.tsx` → `internal/imagefactory/guard_drift_test.go` | ein Browser kann kein Go-Symbol importieren, also transkribiert die TypeScript-Seite, was Go entscheidet. Diese Naht wird von einem regulaeren Ausdruck ueber eine Quelldatei ueberquert, nicht von einem Typsystem |
| die Meldung des Waechters → der Mensch, der sie liest | eine Fehlermeldung ist die einzige Anweisung, die dieser Waechter je gibt. Nennt sie die falsche Ursache, sucht der Leser am falschen Ort und schliesst am Ende auf einen Defekt, den es nicht gibt |
| die Zahl `whole` → zwei verschiedene Fragen | eine Groesse, die zwei Fragen beantwortet, beantwortet mindestens eine davon aus Zufall. Genau das ist in Runde 6 passiert |
| die Erwartung einer Tabellenzeile → die Eigenschaft, nach der sie benannt ist | eine Zeile, deren `wantErr` jeder Fehlerausgang erfuellt, prueft `err != nil` und behauptet, eine Diagnose zu pinnen |

## STRIDE Threat Register

ASVS-Level 1, blockierend ab `high`. Ids T-02-126..T-02-128; `02-28-SUMMARY.md:297` haelt fest, dass
T-02-126 die erste freie Id ist. Die naechste freie Id nach diesem Plan ist T-02-129.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-126 | Repudiation | die Abschneide-Meldung behauptet aus einer Dateizaehlung heraus, die Deklaration sei nicht leer, und schickt den Leser nach einer Klammer, die es nicht gibt. Gemessen: eine wirklich leere `= []` neben einer zweiten Tabelle | medium | mitigate | die Unterscheidung laeuft ueber `strings.TrimSpace(body)`, also ueber eine Eigenschaft der Deklaration; die Abschneide-Meldung zitiert den gelesenen Rumpf mit `%q` und zeigt damit ihren Beleg. Zwei Tabellenzeilen halten die beiden Ursachen auseinander, beide vorher rot gemessen |
| T-02-127 | Repudiation | die Bound-Meldung erklaert eine invertierte Range mit einer verlustbehafteten Konvertierung, die bei ihr nicht stattfindet; ein Operator liest, sein Wert liege oberhalb `utf8.MaxRune`, und sucht dort | low | mitigate | zwei Zweige, zwei Ursachen, zwei Texte; die Inversions-Meldung erwaehnt `utf8.MaxRune` nicht, und die Tabellenzeile `an inverted range` pinnt die neue Formulierung nach einer Rot-Messung gegen die alte |
| T-02-128 | Tampering | der Waechter nimmt schweigend die erste von mehreren Deklarationen des bewachten Bezeichners. Gemessen: eine eingerueckte innere Deklaration vor der echten wird gelesen, waehrend die echte Tabelle ungeprueft daneben steht; die heutige Diagnose nennt dafuer die Ursache eines anderen Defekts | high | mitigate | die Zahl der gefundenen Deklarationen wird festgestellt und mehr als eine ist ein eigener Fehlerausgang, der die Zahl nennt; die Pruefung greift vor dem Lesen des Rumpfes, damit keine spaetere Pruefung die Ursache ueberschreibt. Zwei Tabellenzeilen, beide vorher rot mit je einer anderen falschen Ursache |
| T-02-SC | Tampering | npm/pip/cargo installs | low | mitigate | dieser Plan fuegt kein Go-Modul und kein npm-Paket hinzu; es gibt keinen Installationsschritt und daher nichts zu pruefen. `internal/imagefactory/guard_drift_test.go` benutzt ausschliesslich die Standardbibliothek; `strings`, `regexp`, `fmt`, `strconv`, `os`, `testing` und `unicode/utf8` sind bereits importiert und es kommt keiner hinzu |
</threat_model>

<verification>
- `go build ./... && go vet ./...` ist sauber
- `go test ./internal/imagefactory/... -count=1` ist gruen
- `go test ./... -count=1 -race` ist ueber alle Pakete gruen. **Nicht** `task test`: das `task`-Binary ist auf diesem Host nicht auf dem PATH, und `Taskfile.yml` definiert `test` als genau dieses Kommando
- `golangci-lint run` endet mit 0 (Binary unter `~/go/bin/golangci-lint`, siehe Ledger-Eintrag 34)
- `gofmt -l ./internal` gibt nichts aus
- Die drei Tabellentests werden **einzeln** mit `-run '^Name$' -v` gefahren und auf ihre `--- PASS:`-Zeilen geprueft, nicht nur ueber den Exit-Code der Suite — ein `-run`-Filter, der nichts trifft, endet in Go mit 0. Das ist die Eigenschaft, um die es in dieser Luecke geht, und sie darf in ihrer eigenen Verifikation nicht wieder auftreten
- Die **Zahl** der bestandenen Untertests wird geprueft (10 / 2 / 3) und nicht nur der Zustand des Elterntests, weil ein Tabellentest mit leerer Tabelle gruen ist
- **Jede der vier neuen Zeilen wurde rot gesehen, bevor sie gruen war**, und die vier Fehlausgaben stehen woertlich in der SUMMARY. Fuer die drei umgestellten `wantErr`-Zeilen, deren Eigenschaft heute schon gilt, steht stattdessen die Rot-Ausgabe des vorgeschriebenen Fail-first-Ersatzes dort
- `git diff --stat -- web/` ist ueber den ganzen Plan leer. `npm --prefix web run test` und `npm --prefix web run typecheck` werden **nicht** gefahren: dieser Plan aendert keine Datei unter `web/` und faehrt keinen Lebendbeleg, der eine anfasst
- `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` bleibt bei `"ok": true` und `total_count: 71` — **dieser Plan schreibt den Ledger nicht.** Plan 02-31 besitzt `.planning/WINDOWS.md` fuer diese Runde
- `.planning/REQUIREMENTS.md` wird von diesem Plan nicht geschrieben, und `requirements-completed` in der SUMMARY bleibt leer
</verification>

<success_criteria>
- Die Leer-Meldung und die Abschneide-Meldung werden durch die Form des Rumpfes unterschieden; eine wirklich leere Deklaration neben einer zweiten Tabelle bekommt die Leer-Meldung, und die Abschneide-Meldung zitiert den Rumpf, den sie gelesen hat. Das ist Punkt 4 der Schliessbedingung von Ledger-Eintrag 72
- Der Rest, den `strings.TrimSpace` uebriglaesst — ein Rumpf aus reinem Kommentar nimmt den Abschneide-Zweig —, ist nicht in eine Fussnote geschrieben, sondern von einer eigenen Tabellenzeile festgenagelt und an Plan 02-31 fuer Eintrag 72 uebergeben
- `whole` beantwortet genau eine Frage; die Zaehlpruefung `whole != len(matches)` ist woertlich erhalten und weiterhin gruen
- Ueberlauf und Inversion sind zwei Zweige mit zwei Ursachen; die Inversions-Meldung nennt `utf8.MaxRune` nicht
- Keine Tabellenzeile prueft mehr eine Zeichenkette, die jeder Fehlerausgang der Funktion traegt
- Der Waechter verlangt genau eine Deklaration des bewachten Bezeichners und nennt die Zahl, wenn er mehrere findet; die echte `images.tsx` traegt genau eine und wird nicht falsch-rot
- `var refusedRangesDecl` und `var refusedRangesEntry` tragen je einen eigenen Doc-Kommentar
- Die drei Regressionssperren der Vorrunden bleiben erhalten und rot-faehig: praefixierte Umbenennung, Deklaration hinter `//`, Grenze oberhalb `utf8.MaxRune` (Punkte 1, 2 und 3 der Schliessbedingung von Eintrag 72)
- `TestBrowserRefusalSetEqualsTheServers` misst weiterhin dasselbe und bleibt gruen; die Ablehnungsmenge wurde auf keiner der beiden Seiten angefasst
- Die drei Tabellen haben 10, 2 und 3 Zeilen, und keine bestehende Zeile wurde entfernt oder umbenannt
- **Dieser Plan behauptet nicht, G-02-29 sei geschlossen** — er beruehrt die Anker-Blindheit nicht. Er schliesst G-02-28, den in Runde 6 entstandenen Spiegel-Defekt; ob auch das gilt, misst eine Verifikationsrunde
- **G-02-9 bleibt offen** und keine Zeile dieses Plans sagt etwas anderes; die Ledger-Eintraege 56, 58, 66 und 70 bleiben unberuehrt
- `.planning/WINDOWS.md` und `.planning/REQUIREMENTS.md` werden von diesem Plan **nicht** geschrieben — beide gehoeren Plan 02-31
</success_criteria>

<output>
Create `.planning/phases/02-transport-seam-talossim-image-factory/02-29-SUMMARY.md` when done
</output>
