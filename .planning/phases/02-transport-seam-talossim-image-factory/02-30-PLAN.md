---
phase: 02-transport-seam-talossim-image-factory
plan: 30
type: execute
wave: 2
depends_on: ["02-29"]
gap_closure: true

# gap_ids ist die maschinenlesbare Behauptung "dieser Plan bearbeitet diese Luecken".
#
# **Dieser Plan traegt G-02-29 und SCHLIESST SIE NICHT.** Das ist kein Versehen und keine
# Formulierungsschwaeche, sondern die Sache selbst: `02-DECISION-drift-guard-lexik.md` empfiehlt
# Variante B — den ANSPRUCH des Waechters auf das verengen, was er messen kann — und haelt
# ausdruecklich fest, dass der DEFEKT damit bestehen bleibt ("Was es nicht schliesst: Den Defekt.
# Die Leiche bleibt lesbar, der Waechter bleibt gruen darueber"). Ledger-Eintrag 70 bleibt `open`,
# und Plan 02-31 legt den amendierenden Eintrag 72 an, der ebenfalls `open` bleibt und eine
# sechspunktige, pruefbare Schliessbedingung traegt.
#
# Diese Buchfuehrung folgt dem Praezedenzfall aus derselben Phase: Plan 02-27 fuehrte `G-02-25` in
# `gap_ids` und verbot sich in seinen `prohibitions` ausdruecklich, sie als geschlossen zu melden.
# `02-VERIFICATION.md` Runde 6 hat genau das gelobt.
#
#   G-02-29  ==  02-VERIFICATION.md Runde 6, gaps[0], die ANKER-Haelfte   (rundenlokal G6-1a)
#            ==  02-REVIEW.md Runde 6, WR-01
#            ==  02-VERIFICATION.md Runde 6, Wahrheitszeile 6 und Abschnitt 4, Zeile 1
#            Die SPIEGEL-Haelfte derselben Wahrheitszeile ist G-02-28 und gehoert Plan 02-29.
#
# Naechste freie Nummer nach dieser Runde: G-02-31.
gap_ids: [G-02-29]
gap_id_mapping:
  - gap_id: G-02-29
    verification_round: 6
    round_local_label: G6-1a
    verification_status: partial
    review_finding: "WR-01 (ein REFUSED_RANGES in einem Blockkommentar oder Template-Literal erfuellt den Anker weiterhin)"
    truth: "A guard reports a pass only when it measured the property it is named for — der Browser-Ablehnungs-Waechter meldet keine Zustimmung ueber eine Tabelle, die der Browser nicht ausfuehrt"
    id_decision: "neue Id — Runde 6 fuehrt G-02-25 als geschlossen und `gaps_remaining` als leer; dieser Befund ist eine zweite Familie (lexikalischer Kontext statt Name) und kein Rest der ersten"
    disposition: "VERENGT, NICHT GESCHLOSSEN. Dieser Plan bringt die Behauptung des Waechters auf null Ueberhang und macht die Blindheit messbar; der Defekt bleibt und wird von Plan 02-31 als Ledger-Eintrag 72 gefuehrt."

files_modified:
  - internal/imagefactory/guard_drift_test.go
# `web/src/routes/images.tsx` steht bewusst NICHT hier und wird von diesem Plan weder netto noch
# voruebergehend angefasst. Alle Blindheits-Fixtures sind synthetische Quellen, die der Test in ein
# `t.TempDir()` schreibt und ueber DENSELBEN Lesepfad faehrt, den der Live-Waechter benutzt.
#
# **Eine Aenderung an `web/src/routes/images.tsx`, die der Entwurf von Variante B vorsieht, wird
# hier ausdruecklich NICHT ausgefuehrt:** die drei Hinweiszeilen ueber der Deklaration bei Zeile 105.
# Der Grund ist ihr eigener, im Entscheidungsdokument benannter Preis — sie verschieben alles
# darunter um drei Zeilen und machen rund ein Dutzend `images.tsx:NNN`-Verweise im Planungsbestand
# ungueltig, deren Korrektur in dieselbe Aenderung gehoerte. Der Umfang dieser Runde ist auf
# `internal/imagefactory/guard_drift_test.go` und die beiden Register begrenzt; die Naht unter `web/`
# wird von dieser Runde nicht wieder geoeffnet. Damit fehlt B das einzige Geraet, das den Menschen
# erreicht, der die Leiche erzeugt — und genau das gehoert in Ledger-Eintrag 72 und nicht in eine
# Fussnote. Plan 02-31 traegt es dort ein.
autonomous: false
# `false`, weil der erste Task ein `checkpoint:decision` mit `gate="blocking-human"` ist. Die
# Richtung dieser Runde ist NICHT ratifiziert, und die Wellengrenze zwischen 02-29 und 02-30 ist
# KEIN Mechanismus: `/gsd-execute-phase` faehrt Welle 1 -> 2 -> 3 ohne Unterbrechung durch, also
# stuende an dieser Grenze niemand, und die Billigkeit des Wechsels auf Variante A wuerde nie
# realisiert. Eine Absicherung, die zu schuetzen behauptet, ohne die Eigenschaft herzustellen, ist
# woertlich die Gattung, an der diese Phase seit Runde 3 arbeitet. `blocking-human` und nicht
# `blocking`, weil `blocking` im Auto-Modus die erste Option selbst waehlt — und eine Entscheidung,
# deren Vorgabe-Antwort falsch anzunehmen waere, ist genau der Fall, fuer den das Tor existiert.
requirements: [FACT-01, FACT-06]
# Die uebrigen zwoelf Phasen-Requirements erscheinen in den `requirements`-Frontmattern der Plaene
# 02-01…02-26; `02-VERIFICATION.md` Runde 6, Abschnitt 6 stellt phasenweit fest: "Orphaned
# requirements: keine." Plan 02-31 dieser Runde traegt alle vierzehn, weil sein Task 2 die
# Statuszeile jeder einzelnen im Traceability-Register prueft.

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Der Waechter behauptet nur noch, was er misst. Der allquantifizierte Satz `Renamed, moved or deleted is the same as never having been there` verschwindet aus dem Fehlertext UND aus dem Doc-Kommentar von `parseBrowserRefusalRanges`; an seine Stelle tritt eine Aussage ueber den TEXT plus eine geschriebene Ausschlussliste."
    - "Die Blindheit ist nach MECHANISMUS skopiert und nicht als `non-code`: der Anker bindet an ein LITERAL, nicht an einen WERT, und an einen BEZEICHNER, nicht an CODE. Eine Liste, die nur Kommentarformen aufzaehlt, liesse sich buchstabengetreu abarbeiten, waehrend der Ableitungspfad (`].filter(…)`, `…SPREAD`) offen und ungeledgert bliebe — und der ist der gefaehrlichste bekannte Rest, weil er auf lebendem, kompilierendem, referenziertem Code gruen ist."
    - "Die Ausschlussliste ist DATEN und nicht Prosa. `guardBlindSpots` traegt je einen Eintrag pro Mechanismus mit seiner gemessenen Ausgabe, `honestClaim` wird aus dieser Liste gerendert, und `TestGuardBlindSpotsAreEachMeasured` erzwingt Mengengleichheit zwischen den gelisteten Ids und den von Tabellenzeilen gemessenen. Ein Eintrag ohne messende Zeile ist rot; eine Zeile ohne gelisteten Eintrag ist rot."
    - "Die Blindheitstabellen laufen ueber DENSELBEN Pfad wie der Live-Waechter. `browserRefusalRanges` nimmt den Pfad als Parameter, es gibt keinen Wrapper mehr, und `TestBrowserRefusalSetEqualsTheServers` ruft dieselbe Funktion mit `imagesRoutePath`. Ein kuenftiges `stripNonCode` am Aufrufer statt in der Funktion faerbt die Blindheitstabellen damit rot statt sie zu umgehen — die Umgehung, die `02-DECISION-drift-guard-lexik.md` am Entwurf von B ausdruecklich als Mangel benennt."
    - "Die Blindheitstabellen haben UMGEKEHRTE ABNAHME: sie verlangen, dass der Waechter die Phantom-Tabelle liest, und gehen rot an dem Tag, an dem die Blindheit endet. Ueber beiden Tabellen steht die Anweisung, was dann zu tun ist — die Zeile entfernen, den Eintrag aus `guardBlindSpots` streichen und beides im Ledger vermerken —, damit niemand ein Rot durch Aufweichen des Waechters beseitigt."
    - "Jede in der Liste genannte Form ist EINZELN gemessen und ihre Ausgabe zitiert. Das ist Punkt 5 der Schliessbedingung von Ledger-Eintrag 72, und die Liste nennt mindestens: blosser `/* */`-Block, eingerruecktes `{/* */}`, Template-Literal (je mit dem echten Import daneben, sonst ist die Fixture kein Schadensfall, sondern ein TypeScript-Fehler) sowie die Ableitungsformen `].filter(…)` und `…SPREAD`."
    - "Die Liste traegt einen fuenften Ausschluss, den keine Tabellenzeile messen kann und der deshalb ausdruecklich als solcher markiert ist: der Codepoint-Sweep ueberspringt U+D800..U+DFFF und behauptet die Surrogat-Menge nur an ihren beiden Endpunkten; ihr Server-Zwilling `rawBodyRefusal` wird von hier nie aufgerufen. Fuer 2046 innere Codepoints wird nichts verglichen. Das steht NEBEN der Behauptung und nicht in einem anderen Dokument."
    - "Punkt 6 der Schliessbedingung wird von dieser Runde EINMAL AUSGEFUEHRT und protokolliert: es wird ausdruecklich versucht, eine Form zu bauen, die der Waechter liest und die Liste nicht nennt. Getestet werden mindestens `String.raw`, ein Regex-Literal, eine `${…}`-Interpolation, eine Objekt-Property und ein Re-Export. Das Ergebnis steht in der SUMMARY, auch wenn es negativ ist — und Eintrag 72 bleibt offen, weil ein Eintrag, dessen Bedingung nur die schon bekannten Formen abfragt, sich selbst schliesst."
    - "`TestBrowserRefusalSetEqualsTheServers` misst weiterhin dasselbe und bleibt gruen; die Ablehnungsmenge wird auf keiner der beiden Seiten angefasst. Rumpf- wie Dateizahl bleiben 6."
    - "Die Regressionssperren der Vorrunden und die vier Zeilen aus Plan 02-29 bleiben woertlich erhalten und gruen. Die drei `wantErr`-Teilzeichenketten, die Plan 02-29 auf den Kein-Anker-Ausgang gehoben hat, ueberleben das Ersetzen des allquantifizierten Satzes — sie pinnen andere Saetze derselben Meldung."
  prohibitions:
    - statement: "Kein `stripNonCode`, kein Lexer, kein handgeschriebener Tokenizer, kein `@babel/parser`, kein Codegen, kein `go:generate`. Das sind die Varianten A, C und D aus `02-DECISION-drift-guard-lexik.md`. A ist punktgleich, aber nicht empfohlen: die pruefende Linse hat A's eigenen Lexer gebaut und mit einer Tabelle als JSX-Textknoten in einem `<pre>`-Block gruen bekommen, alle fuenf Invarianten und die Klammerbilanz passiert. C und D gehoeren in die Phase, die die Web-Naht ohnehin wieder oeffnet."
      status: unresolved
    - statement: "Die Ablehnungsmenge selbst nicht aendern — weder `REFUSED_RANGES` in `web/src/routes/images.tsx` noch `NotRepresentableReason` in `internal/imagefactory/schematicid.go`. Es geht um das kuenftige Verhalten des Waechters, nicht um seinen heutigen Befund."
      status: unresolved
    - statement: "`web/src/routes/images.tsx` wird WEDER netto NOCH voruebergehend veraendert — insbesondere werden die drei Hinweiszeilen ueber der Deklaration bei Zeile 105, die der Entwurf von Variante B vorsieht, NICHT geschrieben. Sie verschoeben alles darunter um drei Zeilen und machten rund ein Dutzend `images.tsx:NNN`-Verweise im Planungsbestand ungueltig; ihr Weglassen ist eine Umfangsentscheidung dieser Runde und gehoert als benannter Rest in Eintrag 72, nicht in eine Fussnote. `git diff --quiet HEAD -- web/` muss ueber den ganzen Plan gruen sein."
      status: unresolved
    - statement: "Keine Tabellenzeile wird entfernt oder umbenannt, um einen Fehlschlag loszuwerden. Die zehn Zeilen von `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration`, die zwei von `TestBrowserRefusalGuardRefusesAnEntryItCannotRead` und die drei von `TestBrowserRefusalGuardRefusesABoundItCannotRepresent` bleiben erhalten."
      status: unresolved
    - statement: "**Dieser Plan darf G-02-29 nicht als geschlossen melden — weder in einem Kommentar, noch in der SUMMARY, noch in einem Testnamen.** Er verengt die Behauptung; der Defekt bleibt. Ein bei der Anlage geschlossener Eintrag ueber eine allquantifizierte Eigenschaft war genau der Fehler von Ledger-Eintrag 69, und er ist mechanisch: `workflow.windows_enforce` blockiert `/gsd-ship`, solange `open_count > 0`, und ein `fixed`-Eintrag ist an dieser Grenze unsichtbar."
      status: unresolved
    - statement: "Eine rot gewordene Blindheitszeile wird NIE durch Aufweichen des Waechters gruen gemacht. Rot heisst dort: die Blindheit ist beendet. Die richtige Antwort ist, die Zeile und ihren `guardBlindSpots`-Eintrag zu entfernen und beides im Ledger zu vermerken. Diese Anweisung steht ueber beiden Tabellen im Code, weil sie sonst in einem Planungsdokument steht, das der naechste Ausfuehrende nicht liest."
      status: unresolved
    - statement: "**G-02-9 bleibt offen.** Kein Kommentar und keine SUMMARY-Zeile dieses Plans darf ihn als geschlossen melden. Die Ledger-Eintraege 56, 58, 66 und 70 bleiben unberuehrt; dieser Plan schreibt `.planning/WINDOWS.md` nicht — der Ledger gehoert Plan 02-31."
      status: unresolved
    - statement: "`requirements-completed` in der SUMMARY bleibt LEER. Die Phase traegt `gaps_found`; ein `requirements-completed`-Feld aus zwei SUMMARYs der Runde 6 hat die Buchhaltungsregression ausgeloest, die der Betreiber mit `b7a1ab9` zurueckgenommen hat."
      status: unresolved
  artifacts:
    - path: "internal/imagefactory/guard_drift_test.go"
      provides: "`guardBlindSpots` als Daten, `honestClaim` aus ihnen gerendert und in den Kein-Anker-Ausgang eingebettet, `browserRefusalRanges` mit dem Pfad als Parameter und ohne Wrapper, zwei Blindheitstabellen mit umgekehrter Abnahme ueber den Live-Lesepfad, und ein Abdeckungstest, der Liste und Zeilen aneinander bindet"
      contains: "guardBlindSpots"
  key_links:
    - from: "internal/imagefactory/guard_drift_test.go"
      to: "web/src/routes/images.tsx"
      via: "der Waechter liest die Deklaration `REFUSED_RANGES` (Zeile 105) aus der Quelle; ihre einzige lebende Referenz im ganzen Baum ist der Aufruf bei Zeile 159. Dieser Plan aendert an der Naht nichts — er schreibt auf, was ein regulaerer Ausdruck ueber sie NICHT feststellen kann, und macht jede genannte Form messbar"
      pattern: "REFUSED_RANGES"
    - from: "TestBrowserRefusalGuardBindsToAnIdentifierAndNotToCode"
      to: "browserRefusalRanges"
      via: "die Blindheitstabellen schreiben ihre Fixture in ein `t.TempDir()` und rufen dieselbe Funktion mit demselben `readSource`-Schritt auf, den `TestBrowserRefusalSetEqualsTheServers` benutzt. Der Wrapper entfaellt, damit es keinen Ort gibt, an dem eine kuenftige Vorverarbeitung sitzen koennte, den die Tabelle nicht sieht"
      pattern: "browserRefusalRanges(t, "
    - from: "guardBlindSpots"
      to: "honestClaim"
      via: "die Behauptung wird aus der Liste gerendert statt daneben geschrieben. Zwei Listen mit verschiedenen Laengen waeren selbst ein Drift-Risiko — genau die Gattung, die diese Datei bewacht"
      pattern: "honestClaim"
---

<objective>
Der Waechter sagt kuenftig, was er ist: ein regulaerer Ausdruck ueber TEXT. Nicht mehr, und alles
davon.

Purpose: Drei Verifikationsrunden haben je ein engeres Loch in derselben allquantifizierten
Eigenschaft gefunden — *"Renamed, moved or deleted is the same as never having been there"* —, und
Runde 6 hat den Grund ausgeschrieben (`02-VERIFICATION.md:70-75`): **ein regulaerer Ausdruck ueber
TypeScript-Quelltext hat einen Rest, der nicht auf null geht.** Das ist eine Aussage ueber die
Technik und nicht ueber die ausfuehrenden Plaene.

`02-DECISION-drift-guard-lexik.md` hat die Frage mit vier Entwuerfen und je vier Linsen beantwortet
und empfiehlt **Variante B: den Anspruch verengen.** Zwei unabhaengig ausgefuehrte Messungen tragen
die Empfehlung — der Lexer aus Variante A ist gebaut und mit einer Tabelle als JSX-Textknoten in
einem `<pre>`-Block gruen bekommen, alle fuenf Invarianten und die Klammerbilanz passiert; und der in
`02-REVIEW.md:164-180` entworfene `stripNonCode` wurde woertlich nachgebaut und bleibt an einer
`${…}`-Interpolation gruen. Weg (a) verschiebt den Rest und behaelt die allquantifizierte
Behauptung — genau die Kombination, die dreimal gescheitert ist.

**Die Aritaet des Restes aendert sich, und darum geht es.** Eine allquantifizierte Behauptung hat
unendlich viele Gegenbeispiele, und jedes einzelne ist eine Falsifikation. Eine geschriebene Liste
hat endlich viele Eintraege, und jede neue Form ist eine Ergaenzung.

**Vier Korrekturen, die dieser Plan gegenueber dem Entwurf von B ausdruecklich mitbringt** — alle
vier stehen in `02-DECISION-drift-guard-lexik.md` unter *"Was B in Runde 7 anders bekommen muss"*,
alle vier stammen aus den Pruefungen der Entwuerfe, und ohne sie waere die Verengung selbst wieder
eine Behauptung mit Ueberhang:

1. **Die Blindheit wird nach MECHANISMUS skopiert, nicht als `non-code`.** Testname, Ausschlussliste
   und Ledger-Eintrag sagen: *der Anker bindet an ein Literal, nicht an einen Wert, und an einen
   Bezeichner, nicht an Code.* Sonst laesst sich der Eintrag buchstabengetreu schliessen, waehrend
   der Ableitungspfad offen und ungeledgert bleibt — und der ist der gefaehrlichste bekannte Rest,
   weil er auf lebendem, kompilierendem, bei `images.tsx:159` referenziertem Code gruen ist.
2. **Die Blindheitstabellen laufen ueber den Live-Pfad**, nicht ueber die reine Funktion allein.
   Sonst ist ihr Anti-Rot-Versprechen mit einer Vorverarbeitung am Aufrufer zu umgehen, und der
   unbedingte Satz *"geht rot an dem Tag, an dem die Blindheit endet"* waere selbst allquantifiziert
   und verletzbar.
3. **Die Liste bekommt einen fuenften Ausschluss:** der Sweep ueberspringt U+D800..U+DFFF und prueft
   die Surrogat-Menge nur an ihren beiden Endpunkten; ihr Server-Zwilling `rawBodyRefusal` wird von
   hier nie aufgerufen. Fuer 2046 Codepoints wird nichts verglichen. Das gehoert **neben** die
   Behauptung.
4. **`overrides:` wird nicht benutzt.** Beide Verifikationen tragen `overrides_applied: 0`, und das
   Ledger-Verb fuer bewusste Annahme heisst `waive` (`waived_count: 0`). Die Ausnahme wird als
   offener Ledger-Eintrag gefuehrt, nicht abgewaehlt. Das ist Plan 02-31.

Beim Planen selbst gemessen, gegen die woertlich kopierten Regexe dieser Datei: **alle sieben Formen,
die dieser Plan als Zeile festnagelt, liefern heute `err=nil`** und der Waechter meldet Zustimmung
ueber eine Tabelle, die das Formular nicht benutzt. Die scharfsten beiden sind nicht die
Kommentarformen: eine Deklaration, deren Literal vor der Bindung gefiltert wird
(`].filter((r) => r.class !== 'byte order mark')`), liest sechs Bereiche mit `err=nil`, waehrend das
Formular U+FEFF wieder annimmt und der Server es weiter ablehnt — **gruen auf echter Drift**. Und
eine eingerueckte Rest-Deklaration neben dem echten Import liest zwei Bereiche mit `err=nil`, ohne
einen einzigen Schraegstrich, Backtick oder Anfuehrungsstrich.

Output: eine Behauptung mit null Ueberhang, als Daten statt als Prosa; zwei Tabellen mit umgekehrter
Abnahme, die jede genannte Form einzeln messen und ueber denselben Pfad laufen wie der Live-Waechter;
ein Abdeckungstest, der Liste und Zeilen aneinander bindet; und ein einmal ausgefuehrter,
protokollierter Versuch, eine Form zu finden, die die Liste nicht nennt.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-VERIFICATION.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-REVIEW.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-29-PLAN.md
</context>

## Recorded decisions

**Die Richtung dieser Runde ist VORGELEGT, NICHT RATIFIZIERT — und dieser Plan ist der einzige der
drei, den das betrifft.** Siehe `## Flagged Assumptions`.

**Alles hier Behauptete wurde beim Planen gemessen, nicht uebernommen.** Die drei Regexe und der
Kontrollfluss von `parseBrowserRefusalRanges` wurden woertlich aus
`internal/imagefactory/guard_drift_test.go` in ein eigenstaendiges Go-Programm kopiert und gegen die
echte `web/src/routes/images.tsx` sowie gegen vierzehn synthetische Quellen gefahren. Die sieben
Formen, die dieser Plan als Zeile festnagelt, und ihre gemessenen Ausgaben:

| # | Form | gemessen |
|---|---|---|
| 1 | Deklaration lebt nur in einem `/* … */`-Block, echter Import daneben | `ranges=6 whole=6 err=nil` |
| 2 | Deklaration lebt nur in einem eingerueckten `{/* … */}`-JSX-Kommentar | `ranges=6 whole=6 err=nil` |
| 3 | Deklaration lebt nur in einem Template-Literal | `ranges=6 whole=6 err=nil` |
| 4 | Deklaration lebt nur als JSX-Textknoten in einem `<pre>`-Block | `ranges=6 whole=6 err=nil` |
| 5 | `].filter((r) => r.class !== 'byte order mark')` hinter dem Literal | `ranges=6 whole=6 err=nil` |
| 6 | `[...REFUSED_RANGES, ...PLATFORM_RANGES]` ist, was das Formular benutzt | `ranges=6 whole=6 err=nil` |
| 7 | eingerueckte Rest-Deklaration, echte Tabelle importiert, kein Kommentar im Spiel | `ranges=2 whole=2 err=nil` |

Ebenso gemessen, und der Grund fuer die Form des Abdeckungstests: `String.raw`, ein Regex-Literal und
eine `${…}`-Interpolation liefern **ebenfalls** je `ranges=6 err=nil`. Sie sind Auspraegungen
desselben Mechanismus wie 1 bis 4 — der Anker sieht Text — und **deshalb** wird die Liste nach
Mechanismus und nicht nach Form gefuehrt. Eine Liste von Formen waere nach drei Runden wieder
unvollstaendig; eine Liste von Mechanismen deckt die Auspraegungen mit ab, und der Abdeckungstest
verlangt fuer jeden Mechanismus mindestens eine gemessene Auspraegung.

Ebenfalls gemessen: eine Objekt-Property (`cfg = { REFUSED_RANGES: [ … ] }`) und ein Re-Export
(`export { REFUSED_RANGES } from …`) erfuellen den Anker **nicht** und liefern korrekt einen Fehler.
Die Liste behauptet sie also nicht.

**Die `must_haves.prohibitions` sind descriptor-los** (kein `check_kind`, `check_target`,
`check_rule`, `check_violation_fixture`, `check_clean_fixture`) und disponieren damit als
**flagged-unverified**, nicht als auto-dismissed. Kein Kandidat wurde automatisch verworfen. Zwei
Kandidaten der Recall-Stufe sind als Kanon-Verweis fallengelassen und hier als Brotkrume vermerkt
statt gemuenzt: Schreiben in ein Temp-Verzeichnis (`t.TempDir()` raeumt selbst auf und liegt
ausserhalb des Baums) und ReDoS im Anker (das Muster ist linear und laeuft ueber Quellen fester
Groesse im Test).

## API coverage

`.planning/phases/02-transport-seam-talossim-image-factory/COVERAGE.md` traegt die Matrix dieser
Phase und ist von diesem Plan unberuehrt. Dieser Plan aendert eine Testdatei; er spricht mit keinem
externen Dienst, fuegt keinen Aufruf hinzu und entfernt keinen.

<assumption_delta_decision>
- **Primaeres Substantiv:** unveraendert `REFUSED_RANGES` als die eine Deklaration, aus der die
  Ablehnungsmenge des Browsers gelesen wird. Dieser Plan schafft kein zweites Vorkommen; Plan 02-29
  hat die Existenz genau einer erzwingbar gemacht.
- **Entscheidung:** `no-change`. Was sich pluralisiert, ist nicht das Substantiv, sondern die
  **Aufzaehlung der Dinge, die der Waechter nicht sieht** — und die wird von `guardBlindSpots` von
  Anfang an als Menge gefuehrt, nicht als Sonderfall neben einer Regel.
- **Begleit-Invariantentest:** angenommen und ausgefuehrt. `TestGuardBlindSpotsAreEachMeasured`
  kodiert die verallgemeinerte Absicht — jeder gelistete Mechanismus hat eine gemessene Auspraegung,
  jede Auspraegung einen gelisteten Mechanismus — und geht rot, sobald ein kuenftiger Plan die eine
  Seite ohne die andere anfasst.
</assumption_delta_decision>

## Flagged Assumptions

### 1. Die Richtung dieser Runde ist nicht ratifiziert — und dieser Plan ist der, der davon abhaengt

`02-DECISION-drift-guard-lexik.md` traegt im Statusblock `offen — vorgelegt, nicht ratifiziert`. Sie
wurde von einem autonomen Lauf am 2026-09-05 erarbeitet und empfiehlt Variante B; **Variante A liegt
punktgleich daneben, 7,0 gegen 7,0.** A gewinnt die Robustheit um einen Punkt, B die Passung um
einen. Der Verifier hat die Frage ausdruecklich dem Betreiber zugewiesen (`02-VERIFICATION.md`
Human-Verification-Punkt 1: *"Welcher Weg genommen wird, gehoert dem Betreiber"*). Der Code-Review
ordnet ausserdem **anders und begruendet** — er nennt das Entfernen von Kommentaren und Zeichenketten
vor dem Anker zuerst und die Verengung erst danach, ausdruecklich als *"Falls das als zu teuer
gilt"*. Diese Runde verlangt vom Betreiber, die Ordnung des Reviews zu ueberstimmen.

Diese Phase hat den Praezedenzfall in beide Richtungen: `02-DECISION-probe-budget.md` wurde in
Sitzung ratifiziert und Runde 4 war bis dahin blockiert; `02-DECISION-schematic-identity.md` wurde
vom Ausfuehrenden selbst aufgeloest und traegt das im Text.

**Was passiert, wenn der Betreiber Variante A waehlt:**

- **Plan 02-29 bleibt unveraendert gueltig.** Er enthaelt ausschliesslich das, was das
  Entscheidungsdokument unter *"Was in jedem Fall gilt"* fuehrt. Nichts davon ist zu revidieren.
- **Dieser Plan (02-30) wird verworfen und neu geplant.** Er ist noch nicht ausgefuehrt, solange die
  Welle 2 nicht laeuft; die Kosten sind bis dahin bezahlt, wie das Entscheidungsdokument selbst
  festhaelt: *"der Wechsel auf Variante A kostet den Betreiber einen Satz."*
- **Plan 02-31 verliert die Abschnitte von Eintrag 72, die B betreffen**, und behaelt seine beiden
  anderen Eintraege sowie die Requirement-Korrektur.

Dass die Runde in drei Plaene und drei Wellen geschnitten ist, statt in einen grossen, **ist** diese
Absicherung. Sie ist der Grund, warum kein blockierender Checkpoint noetig ist: der Umschaltpunkt
liegt an einer Wellengrenze und nicht mitten in einer Datei.

### 2. Der spezifikationslose Probe-Fallback lief und hat nichts klassifiziert

Diese Phase hat keine `SPEC.md`, also `EDGE_ABSENT=1` und `PROHIB_ABSENT=1`. Die deterministische
Edge-Probe lief ueber alle vierzehn Requirement-Ids und lieferte
`{"applicable":14,"resolved":0,"unresolved":14,"byVerification":{"explicit":0,"backstop":0}}` — jede
Zeile `unresolved`, dreizehn davon `category: unclassified` mit `probe: "unclassified — review
manually"`. **Es wurde nichts klassifiziert und nichts aufgeloest.** 14 == 0 uebernommene + 14 hier
sichtbar gemachte. **Keine stille Auslassung, keine automatische Verwerfung.**

| requirement | category | status | Disposition in diesem Plan |
|---|---|---|---|
| FACT-01 | unclassified | unresolved | **live** — der Waechter bewacht die Naht zwischen der Eingabepruefung des Browsers und der Ablehnungsmenge des Servers. Dieser Plan verengt seine Behauptung; er loest die Requirement-Zeile nicht auf |
| FACT-06 | unclassified | unresolved | **live** — die vorberechnete Id ruht auf `NotRepresentableReason`. Dieser Plan aendert sie nicht; er loest die Requirement-Zeile nicht auf |
| FOUND-12 | unclassified | unresolved | von 02-07 abgedeckt; von dieser Runde unberuehrt |
| TRANS-01 | concurrency | unresolved | von 02-01 abgedeckt; von dieser Runde unberuehrt. **Nicht** auf `backstop` promoviert — dieser Plan fasst die Transport-Naht nicht an |
| TRANS-02 | unclassified | unresolved | von 02-01 abgedeckt; von dieser Runde unberuehrt |
| TRANS-03 | unclassified | unresolved | von 02-05 abgedeckt; von dieser Runde unberuehrt |
| TRANS-04 | unclassified | unresolved | von 02-05 abgedeckt; von dieser Runde unberuehrt |
| TRANS-05 | unclassified | unresolved | Transport-Haelfte belegt; UI-Haelfte nach Phase 3 verschoben |
| TRANS-06 | unclassified | unresolved | von 02-01/02-03/02-08 abgedeckt. Seine Statuszeile im Register ist falsch — siehe Plan 02-31, G-02-30 |
| TRANS-07 | unclassified | unresolved | von 02-03 abgedeckt; von dieser Runde unberuehrt |
| FACT-02 | unclassified | unresolved | von 02-02/02-22/02-25 abgedeckt; von dieser Runde unberuehrt |
| FACT-03 | unclassified | unresolved | von 02-04/02-23 abgedeckt; von dieser Runde unberuehrt |
| FACT-04 | unclassified | unresolved | von 02-06 abgedeckt; von dieser Runde unberuehrt |
| FACT-05 | unclassified | unresolved | von 02-04 abgedeckt; von dieser Runde unberuehrt |

### 3. Was diese Verengung ausdruecklich NICHT leistet

Damit es nicht in einer SUMMARY untergeht: **der Defekt bleibt.** Die Leiche bleibt lesbar, der
Waechter bleibt gruen darueber, und der Zeitpunkt, an dem das schadet, ist unveraendert. Wer haelt,
dass ein Waechter zu bewachen und nicht sich zu beschreiben hat, liest diesen Plan als die Runde, die
sich aus einer Reparatur herausredet — und dieser Einwand steht im Entscheidungsdokument als
Gegenrede Nummer 2. Die Antwort darauf ist nicht, dass er falsch ist, sondern dass Weg (a) den Rest
nachweislich auch nicht auf null bringt und dabei die allquantifizierte Behauptung behaelt.

Ein zweiter Einwand, ebenfalls aus dem Dokument: **die naechsten Funde wandern in die Prosa.** Runde 8
findet unter B Saetze statt Muster, und Prosa hat keinen Compiler. Der Mechanismus dagegen ist
`TestGuardBlindSpotsAreEachMeasured` — er bindet die Prosa an gemessene Zeilen —, und er pinnt
Mechanismen statt Formen, was mehr ist als der Entwurf vorsah. Ganz weg ist der Einwand damit nicht.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: Variante B oder Variante A — die Richtung dieser Runde ist nicht ratifiziert</name>
  <decision>Welchen der beiden punktgleichen Wege der Browser-Ablehnungs-Waechter nimmt: Variante B (den Anspruch verengen) oder Variante A (einen Lexer vor den Anker setzen).</decision>
  <context>
**Warum dieses Tor hier steht und nicht an der Wellengrenze.** Der Planer hatte urspruenglich
argumentiert, ein Checkpoint sei entbehrlich, weil der Umschaltpunkt zwischen Plan 02-29
(richtungsunabhaengig) und diesem Plan an einer Wellengrenze liegt und der Wechsel bis dahin nichts
kostet. Das trug nicht: `/gsd-execute-phase` faehrt Welle 1 -> 2 -> 3 ohne Unterbrechung durch. An
einer Wellengrenze steht niemand, also waere die Billigkeit des Wechsels nie realisiert worden — eine
Absicherung, die zu schuetzen behauptet, ohne die Eigenschaft herzustellen. Das ist woertlich die
Gattung Defekt, an der diese Phase seit Runde 3 arbeitet, und deshalb ist die Absicherung jetzt ein
Mechanismus statt eines Satzes.

**Der Stand der Entscheidung.** `02-DECISION-drift-guard-lexik.md` wurde von einem autonomen Lauf am
2026-09-05 erarbeitet — vier Entwuerfe, je vier Linsen, jede zitierte Messung von der pruefenden
Linse an einem eigenen Harness nachgefahren. Ihr Statusblock traegt `offen — vorgelegt, nicht
ratifiziert`. **Kein Betreiber hat sie ratifiziert.** Der Verifier hat die Frage ausdruecklich dem
Betreiber zugewiesen (`02-VERIFICATION.md` Human-Verification-Punkt 1: *"Welcher Weg genommen wird,
gehoert dem Betreiber"*). Diese Phase hat den Praezedenzfall: `02-DECISION-probe-budget.md` wurde in
Sitzung ratifiziert, und Runde 4 war bis dahin blockiert.

**Das Panel-Ergebnis, so knapp wie es ist.** A und B stehen exakt punktgleich bei **7,0 gegen 7,0**.
A gewinnt die Robustheit um einen Punkt, B die Passung um einen. Wer Kosten und Robustheit hoeher
haengt als Passung, kommt bei A heraus, und das ist keine Fehlbewertung, sondern eine andere
Gewichtung derselben Zahlen. Alle vier Entwuerfe bekamen Wahrhaftigkeit 8 — die Wahl faellt nicht
zwischen ehrlich und unehrlich.

**Der Code-Review ordnet anders, und er ordnet begruendet.** `02-REVIEW.md:160-162` schreibt:
*"Kommentare und Zeichenketten vor dem Anker aus der Quelle entfernen, statt den Anker weiter zu
verfeinern — der Anker kann diese Unterscheidung strukturell nicht treffen."* Die Verengung erscheint
dort erst bei `:182-186` und ausdruecklich als *"Falls das als zu teuer gilt"*. **Nur der Verifier hat
Weg (b) zur Gleichrangigkeit erhoben.** Diese Empfehlung verlangt vom Betreiber, die Ordnung des
Reviews zu ueberstimmen — und dieser Satz gehoert vor die Entscheidung und nicht in eine Fussnote
danach.

**Was bereits steht und von der Wahl unberuehrt bleibt.** Plan 02-29 (Welle 1) ist ausgefuehrt und
richtungsunabhaengig: der Spiegel-Defekt ist ueber die Rumpfform beseitigt, Ueberlauf und Inversion
sind zwei Ursachen, die drei trivial erfuellten `wantErr`-Zeilen pinnen unterscheidende
Teilzeichenketten, der Waechter verlangt genau eine Deklaration, und der Doc-Kommentar steht unter
seinem Symbol. Nichts davon ist zu revidieren, egal wie diese Entscheidung faellt.
  </context>
  <options>
    <option id="variante-b">
      <name>Variante B — den Anspruch verengen (die Empfehlung des Entscheidungsdokuments)</name>
      <pros>
Der Anker bleibt, die Behauptung schrumpft auf das Gemessene. Die allquantifizierte Aussage
verschwindet aus Doc-Kommentar und Fehlertext; an ihre Stelle tritt eine nach MECHANISMUS skopierte
Ausschlussliste, die als Daten gefuehrt und von einem Abdeckungstest an sieben einzeln gemessene
Tabellenzeilen gebunden wird. **Die Aritaet des Restes aendert sich:** eine allquantifizierte
Behauptung hat unendlich viele Gegenbeispiele und jedes ist eine Falsifikation; eine geschriebene
Liste hat endlich viele Eintraege und jede neue Form ist eine Ergaenzung. Keine Abhaengigkeit, kein
neuer Import, kein Subprozess, kein Codegen — `GOPROXY=off go test ./internal/imagefactory/` bleibt
offline gruen. Rund 150 Zeilen Prosa und Test. Und: zwei unabhaengig ausgefuehrte Messungen sagen,
dass Weg (a) das Muster NICHT beendet — die pruefende Linse hat A's eigenen Lexer gebaut und mit einer
Tabelle als JSX-Textknoten in einem vorformatierten `pre`-Block GRUEN bekommen, alle fuenf Invarianten und die
Klammerbilanz passiert; und der in `02-REVIEW.md:164-180` entworfene `stripNonCode` bleibt woertlich
nachgebaut an einer `${…}`-Interpolation gruen.
      </pros>
      <cons>
**Es entfernt die Behauptung, nicht den Defekt.** Die Leiche bleibt lesbar, der Waechter bleibt gruen
darueber, und der Zeitpunkt, an dem das schadet, ist unveraendert. Wer haelt, dass ein Waechter zu
bewachen und nicht sich zu beschreiben hat, liest B als die Runde, die sich aus einer Reparatur
herausredet. Unter A waeren zwei der drei bekannten Kommentarformen nicht mehr erreichbar. Ausserdem:
**die naechsten Funde wandern in die Prosa** — Runde 8 findet unter B Saetze statt Muster ("die
Ausschlussliste nennt `${…}` nicht"), und Prosa hat keinen Compiler. Der einzige Mechanismus dagegen
ist der Abdeckungstest, und der pinnt Mechanismen, nicht die Klasse.
      </cons>
    </option>
    <option id="variante-a">
      <name>Variante A — der Lexer vor dem Anker (punktgleich, vom Code-Review zuerst genannt)</name>
      <pros>
Der Anker bleibt Byte fuer Byte; was ihm gereicht wird, aendert sich. Ein handgeschriebener,
byteweiser TSX-Lexer leert jeden Kommentar, jede Zeichenkette und jedes Template-Literal laengentreu
zu Leerzeichen, gehalten an fuenf Invarianten plus einer Klammerbilanz ueber alle 49 `.ts`/`.tsx`-
Dateien unter `web/src`. **Er schliesst die gesamte lexikalische Familie auf einen Schlag**, nicht
Mitglied fuer Mitglied: alle drei Runde-6-Formen gehen nachgefahren von GRUEN auf einen Fehlerausgang.
Null Abhaengigkeiten. Der Code-Review nennt diesen Weg zuerst und begruendet ihn strukturell. Die
Robustheits-Linse ist die, die in dieser Phase dreimal recht behalten hat, und sie gibt A einen Punkt
mehr.
      </pros>
      <cons>
**Die allquantifizierte Behauptung wird substituiert, nicht eliminiert:** aus "der Anker sieht nur
Code" wird "`stripNonCode` erkennt Code in allem TSX". Gemessen falsch an vier Eingaben, und die
gefaehrliche Richtung wurde ausgefuehrt: eine Tabelle als reiner JSX-Textknoten in einem
vorformatierten `pre`-Block, neben dem echten Import, ohne Schraegstrich, Backtick oder Anfuehrungspaar, ergibt
GUARD GREEN und passiert alle fuenf Invarianten sowie die Klammerbilanz. Runde 8 haette bei A ein
fertiges Ziel. Kosten: ein zweites `guard_drift_test.go` an Volumen (realistisch 550-750 Zeilen in
der Kommentarquote dieses Pakets), und eine **dauerhafte Kopplung** der Go-Suite an das gesamte
Frontend — der Bilanz-Sweep macht jede `.ts`-Datei unter `web/src` zur moeglichen Ursache eines roten
Go-Waechters. Zwei bestehende Testzeilen kippen ihr Verhalten, und
`TestBrowserRefusalGuardRefusesAnEntryItCannotRead` bricht, bis die Meldungen auf die Quellsicht
umgestellt sind. Unberuehrt bleiben in beiden Faellen: die Ableitung vom Literal zum Wert
(`].filter(…)`, gemessen **gruen auf echter Drift**) und die eine tragende Verbindung bei
`images.tsx:159`.
      </cons>
    </option>
  </options>
  <resume-signal>
Antworte mit **`B`**, um diesen Plan wie geschrieben auszufuehren — dann traegt Plan 02-31 die
Ratifikation in den Statusblock von `02-DECISION-drift-guard-lexik.md` UND in den Text von
Ledger-Eintrag 72, und der angehaengte Nachtrag haelt fest, dass sie hier erfolgt ist.

Antworte mit **`A`**, um stattdessen den Lexer zu bauen — dann wird **dieser Plan verworfen und neu
geplant**; Plan 02-29 bleibt unveraendert gueltig, und Plan 02-31 verliert die B-spezifischen
Abschnitte von Eintrag 72 und behaelt seine beiden anderen Eintraege sowie die Requirement-Korrektur.
Kein Commit dieses Plans ist dann zurueckzunehmen: das Tor steht vor der ersten Handlung.

Antwortest du mit **`weder noch`** oder nennst einen dritten Weg (Varianten C und D des Dokuments —
generierter Spiegel, Parser-Bindung), wird die Runde angehalten und neu geplant. Beide sind im
Entscheidungsdokument in voller Behandlung mitgeschrieben und dort ausdruecklich der Phase zugewiesen,
die die Web-Naht ohnehin wieder oeffnet.
  </resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: Ein Mechanismus, einmal ganz durch — die Blindheit als Daten, als Behauptung, als gemessene Zeile ueber den Live-Pfad und als Abdeckungstest</name>
  <reversibility rating="costly">Die Aenderung liegt vollstaendig in einer Testdatei und erzeugt keinen Zustand, aber sie ist die erste ausgefuehrte Handlung auf einer NICHT RATIFIZIERTEN Richtungsentscheidung. Sie zurueckzunehmen ist eine Umkehrung des Diffs — technisch billig, aber sie kostet den Betreiber, der Variante A vorzieht, eine Rueckabwicklung statt eines Satzes. Deshalb liegt sie in Welle 2 und nicht in Welle 1: bis dahin ist der Wechsel kostenlos. Kein `one-way`: es entsteht keine Datei ausserhalb dieses Diffs, keine Migration und kein Ledger-Eintrag — der kommt erst in Plan 02-31.</reversibility>
  <read_first>
    - `internal/imagefactory/guard_drift_test.go` in dem Zustand, den Plan 02-29 hinterlassen hat —
      **vollstaendig**, weil dieser Task an vier verschiedenen Stellen ansetzt.
    **Alle Fundstellen unten sind ueber SYMBOLNAMEN benannt und nicht ueber Zeilennummern.** Plan
    02-29 hat die Datei in drei Tasks verschoben, also waere jede Zeilenzahl aus dem HEAD-Stand vor
    dieser Runde hier bereits falsch — dieselbe Gattung wie 02-REVIEW IN-05, den diese Runde behebt.
    Finde jede Stelle mit einer Suche nach dem genannten Bezeichner.

    - `browserRefusalRanges` mit seinem Doc-Kommentar, der die Aufteilung in Leser und reine Funktion
      begruendet ("Only the file read and the fatal stay on this side of the line"). **Die Signatur
      ohne Pfadparameter und der hartkodierte `imagesRoutePath` sind die Stelle, die dieser Task
      ersetzt.** Es gibt genau EINEN Aufrufer — belege das mit einer Suche nach dem Funktionsnamen,
      bevor du die Signatur aenderst, statt es diesem Plan zu glauben.
    - Der Kopf von `TestBrowserRefusalSetEqualsTheServers` bis einschliesslich seines Aufrufs von
      `browserRefusalRanges`. Das ist der eine Aufrufer, und er wird mitgezogen.
    - Die Sonderbehandlung des Surrogat-Bereichs in `TestBrowserRefusalSetEqualsTheServers`: der
      Kommentar, der erklaert, warum Go keinen unpaarigen Surrogat halten kann und dass der
      Server-Zwilling `rawBodyRefusal` heisst, die Behauptung an den beiden Endpunkten ueber
      `refusedByBrowser`, und das `continue` im Sweep, das den Bereich ueberspringt. **Das ist der
      fuenfte Ausschluss, und sein Text ist die Quelle fuer den entsprechenden
      `guardBlindSpots`-Eintrag.** Zum Auffinden: such nach `surrogateLow` in dieser Datei.
    - `parseBrowserRefusalRanges` mit seinem Doc-Kommentar und dem Fehlertext des
      Deklarations-Schnitts. **Der Satz `It reads the declaration and not the file. Renamed, moved or
      deleted is the same as never having been there` im Doc-Kommentar und seine woertliche
      Entsprechung im Fehlertext sind die beiden Stellen, die dieser Task ersetzt** — such nach
      `Renamed, moved or deleted`, das findet beide. Die beiden Teilzeichenketten, die Plan 02-29 in
      den `wantErr`-Slices der drei Deklarations-Zeilen gepinnt hat, stehen in DERSELBEN Meldung und
      muessen woertlich erhalten bleiben; welche das sind, steht in
      `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration` und nicht in diesem Plan.
    - `TestBrowserRefusalGuardRefusesAnEntryItCannotRead` als Formvorlage: `wantErr []string`, ein
      Doc-Kommentar, der begruendet, warum die Tabelle getrennt bleibt, und eine Auswertung, die ueber
      Teilzeichenketten schleift. **Die neue Tabelle hat die umgekehrte Abnahme und braucht deshalb
      eine andere Auswertung, aber dieselbe Trennungs-Begruendung.**
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md`,
      Abschnitte "Variante B", "Was B in Runde 7 anders bekommen muss, als der Entwurf es schreibt"
      und "Was keine der vier Varianten schliesst" — vollstaendig. Die vier Korrekturen und der
      benannte Rest sind der Inhalt dieses Tasks.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-REVIEW.md:111-187` — WR-01
      vollstaendig, mit den vom Reviewer gemessenen Ausgaben.
  </read_first>
  <behavior>
    - `browserRefusalRanges` nimmt einen Pfad. `TestBrowserRefusalSetEqualsTheServers` uebergibt
      `imagesRoutePath` und verhaelt sich unveraendert. Es gibt keine zweite Funktion mehr, die den
      Pfad kennt, und keinen Wrapper, in dem eine kuenftige Vorverarbeitung sitzen koennte, ohne von
      den Blindheitstabellen gesehen zu werden.
    - `honestClaim` rendert die Behauptung des Waechters plus je eine Zeile pro Eintrag in
      `guardBlindSpots`. Die Zeichenkette taucht im Fehlertext des Kein-Anker-Ausgangs auf.
    - Der allquantifizierte Satz steht weder im Doc-Kommentar noch in irgendeinem Fehlertext dieser
      Datei.
    - Eine Quelle, in der die Deklaration nur noch in einem `/* … */`-Block lebt und die echte
      Tabelle danebenimportiert wird, wird vom Waechter GELESEN — sechs Bereiche, kein Fehler. Die
      Tabellenzeile verlangt genau das und geht rot, sobald das nicht mehr gilt.
    - `TestGuardBlindSpotsAreEachMeasured` ist gruen, solange jeder nicht-rowless Eintrag von
      mindestens einer Zeile gemessen wird und jede Zeile einen gelisteten Eintrag nennt.
    - Die drei Deklarations-Zeilen aus Plan 02-29 bleiben gruen; ihre Teilzeichenketten stehen
      weiterhin in der Meldung.
  </behavior>
  <files>internal/imagefactory/guard_drift_test.go</files>
  <action>
**Erstens der Lesepfad, weil alles andere darauf steht.** `browserRefusalRanges` bekommt den Pfad als
zweiten Parameter und liest ihn statt `imagesRoutePath`; der einzige heutige Aufrufer in
`TestBrowserRefusalSetEqualsTheServers` uebergibt `imagesRoutePath`. Es entsteht **kein** Wrapper und
keine zweite Funktion — das ist der Punkt der Aenderung, nicht ihr Nebeneffekt, und er gehoert als
Satz in den Doc-Kommentar: die Blindheitstabellen fahren genau diese Funktion, damit eine
Vorverarbeitung, die jemand kuenftig zwischen Datei und Ausdruck schiebt, in ihnen sichtbar wird und
nicht an ihnen vorbei. Der bestehende Kommentar, der die Aufteilung in Leser und reine Funktion
begruendet, bleibt und bekommt diesen Satz dazu.

**Zweitens die Liste, als Daten.** Ein Typ mit vier Feldern: eine stabile Id, die eine Tabellenzeile
nennt; eine Zeile Prosa, die den **Mechanismus** benennt; die gemessene Ausgabe, die den Eintrag
belegt; und ein Kennzeichen fuer den einen Eintrag, den keine Tabellenzeile messen kann. Dazu eine
Paketvariable `guardBlindSpots` mit **zwei** Eintraegen in diesem Task:

- Der Mechanismus, der Text mit Programm verwechselt: der Anker trifft den Bezeichner ueberall dort,
  wo er im Quelltext steht; ob der Browser diesen Text jemals ausfuehrt, kann ein regulaerer Ausdruck
  nicht feststellen. Gemessene Belege: die vier in `## Recorded decisions` tabellierten Formen und
  zusaetzlich `String.raw`, Regex-Literal und `${…}`-Interpolation, die dieselben Werte liefern.
- Der Ausschluss, den keine Zeile messen kann und der deshalb markiert ist: der Codepoint-Sweep
  ueberspringt U+D800..U+DFFF, behauptet die Surrogat-Menge nur an ihren beiden Endpunkten, und ihr
  Server-Zwilling `rawBodyRefusal` wird von hier nie aufgerufen — fuer 2046 innere Codepoints wird
  nichts verglichen. Sein Kennzeichen-Feld traegt die Begruendung, warum er keine Zeile hat: er ist
  eine Eigenschaft des Sweeps und nicht des Lesers, und die Blindheitstabellen fahren den Leser.

Der Doc-Kommentar von `guardBlindSpots` sagt in einem Satz, warum die Liste nach **Mechanismus** und
nicht nach Form gefuehrt wird: eine Liste von Formen ist nach drei Runden wieder unvollstaendig, eine
Liste von Mechanismen deckt ihre Auspraegungen mit ab, und der Abdeckungstest verlangt fuer jeden
Mechanismus mindestens eine gemessene Auspraegung.

**Drittens die Behauptung.** Eine Funktion `honestClaim`, die aus `guardBlindSpots` einen Text
rendert: zuerst der Satz, was der Waechter ist — er liest Text und kein Programm, sein Anker bindet an
ein **Literal** und nicht an einen **Wert**, an einen **Bezeichner** und nicht an **Code**, den der
Browser ausfuehrt —, danach je eine Zeile pro Eintrag mit Mechanismus und gemessener Ausgabe. Diesen
Text an den Fehlertext des Kein-Anker-Ausgangs anhaengen. Nur an diesen einen: er ist der Ausgang, an
dem der Waechter sagt, dass er nichts gefunden hat, und damit der Ort, an dem ein Leser wissen muss,
was Nichtfinden hier bedeutet und was nicht.

**Viertens den allquantifizierten Satz entfernen — an beiden Stellen.** Im Doc-Kommentar von
`parseBrowserRefusalRanges` und im Fehlertext des Deklarations-Schnitts. Der Doc-Kommentar sagt
stattdessen, was gilt: die Funktion liest die Deklaration und nicht die Datei, und was sie ueber den
Rest der Datei **nicht** feststellt, steht in `guardBlindSpots`. Der Fehlertext behaelt seine beiden
von Plan 02-29 gepinnten Teilzeichenketten woertlich und traegt statt des allquantifizierten Satzes
das Ergebnis von `honestClaim`.

**Fuenftens die erste Blindheitstabelle.** Ein Zeilentyp mit Namen, Blindspot-Id, Quelle und
erwarteter Bereichszahl; eine Paketvariable mit den Zeilen dieses Mechanismus, in diesem Task mit
**einer** Zeile: `a declaration surviving only in a block comment`. Ihre Quelle traegt den echten
Import aus einem anderen Modul plus eine Verwendung — **ohne die ist die Fixture kein Schadensfall,
sondern ein TypeScript-Fehler** — und darunter die vollstaendige, sechszeilige Tabelle in einem
`/* … */`-Block. Erwartete Bereichszahl 6.

Der Test `TestBrowserRefusalGuardBindsToAnIdentifierAndNotToCode` schreibt die Quelle jeder Zeile in
eine Datei unter `t.TempDir()` und ruft `browserRefusalRanges` mit diesem Pfad. **Umgekehrte
Abnahme:** er verlangt, dass die Bereiche gelesen werden und die Zahl stimmt. Scheitert der Leser,
faellt die Zeile ueber das `t.Fatalf` in `browserRefusalRanges` — und genau das ist gewollt.

Ueber dem Test steht die Anweisung, ohne die diese Tabelle gefaehrlich waere: **ein Rot hier heisst,
dass die Blindheit geendet hat.** Die richtige Antwort ist dann, die Zeile und ihren
`guardBlindSpots`-Eintrag zu entfernen und beides im Ledger zu vermerken — niemals, den Waechter
aufzuweichen, bis die Zeile wieder gruen ist. Dazu die Begruendung, die die drei Nachbartabellen
bereits fuehren: eine eigene Tabelle, weil die Abnahme jeder Tabelle die Zahl ihrer Zeilen pinnt und
diese Zahl nur haelt, solange die Tabellen getrennt bleiben.

**Sechstens der Abdeckungstest.** `TestGuardBlindSpotsAreEachMeasured` bildet die Menge der von
Zeilen genannten Ids und die Menge der nicht markierten Ids aus `guardBlindSpots` und verlangt
Gleichheit in **beide** Richtungen. Ein gelisteter Mechanismus ohne messende Zeile ist eine
Behauptung ohne Beleg; eine Zeile ohne gelisteten Mechanismus ist ein Beleg, den die Behauptung nicht
nennt. Zusaetzlich verlangt er, dass es genau **einen** markierten Eintrag gibt und dass dessen Id
die des Surrogat-Ausschlusses ist — sonst waere das Kennzeichen ein Schlupfloch, durch das ein
kuenftiger Eintrag der Messung entkaeme.

**Zuletzt beide neuen Tests fahren und die vier bestehenden Tabellen unveraendert gruen sehen.** Die
Rumpf- und Dateizahlen gegen die echte Datei bleiben 6 und 6.
  </action>
  <acceptance_criteria>
    - `internal/imagefactory/guard_drift_test.go` enthaelt `guardBlindSpots` und `honestClaim` als Bezeichner
    - Die Datei enthaelt keine Signatur `func browserRefusalRanges(t *testing.T)` mehr; die Funktion nimmt einen Pfadparameter, und `TestBrowserRefusalSetEqualsTheServers` ruft sie mit `imagesRoutePath` auf
    - Es gibt genau eine Funktion in dieser Datei, die `readSource` fuer die Route aufruft und danach `parseBrowserRefusalRanges` — kein Wrapper, der einen Pfad hartkodiert
    - Der allquantifizierte Satz ueber Umbenennen, Verschieben und Loeschen steht weder in einem Doc-Kommentar noch in einem Fehlertext dieser Datei
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalGuardBindsToAnIdentifierAndNotToCode$' -count=1 -v` gibt eine Zeile `--- PASS: …/a_declaration_surviving_only_in_a_block_comment` aus
    - `go test ./internal/imagefactory -run '^TestGuardBlindSpotsAreEachMeasured$' -count=1 -v` gibt `--- PASS: TestGuardBlindSpotsAreEachMeasured` aus
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration$' -count=1 -v` gibt weiterhin zehn `--- PASS:`-Untertestzeilen und keine `--- FAIL:`-Zeile aus — die drei von Plan 02-29 umgestellten Deklarations-Zeilen ueberleben das Ersetzen des allquantifizierten Satzes
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalSetEqualsTheServers$' -count=1 -v` gibt `--- PASS:` aus, ohne dass der Sweep dieses Tests veraendert wurde
    - **Fail-first des Abdeckungstests:** die SUMMARY zitiert seine Rot-Ausgabe, gemessen indem einmal ein `guardBlindSpots`-Eintrag ohne messende Zeile angelegt wurde; die Aenderung wird zurueckgenommen
    - **Fail-first der Blindheitszeile:** die SUMMARY zitiert die Rot-Ausgabe der Zeile, gemessen indem die Fixture-Quelle einmal so veraendert wurde, dass der Anker sie nicht mehr trifft. Das ist der Beleg, dass die Zeile ueberhaupt etwas misst — bei umgekehrter Abnahme ist das nicht selbstverstaendlich
    - **Live-Pfad-Beleg:** die SUMMARY belegt, dass die Blindheitszeile durch `browserRefusalRanges` und damit durch `readSource` laeuft, gemessen indem einmal eine Vorverarbeitung in diese Funktion gesetzt wurde, die die Fixture leert, und die Zeile rot gesehen wurde; die Aenderung wird zurueckgenommen
    - Ueber `TestBrowserRefusalGuardBindsToAnIdentifierAndNotToCode` steht im Code die Anweisung, was bei einem Rot zu tun ist, und dass der Waechter dafuer nicht aufgeweicht wird
    - `git diff --quiet HEAD -- web/` ist gruen
    - `gofmt -l ./internal` gibt nichts aus
  </acceptance_criteria>
  <verify>
    <automated>grep -Fc 'guardBlindSpots' internal/imagefactory/guard_drift_test.go | grep -qv '^0$'</automated>
    <fails_when>Exit 1 — die Datei enthaelt den Bezeichner nicht, die Ausschlussliste existiert also nicht als Daten. Dann ist die Behauptung des Waechters wieder Prosa, und der Abdeckungstest hat nichts, woran er sie binden koennte.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalGuardBindsToAnIdentifierAndNotToCode$' -count=1 -v 2>&1 | grep -q '^    --- PASS: TestBrowserRefusalGuardBindsToAnIdentifierAndNotToCode/a_declaration_surviving_only_in_a_block_comment'</automated>
    <fails_when>Exit 1 der Pipeline. Der Test existiert nicht — ein `-run`-Filter, der nichts trifft, endet in Go fuer sich allein mit 0, deshalb diese Form. Oder das Paket uebersetzt nicht. Oder die Zeile ist `--- FAIL:`, und dann in einer von zwei Richtungen: der Leser hat die Fixture abgelehnt (dann steht die Fehlermeldung des Waechters im Log, und das hiesse, die Blindheit ist geendet — siehe die Anweisung ueber dem Test), oder er hat eine andere Zahl von Bereichen gelesen als sechs, dann nennt die Zeile beide Zahlen.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestGuardBlindSpotsAreEachMeasured$' -count=1 -v 2>&1 | grep -q '^--- PASS: TestGuardBlindSpotsAreEachMeasured '</automated>
    <fails_when>Exit 1 ohne `--- PASS:`-Zeile. Entweder existiert der Test nicht, oder Liste und Zeilen stimmen nicht ueberein: ein Mechanismus ist gelistet und von keiner Zeile gemessen, oder eine Zeile nennt eine Id, die die Liste nicht fuehrt, oder es gibt nicht genau einen markierten Eintrag. Die Ausgabe nennt die abweichenden Ids namentlich.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration$' -count=1 -v 2>&1 | grep -c '^    --- PASS: ' | grep -qx '10'</automated>
    <fails_when>Exit 1, weil die Zahl der bestandenen Untertests nicht 10 ist. Der wahrscheinliche Weg dorthin: beim Ersetzen des allquantifizierten Satzes ist eine der beiden Teilzeichenketten mitverschwunden, die Plan 02-29 in den drei Deklarations-Zeilen gepinnt hat. Die Ausgabe zitiert dann die gesuchte Zeichenkette und die tatsaechliche Meldung nebeneinander.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalSetEqualsTheServers$' -count=1 -v 2>&1 | grep -q '^--- PASS: TestBrowserRefusalSetEqualsTheServers '</automated>
    <fails_when>Exit 1 ohne `--- PASS:`-Zeile. Der einzige Weg, den dieser Task oeffnet, ist der umgebaute Aufruf: wird ein anderer Pfad als `imagesRoutePath` uebergeben, meldet die Ausgabe einen Lesefehler mit dem falschen Dateinamen; wird die Signatur falsch verdrahtet, uebersetzt das Paket nicht.</fails_when>
    <automated>git diff --quiet HEAD -- web/</automated>
    <fails_when>Exit 1, weil ein Unterschied unter `web/` im Baum steht. Dieser Plan darf `web/src/routes/images.tsx` weder netto noch voruebergehend anfassen — insbesondere nicht mit den drei Hinweiszeilen, die der Entwurf von Variante B vorsieht und die diese Runde ausdruecklich nicht schreibt.</fails_when>
  </verify>
  <done>Der Waechter behauptet nur noch, was er misst; die Behauptung ist aus einer Datenliste gerendert; ein Mechanismus ist ueber den Live-Lesepfad gemessen und mit der Liste verbunden; und beide Richtungen des Abdeckungstests sind rot gesehen worden, bevor sie gruen waren.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Die uebrigen Mechanismen und ihre gemessenen Auspraegungen — sieben Zeilen, drei Mechanismen, ein markierter Ausschluss</name>
  <reversibility rating="reversible">Weitere Zeilen und Listeneintraege in derselben Testdatei. Kein Produktionsverhalten, kein Zustand.</reversibility>
  <precondition>Die Liste, die Behauptung, der parametrisierte Lesepfad und der Abdeckungstest aus Task 1 stehen. Pruefen mit `go test ./internal/imagefactory -run '^TestGuardBlindSpotsAreEachMeasured$' -count=1` — erwartet Exit 0. Ohne den Abdeckungstest sind die Zeilen dieses Tasks nicht an die Liste gebunden, und genau diese Bindung ist der Gegenstand von Punkt 5 der Schliessbedingung von Ledger-Eintrag 72.</precondition>
  <read_first>
    - `internal/imagefactory/guard_drift_test.go` in dem Zustand, den Task 1 hinterlassen hat —
      insbesondere der Zeilentyp, die Zeilen-Paketvariable, `guardBlindSpots`, `honestClaim` und der
      Abdeckungstest.
    - `web/src/routes/images.tsx:103-131` — der Typalias `RefusedRange`, die Deklarationszeile und
      die sechs Eintraege mit ihren Kommentaren. **Die sechs Eintraege werden in die Fixtures dieses
      Tasks woertlich uebernommen**, damit die gemessene Bereichszahl dieselbe ist wie am echten Baum
      und die Fixture nicht versehentlich eine andere Menge beschreibt.
    - `web/src/routes/images.tsx:152-165` — `hasControlCharacter` mit dem Aufruf, der die einzige
      lebende Referenz auf die Tabelle im ganzen Baum ist. **Das ist der Mechanismus der dritten
      Gruppe:** faellt dieser Aufruf aus dem Validierungspfad, ist die Tabelle eine wohlgeformte,
      lebende, uebereinstimmende Leiche, und der Waechter ist gruen darueber.
    - `web/src/routes/images.tsx:590-600` und `:1150-1160` — zwei der fuenf bestehenden
      `{/* … */}`-JSX-Kommentare dieser Datei, als Beleg dafuer, dass diese Kommentarform hier
      etablierte Konvention ist und die zugehoerige Fixture keine erfundene Form beschreibt.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md`,
      Abschnitt "Was keine der vier Varianten schliesst" — die Ableitung und die eine tragende
      Verbindung, beide mit ihren gemessenen Ausgaben. Sie sind der Inhalt der zweiten und dritten
      Gruppe.
  </read_first>
  <behavior>
    - Drei weitere Auspraegungen desselben Text-Mechanismus werden gemessen und gelesen: ein
      eingerruecktes `{/* … */}`-JSX-Kommentar, ein Template-Literal und ein JSX-Textknoten in einem
      `<pre>`-Block. Alle drei mit dem echten Import daneben, alle drei sechs Bereiche, kein Fehler.
    - Ein zweiter Mechanismus wird gelistet und gemessen: der Anker bindet an das **Literal**, nicht
      an den **Wert**, den der Ausdruck darum herum erzeugt. Zwei Auspraegungen — ein hinter das
      Literal gehaengtes `].filter(…)`, das eine Klasse entfernt, und ein Spread in die Tabelle, die
      das Formular tatsaechlich benutzt. Beide sechs Bereiche, kein Fehler; beide auf lebendem,
      kompilierendem Code, bei dem das Formular und der Server auseinanderlaufen.
    - Ein dritter Mechanismus wird gelistet und gemessen: der Anker bindet an die **Deklaration**,
      nicht an die **Verwendung**. Eine eingerueckte Rest-Deklaration neben dem Import der echten
      Tabelle wird gelesen — zwei Bereiche, kein Fehler, ohne einen Schraegstrich, Backtick oder
      Anfuehrungsstrich im Spiel.
    - `TestGuardBlindSpotsAreEachMeasured` bleibt gruen: vier Eintraege, drei davon von Zeilen
      gemessen, einer markiert.
    - Alle vier bestehenden Tabellen bleiben unveraendert gruen.
  </behavior>
  <files>internal/imagefactory/guard_drift_test.go</files>
  <action>
**Die drei weiteren Auspraegungen des Text-Mechanismus**, als Zeilen der bestehenden
Zeilen-Paketvariable. Namen exakt `a declaration surviving only in an indented JSX comment`,
`a declaration surviving only in a template literal` und
`a declaration surviving only as JSX text in a pre block`. Alle drei nennen dieselbe Blindspot-Id wie
die Zeile aus Task 1, alle drei tragen den echten Import plus eine Verwendung, alle drei erwarten
sechs Bereiche.

Zwei Hinweise zur Ausfuehrung, damit der Executor nicht gegen die Sprache kaempft. Die
Template-Literal-Fixture enthaelt Backticks; ein Backtick hat in einem in Anfuehrungszeichen
gesetzten Go-String **keine** Sonderbedeutung, also wird diese Fixture als interpretierter String mit
`\n`-Escapes geschrieben und nicht als roher String. Und die JSX-Kommentar-Fixture wird
**eingerueckt** geschrieben, weil genau das der Punkt ist: der Anker beginnt mit einem
Zeilenanfangs-Muster gefolgt von Leerraum, und Einrueckung erfuellt ihn.

**Der zweite Mechanismus: Literal statt Wert.** Ein neuer Eintrag in `guardBlindSpots`, dessen
Prosa-Zeile den Mechanismus benennt — der Anker liest das Array-Literal, waehrend das Formular den
Wert benutzt, den der Ausdruck darum herum erzeugt — und dessen Beleg-Feld die gemessenen Ausgaben
traegt. Der Kommentar an diesem Eintrag sagt, warum er der gefaehrlichste bekannte Rest ist: er ist
**gruen auf echter Drift**. Bei `].filter((r) => r.class !== 'byte order mark')` liest der Waechter
sechs Bereiche und meldet Uebereinstimmung, waehrend das Formular U+FEFF wieder annimmt und der
Server es weiter mit einem 400 ablehnt — der Operator lernt es aus dem Fehler statt aus der Zeile,
die er gerade anschaut, also woertlich der Defekt, fuer den G-02-11 existiert.

Dazu eine zweite Zeilen-Paketvariable und ein zweiter Test,
`TestBrowserRefusalGuardBindsToALiteralAndNotToTheValueTheFormUses`, mit derselben umgekehrten
Abnahme, demselben Lesepfad und derselben Anweisung darueber, was bei einem Rot zu tun ist. Zwei
Zeilen: `a declaration whose literal is filtered before it is bound` und
`a declaration spread into the table the form really uses`, beide sechs Bereiche.

**Der dritte Mechanismus: Deklaration statt Verwendung.** Ein weiterer Eintrag in `guardBlindSpots`
mit dem Mechanismus — der Anker bindet an die Deklaration, nicht an die Aufrufstelle, die sie lebendig
macht — und der gemessenen Ausgabe. Sein Kommentar nennt die Tatsache, die ihn traegt: `images.tsx`
hat genau **eine** lebende Referenz auf die Tabelle, und faellt sie aus dem Validierungspfad, ist die
Tabelle eine wohlgeformte, lebende, uebereinstimmende Leiche. Dazu eine dritte Zeile in der zweiten
Tabelle: `an indented leftover declaration beside the real table's import`, mit dem Import der echten
Tabelle aus einem anderen Modul, einer Verwendung daraus und einer eingerueckten Rest-Deklaration in
einer Funktion. Erwartete Bereichszahl **zwei**, nicht sechs — die Rest-Tabelle ist kuerzer als die
echte, und genau das macht den Schaden sichtbar.

**Den Abdeckungstest gruen halten.** Nach diesem Task hat `guardBlindSpots` vier Eintraege, drei
davon von zusammen sieben Zeilen gemessen, einer markiert. Der Abdeckungstest wird nicht
aufgeweicht — er zieht jetzt beide Zeilen-Paketvariablen zusammen.

**Rot-vor-gruen fuer die beiden neuen Mechanismen.** Beide Eintraege zuerst in `guardBlindSpots`
anlegen und den Abdeckungstest fahren: er ist rot, weil keine Zeile sie nennt, und diese Ausgabe
gehoert in die SUMMARY. Danach die Zeilen anlegen und ihn gruen sehen. Das ist der Beleg, dass die
Bindung zwischen Liste und Zeilen in beide Richtungen wirkt.

**Zuletzt alle sechs Tabellen einzeln fahren** und ihre Untertestzahlen pruefen: zehn, zwei, drei,
vier, drei — und den Abdeckungstest.
  </action>
  <acceptance_criteria>
    - `guardBlindSpots` traegt vier Eintraege; genau einer ist markiert, und seine Id ist die des Surrogat-Ausschlusses
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalGuardBindsToAnIdentifierAndNotToCode$' -count=1 -v` gibt genau vier `--- PASS:`-Untertestzeilen und keine `--- FAIL:`-Zeile aus
    - `go test ./internal/imagefactory -run '^TestBrowserRefusalGuardBindsToALiteralAndNotToTheValueTheFormUses$' -count=1 -v` gibt genau drei `--- PASS:`-Untertestzeilen und keine `--- FAIL:`-Zeile aus
    - Die Untertestnamen enthalten `an_indented_JSX_comment`, `a_template_literal`, `JSX_text_in_a_pre_block`, `filtered_before_it_is_bound`, `spread_into_the_table_the_form_really_uses` und `indented_leftover_declaration`
    - `go test ./internal/imagefactory -run '^TestGuardBlindSpotsAreEachMeasured$' -count=1 -v` gibt `--- PASS:` aus
    - Die Zeile `an indented leftover declaration beside the real table's import` erwartet zwei Bereiche und nicht sechs; ihr Kommentar sagt, warum die kuerzere Zahl der Schaden ist
    - Jede der sieben Fixtures traegt entweder den Import der echten Tabelle aus einem anderen Modul plus eine Verwendung, oder eine Verwendung des abgeleiteten Wertes — keine Fixture ist ein blosser TypeScript-Fehler
    - **Rot-vor-gruen der Listenbindung:** die SUMMARY zitiert die Rot-Ausgabe des Abdeckungstests, gemessen mit den beiden neuen Eintraegen und noch ohne ihre Zeilen
    - Die SUMMARY zitiert fuer **jede** der sieben Zeilen die gemessene Ausgabe des Waechters — Bereichszahl und Fehlerzustand. Das ist Punkt 5 der Schliessbedingung von Ledger-Eintrag 72, und eine Liste, deren Formen nicht einzeln gemessen sind, erfuellt ihn nicht
    - `go test ./internal/imagefactory/... -count=1` ist gruen
    - `git diff --quiet HEAD -- web/` ist gruen
    - `gofmt -l ./internal` gibt nichts aus
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalGuardBindsToAnIdentifierAndNotToCode$' -count=1 -v 2>&1 | grep -c '^    --- PASS: ' | grep -qx '4'</automated>
    <fails_when>Exit 1, weil die Zahl der bestandenen Untertests nicht 4 ist. Weniger heisst, eine Auspraegung fehlt oder ist rot — im zweiten Fall hat der Waechter die Fixture abgelehnt, und die Ausgabe traegt seine Fehlermeldung; bei umgekehrter Abnahme ist das die Meldung, dass die Blindheit geendet hat, und nicht ein Defekt der Zeile.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestBrowserRefusalGuardBindsToALiteralAndNotToTheValueTheFormUses$' -count=1 -v 2>&1 | grep -c '^    --- PASS: ' | grep -qx '3'</automated>
    <fails_when>Exit 1, weil die Zahl der bestandenen Untertests nicht 3 ist. Der wahrscheinlichste Weg ist die dritte Zeile: sie erwartet zwei Bereiche, und eine versehentlich sechszeilige Rest-Tabelle in der Fixture liesse sie mit `read 6 ranges, want 2` scheitern.</fails_when>
    <automated>go test ./internal/imagefactory -run '^TestGuardBlindSpotsAreEachMeasured$' -count=1 -v 2>&1 | grep -q '^--- PASS: TestGuardBlindSpotsAreEachMeasured '</automated>
    <fails_when>Exit 1 ohne `--- PASS:`-Zeile. Nach diesem Task heisst das: einer der beiden neuen Eintraege hat keine messende Zeile bekommen, oder eine der neuen Zeilen nennt eine Id, die die Liste nicht fuehrt. Die Ausgabe nennt die abweichenden Ids namentlich.</fails_when>
    <automated>go build ./... && go vet ./... && go test ./internal/imagefactory/... -count=1</automated>
    <fails_when>Nicht-Null-Exit, oder `FAIL` in der Ausgabe. `go vet` faellt insbesondere ueber eine Fixture, in der ein roher String-Literal einen Backtick enthaelt — genau der Fall, den die Template-Literal-Zeile provoziert und den die Aktion deshalb ausdruecklich adressiert.</fails_when>
    <automated>git diff --quiet HEAD -- web/</automated>
    <fails_when>Exit 1, weil ein Unterschied unter `web/` im Baum steht. Die Eintraege der echten Tabelle werden in die Fixtures KOPIERT, nicht aus der Datei gelesen und nicht in ihr veraendert.</fails_when>
  </verify>
  <done>Vier Mechanismen sind gelistet, drei davon von sieben Zeilen einzeln gemessen und der vierte ausdruecklich als nicht zeilenmessbar markiert; jede gemessene Ausgabe steht in der SUMMARY; und die Bindung zwischen Liste und Zeilen wurde in beide Richtungen rot gesehen.</done>
</task>

<task type="auto">
  <name>Task 3: Der Versuch, eine Form zu finden, die die Liste nicht nennt — Punkt 6 der Schliessbedingung, einmal ausgefuehrt und protokolliert</name>
  <reversibility rating="reversible">Ein Messlauf gegen den ausgelieferten Waechter und, nur falls er etwas findet, ein weiterer Listeneintrag mit seiner Zeile. Kein Produktionsverhalten, kein Zustand.</reversibility>
  <precondition>Die vier Eintraege in `guardBlindSpots` und die sieben messenden Zeilen aus Task 2 stehen, denn dieser Task misst gegen den ausgelieferten Waechter und nicht gegen einen Zwischenstand. Pruefen mit `go test ./internal/imagefactory -run '^TestGuardBlindSpotsAreEachMeasured$' -count=1` — erwartet Exit 0.</precondition>
  <read_first>
    - `internal/imagefactory/guard_drift_test.go` in dem Zustand, den Task 2 hinterlassen hat.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-DECISION-drift-guard-lexik.md`,
      Abschnitt "Die Schliessbedingung von Eintrag 72, pruefbar und endlich", **Punkt 6
      vollstaendig**, samt dem Satz danach: *"Punkt 6 ist die eigentliche Huerde und der Grund, warum
      diese Bedingung diese Empfehlung widerlegen kann. Ein Eintrag, dessen Bedingung nur die schon
      bekannten Formen abfragt, waere ein Eintrag, der sich selbst schliesst."*
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-29-PLAN.md`, Task 3 — die
      Duplikatspruefung und die beiden Zeilen, die sie festnagelt. **Eine der Formen, die dieser Task
      versucht, ist von Plan 02-29 bereits geschlossen worden**, und das gehoert ins Protokoll: sie
      wurde beim Planen dieser Runde durch genau diesen Versuch gefunden.
  </read_first>
  <behavior>
    - Mindestens fuenf Formen werden gegen den ausgelieferten Waechter gefahren: `String.raw`, ein
      Regex-Literal, eine `${…}`-Interpolation, eine Objekt-Property und ein Re-Export. Jede Ausgabe
      wird protokolliert.
    - Formen, die der Waechter liest und deren Mechanismus die Liste bereits nennt, sind kein Fund —
      sie sind der Beleg dafuer, dass die Skopierung nach Mechanismus traegt.
    - Eine Form, die der Waechter liest und deren Mechanismus die Liste **nicht** nennt, ist ein Fund:
      sie bekommt einen Eintrag in `guardBlindSpots` und eine messende Zeile, und der Fund wird in der
      SUMMARY benannt.
    - Eine Form, die der Waechter ablehnt, ist kein Fund und wird mit ihrer Fehlermeldung
      protokolliert.
    - Der Versuch wird protokolliert, auch wenn er nichts findet. Ein nicht protokollierter Versuch
      ist von einem unterlassenen nicht zu unterscheiden.
  </behavior>
  <files>internal/imagefactory/guard_drift_test.go</files>
  <action>
Diesen Task als **Messung** fahren, nicht als Aenderung. Die fuenf Formen werden gegen die
ausgelieferte `parseBrowserRefusalRanges` gefahren — am billigsten ueber einen temporaeren Testfall im
Paket, der danach wieder entfernt wird, genau wie der Verifier der Runde 6 seine eigene Sonde gefahren
und danach geloescht hat. Am Ende muss `git status` ausser den geplanten Aenderungen nichts zeigen.

Die fuenf Formen, jede mit dem echten Import daneben, damit sie ein Schadensfall ist und kein
TypeScript-Fehler: die Tabelle in einem `String.raw`-Template; die Tabelle innerhalb eines
Regex-Literals; die Tabelle innerhalb einer `${…}`-Interpolation in einem Template-Literal; die
Tabelle als Wert einer Objekt-Property statt als eigene Deklaration; und ein Re-Export des
Bezeichners aus einem anderen Modul.

Fuer jede Form protokollieren: die gelesene Bereichszahl, den Fehlerzustand und — falls gelesen —
welcher Eintrag aus `guardBlindSpots` sie deckt. Beim Planen dieser Runde wurden alle fuenf gemessen;
die ersten drei werden gelesen und sind Auspraegungen des Text-Mechanismus, die letzten beiden werden
korrekt abgelehnt. Diese Werte sind **Erwartung und nicht Ergebnis** — wird etwas anderes gemessen,
gilt das Gemessene, und die Abweichung gehoert benannt.

**Eine sechste Form gehoert ins Protokoll, weil sie beim Planen dieser Runde durch genau diesen
Versuch gefunden wurde:** zwei Deklarationen desselben Bezeichners, die eingerueckte innere zuerst.
Der Waechter nahm schweigend die erste und erklaerte den Fall mit der Ursache eines anderen Defekts.
Plan 02-29 hat sie geschlossen. Sie gegen den ausgelieferten Waechter nachfahren, die
Duplikat-Meldung protokollieren und in der SUMMARY festhalten, dass Punkt 6 in dieser Runde einen
Fund geliefert hat und dass er behoben und nicht gelistet wurde. Das ist der Unterschied zwischen
einem Versuch, der etwas kostet, und einer Zeremonie.

**Findet der Lauf eine Form, die der Waechter liest und deren Mechanismus die Liste nicht nennt:**
einen Eintrag in `guardBlindSpots` anlegen, eine messende Zeile in die passende Tabelle setzen und
den Abdeckungstest gruen sehen. Der Fund wird ausdruecklich in der SUMMARY benannt und geht an Plan
02-31 fuer den Text von Eintrag 72.

**In jedem Fall gilt:** Eintrag 72 bleibt danach offen. Dieser Task fuehrt Punkt 6 **einmal** aus; die
Schliessbedingung verlangt ihn von der Verifikationsrunde, die den Eintrag schliesst. Ein Eintrag,
dessen Bedingung nur die schon bekannten Formen abfragt, waere ein Eintrag, der sich selbst schliesst
— und dieser Satz gehoert in die SUMMARY, damit die naechste Runde ihn nicht als erledigt liest.
  </action>
  <acceptance_criteria>
    - Die SUMMARY fuehrt eine Tabelle mit mindestens sechs Zeilen — `String.raw`, Regex-Literal, `${…}`-Interpolation, Objekt-Property, Re-Export und die Doppel-Deklaration —, je mit gelesener Bereichszahl, Fehlerzustand und, falls gelesen, dem deckenden Eintrag aus `guardBlindSpots`
    - Die SUMMARY sagt ausdruecklich, ob der Versuch einen Fund geliefert hat oder nicht. Ein nicht protokollierter Versuch ist von einem unterlassenen nicht zu unterscheiden
    - Die SUMMARY haelt fest, dass die Doppel-Deklaration beim Planen dieser Runde durch diesen Versuch gefunden und von Plan 02-29 behoben wurde — Punkt 6 hat in dieser Runde etwas gekostet
    - Wurde ein Fund gemacht, traegt `guardBlindSpots` einen entsprechenden Eintrag und die passende Tabelle eine messende Zeile; `TestGuardBlindSpotsAreEachMeasured` ist gruen
    - Die SUMMARY sagt, dass Eintrag 72 offen bleibt und dass Punkt 6 von der schliessenden Verifikationsrunde erneut verlangt wird
    - Die temporaere Sonde ist entfernt; `git status --porcelain` zeigt nach dem Herausfiltern von `internal/imagefactory/guard_drift_test.go`, aller `.planning/`-Pfade und der SUMMARY keine Zeile mehr. **`.planning/` gehoert ausdruecklich in den Filter:** `/gsd-execute-phase` schreibt regulaer `STATE.md` und das Phasen-Manifest, und ein Filter, der das nicht durchlaesst, geht falsch-rot ueber die eigene Werkzeugkette
    - `go test ./internal/imagefactory/... -count=1` ist gruen
    - `git diff --quiet HEAD -- web/` ist gruen
    - `gofmt -l ./internal` gibt nichts aus
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/imagefactory -run '^TestGuardBlindSpotsAreEachMeasured$' -count=1 -v 2>&1 | grep -q '^--- PASS: TestGuardBlindSpotsAreEachMeasured '</automated>
    <fails_when>Exit 1 ohne `--- PASS:`-Zeile. Wurde in diesem Task ein Fund gemacht, heisst das: der Eintrag wurde angelegt und die messende Zeile nicht, oder umgekehrt. Wurde keiner gemacht, heisst es, dass die Sonde die Liste beruehrt hat, obwohl sie nur messen sollte.</fails_when>
    <automated>git status --porcelain | grep -vE '^.{2} internal/imagefactory/guard_drift_test\.go$' | grep -vE '^.{2} \.planning/' | grep -vE 'SUMMARY\.md$' | grep -qc . && exit 1 || exit 0</automated>
    <fails_when>Exit 1, weil `git status --porcelain` nach dem Herausfiltern der Testdatei, aller `.planning/`-Pfade und der SUMMARY noch mindestens eine Zeile ausgibt. Der wahrscheinlichste Fall ist eine liegengebliebene Sondendatei im Paket — genau das, was der Verifier der Runde 6 mit `git status ist leer` als Abnahme seiner eigenen Sonde gefuehrt hat. Die beiden Statusspalten werden mit `^.{2} ` toleriert und nicht mit `^ M `: nach einem `git add` steht dort `M ` statt ` M`, und ein Filter, der nur den ungestageten Fall trifft, meldet den gestageten als Fund. Und `.planning/` steht im Filter, weil `/gsd-execute-phase` dort regulaer `STATE.md` und das Manifest schreibt — dieser Task duerfte sonst nicht bestehen, waehrend er alles richtig macht.</fails_when>
    <automated>go build ./... && go vet ./... && go test ./internal/imagefactory/... -count=1</automated>
    <fails_when>Nicht-Null-Exit, oder `FAIL` in der Ausgabe. Nach einer entfernten Sonde ist der haeufigste Weg dorthin ein zurueckgebliebener, jetzt unbenutzter Import oder Hilfsbezeichner — `go vet` und der Compiler nennen ihn namentlich.</fails_when>
    <automated>git diff --quiet HEAD -- web/</automated>
    <fails_when>Exit 1, weil ein Unterschied unter `web/` im Baum steht. Auch die Sonde arbeitet ausschliesslich auf synthetischen Quellen in einem Temp-Verzeichnis.</fails_when>
  </verify>
  <done>Punkt 6 der Schliessbedingung ist einmal ausgefuehrt: mindestens sechs Formen sind gegen den ausgelieferten Waechter gefahren, jede Ausgabe steht protokolliert in der SUMMARY, ein Fund ist entweder gelistet und gemessen oder ausdruecklich als ausgeblieben vermerkt, und die Sonde ist rueckstandslos entfernt.</done>
</task>

</tasks>

## Artifacts this phase produces

| Kind | Symbol | Location |
|---|---|---|
| type | `guardBlindSpot` — ein Mechanismus, den der Anker nicht sieht: Id, Prosa, gemessene Ausgabe, Kennzeichen fuer nicht zeilenmessbar | `internal/imagefactory/guard_drift_test.go` |
| data | `guardBlindSpots` — vier Eintraege: Text-statt-Code, Literal-statt-Wert, Deklaration-statt-Verwendung, Surrogat-Inneres (markiert) | `internal/imagefactory/guard_drift_test.go` |
| function | `honestClaim` — rendert die verengte Behauptung des Waechters aus `guardBlindSpots` | `internal/imagefactory/guard_drift_test.go` |
| signature change | `browserRefusalRanges` nimmt den Pfad als Parameter; der Wrapper entfaellt, es gibt genau einen Lesepfad | `internal/imagefactory/guard_drift_test.go` |
| text removal | der allquantifizierte Satz ueber Umbenennen, Verschieben und Loeschen verschwindet aus dem Doc-Kommentar und aus dem Fehlertext | `internal/imagefactory/guard_drift_test.go` |
| type | `blindnessRow` — Name, Blindspot-Id, synthetische Quelle, erwartete Bereichszahl | `internal/imagefactory/guard_drift_test.go` |
| test | `TestBrowserRefusalGuardBindsToAnIdentifierAndNotToCode` mit vier Zeilen: Blockkommentar, eingerruecktes JSX-Kommentar, Template-Literal, JSX-Text in `<pre>` | `internal/imagefactory/guard_drift_test.go` |
| test | `TestBrowserRefusalGuardBindsToALiteralAndNotToTheValueTheFormUses` mit drei Zeilen: `].filter(…)`, Spread, eingerueckte Rest-Deklaration | `internal/imagefactory/guard_drift_test.go` |
| test | `TestGuardBlindSpotsAreEachMeasured` — Mengengleichheit zwischen gelisteten und gemessenen Mechanismen, plus genau ein markierter Eintrag | `internal/imagefactory/guard_drift_test.go` |
| test source | die sieben Blindheits-Fixtures, je mit dem echten Import bzw. einer Verwendung des abgeleiteten Wertes daneben | `internal/imagefactory/guard_drift_test.go` |
| measurement | die gemessene Ausgabe jeder der sieben Zeilen (Punkt 5 der Schliessbedingung von Eintrag 72) | `02-30-SUMMARY.md` |
| measurement | das Protokoll des Punkt-6-Versuchs ueber mindestens sechs Formen, mit Fund oder ausdruecklichem Nicht-Fund | `02-30-SUMMARY.md` |
| measurement | die Rot-Ausgaben: Abdeckungstest in beide Richtungen, Blindheitszeile gegen eine nicht getroffene Fixture, Live-Pfad-Beleg per eingesetzter Vorverarbeitung | `02-30-SUMMARY.md` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `web/src/routes/images.tsx` → `internal/imagefactory/guard_drift_test.go` | ein Browser kann kein Go-Symbol importieren, also transkribiert die TypeScript-Seite, was Go entscheidet. Diese Naht wird von einem regulaeren Ausdruck ueber eine Quelldatei ueberquert, nicht von einem Typsystem — und was ein regulaerer Ausdruck darueber nicht feststellen kann, ist der Gegenstand dieses Plans |
| die Behauptung des Waechters → der Mensch, der ihr glaubt | ein gruener Lauf ist die einzige Evidenz fuer die Behauptung "die beiden Ablehnungsmengen stimmen ueberein". Behauptet der Waechter mehr, als er misst, ist der Ueberhang unsichtbar und wird geglaubt |
| das Literal in der Quelle → der Wert, den das Formular benutzt | zwischen `const REFUSED_RANGES = [ … ]` und dem, was `hasControlCharacter` prueft, kann ein beliebiger Ausdruck stehen. Der Waechter sieht das Literal; der Operator erlebt den Wert |
| die Deklaration → ihre einzige lebende Verwendung | `images.tsx` hat genau einen Aufruf, der die Tabelle lebendig macht. Faellt er aus dem Validierungspfad, ist die Tabelle eine uebereinstimmende Leiche und jeder Waechter dieser Bauart gruen darueber |

## STRIDE Threat Register

ASVS-Level 1, blockierend ab `high`. Ids T-02-129..T-02-131. Plan 02-29 dieser Runde vergibt
T-02-126..T-02-128; die naechste freie Id nach diesem Plan ist T-02-132.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-129 | Repudiation | der Waechter behauptet in seinem eigenen Fehlertext und Doc-Kommentar eine allquantifizierte Eigenschaft, die in drei aufeinanderfolgenden Runden je einmal falsifiziert wurde. Wer sie liest, haelt eine Menge fuer bewacht, die es nicht ist | high | mitigate | der Satz wird an beiden Stellen entfernt und durch eine Aussage ueber den Text plus eine Ausschlussliste ersetzt, die als Daten gefuehrt und von `TestGuardBlindSpotsAreEachMeasured` an gemessene Zeilen gebunden wird. Die Aritaet des Restes aendert sich von unendlich vielen Falsifikationen zu endlich vielen Ergaenzungen |
| T-02-130 | Tampering | eine Ableitung zwischen Literal und Wert — `].filter(…)`, `…SPREAD`, `].slice(…)` — laesst den Waechter gruen, waehrend das Formular eine andere Menge ablehnt als der Server. Gemessen auf lebendem, kompilierendem, referenziertem Code; **gruen auf echter Drift** und damit von derselben Schwere wie G-02-11 selbst | high | mitigate | nicht behebbar mit den Mitteln dieser Runde, deshalb **benannt statt verschwiegen**: eigener Eintrag in `guardBlindSpots` mit dem Mechanismus und der gemessenen Ausgabe, zwei messende Tabellenzeilen mit umgekehrter Abnahme, und Aufnahme in Ledger-Eintrag 72 durch Plan 02-31. Die Skopierung nach Mechanismus statt nach Kommentarform ist genau das, was verhindert, dass dieser Pfad ungeledgert bleibt, waehrend der Eintrag buchstabengetreu geschlossen wird |
| T-02-131 | Spoofing | ein kuenftiges `stripNonCode` am Aufrufer statt in der gelesenen Funktion laesst die Blindheitstabellen gruen, obwohl die Blindheit geendet hat — die Tabelle gibt dann vor, eine Eigenschaft zu bewachen, die niemand mehr prueft | medium | mitigate | `browserRefusalRanges` nimmt den Pfad als Parameter und es gibt keinen Wrapper mehr; die Blindheitstabellen fahren genau die Funktion, die `TestBrowserRefusalSetEqualsTheServers` fährt, einschliesslich `readSource`. Der Live-Pfad-Beleg wird gemessen, indem einmal eine Vorverarbeitung in diese Funktion gesetzt und die Zeile rot gesehen wird |
| T-02-SC | Tampering | npm/pip/cargo installs | low | mitigate | dieser Plan fuegt kein Go-Modul und kein npm-Paket hinzu; es gibt keinen Installationsschritt und daher nichts zu pruefen. Neu importiert wird ausschliesslich `path/filepath` aus der Standardbibliothek, fuer das Zusammensetzen des Pfades unter `t.TempDir()` |
</threat_model>

<verification>
- `go build ./... && go vet ./...` ist sauber
- `go test ./internal/imagefactory/... -count=1` ist gruen
- `go test ./... -count=1 -race` ist ueber alle Pakete gruen. **Nicht** `task test`: das `task`-Binary ist auf diesem Host nicht auf dem PATH, und `Taskfile.yml` definiert `test` als genau dieses Kommando
- `golangci-lint run` endet mit 0 (Binary unter `~/go/bin/golangci-lint`, siehe Ledger-Eintrag 34)
- `gofmt -l ./internal` gibt nichts aus
- `GOPROXY=off go test ./internal/imagefactory/` bleibt offline gruen — Variante B fuegt keine Abhaengigkeit hinzu, und das ist einer ihrer beiden Kostenvorteile gegenueber Variante D. Der Lauf und seine Dauer gehoeren in die SUMMARY
- Alle sechs Tabellentests werden **einzeln** mit `-run '^Name$' -v` gefahren und auf ihre `--- PASS:`-Zeilen geprueft, und die **Zahl** der bestandenen Untertests wird geprueft (10 / 2 / 3 / 4 / 3) — ein `-run`-Filter, der nichts trifft, endet in Go mit 0, und ein Tabellentest mit leerer Tabelle ist gruen. Beides ist die Eigenschaft, um die es in dieser Luecke geht
- **Die umgekehrte Abnahme braucht ihren eigenen Beleg.** Eine Tabelle, die `err == nil` verlangt, ist auch dann gruen, wenn sie gar nichts misst. Deshalb wird fuer mindestens eine Zeile gemessen, dass sie rot wird, wenn die Fixture den Anker nicht mehr trifft, und fuer mindestens eine, dass sie rot wird, wenn eine Vorverarbeitung in den Lesepfad gesetzt wird
- **Punkt 6 der Schliessbedingung ist ausgefuehrt und protokolliert**, auch wenn er nichts gefunden hat
- `git diff --quiet HEAD -- web/` ist ueber den ganzen Plan gruen. `npm --prefix web run test` und `npm --prefix web run typecheck` werden **nicht** gefahren: dieser Plan aendert keine Datei unter `web/`
- `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` bleibt bei `"ok": true` und `total_count: 71` — **dieser Plan schreibt den Ledger nicht.** Plan 02-31 besitzt `.planning/WINDOWS.md` fuer diese Runde
- `.planning/REQUIREMENTS.md` wird von diesem Plan nicht geschrieben, und `requirements-completed` in der SUMMARY bleibt leer
</verification>

<success_criteria>
- Der allquantifizierte Satz ist aus dem Doc-Kommentar und aus dem Fehlertext verschwunden; an seiner Stelle steht eine Aussage ueber den Text plus eine Ausschlussliste, und die Liste ist Daten und nicht Prosa
- Die Blindheit ist nach **Mechanismus** skopiert: Literal statt Wert, Bezeichner statt Code, Deklaration statt Verwendung. Kein Listeneintrag beschreibt eine Kommentarform als solche, weil eine Formenliste nach drei Runden wieder unvollstaendig waere
- Vier Mechanismen sind gelistet; drei sind von sieben Tabellenzeilen einzeln gemessen und ihre Ausgaben in der SUMMARY zitiert; der vierte — die 2046 nie verglichenen Codepoints im Inneren des Surrogat-Bereichs — ist ausdruecklich als nicht zeilenmessbar markiert und steht **neben** der Behauptung
- Die Blindheitstabellen laufen ueber denselben Lesepfad wie der Live-Waechter; es gibt keinen Wrapper, in dem eine Vorverarbeitung an ihnen vorbei sitzen koennte, und der Beleg dafuer ist gemessen und nicht behauptet
- Ueber beiden Blindheitstabellen steht im Code, was bei einem Rot zu tun ist und dass der Waechter dafuer nicht aufgeweicht wird
- Punkt 6 der Schliessbedingung von Eintrag 72 ist einmal ausgefuehrt und protokolliert; der Fund, den er beim Planen geliefert hat, ist benannt und von Plan 02-29 behoben
- `TestBrowserRefusalSetEqualsTheServers` misst weiterhin dasselbe und bleibt gruen; die Ablehnungsmenge wurde auf keiner Seite angefasst; die vier bestehenden Tabellen sind unveraendert
- `GOPROXY=off go test ./internal/imagefactory/` bleibt offline gruen; keine neue Abhaengigkeit, kein Subprozess, kein Codegen
- **Dieser Plan behauptet nicht, G-02-29 sei geschlossen.** Er verengt die Behauptung auf null Ueberhang; der Defekt bleibt, Ledger-Eintrag 70 bleibt offen, und der amendierende Eintrag 72 aus Plan 02-31 bleibt es ebenfalls. Ob die verengte Behauptung gilt, misst eine Verifikationsrunde
- **G-02-9 bleibt offen** und keine Zeile dieses Plans sagt etwas anderes; die Ledger-Eintraege 56, 58, 66 und 70 bleiben unberuehrt
- `.planning/WINDOWS.md` und `.planning/REQUIREMENTS.md` werden von diesem Plan **nicht** geschrieben — beide gehoeren Plan 02-31
</success_criteria>

<output>
Create `.planning/phases/02-transport-seam-talossim-image-factory/02-30-SUMMARY.md` when done
</output>

