---
phase: 02-transport-seam-talossim-image-factory
plan: 28
type: execute
wave: 2
depends_on: ["02-27"]
gap_closure: true

# gap_ids ist die maschinenlesbare Behauptung "dieser Plan schliesst diese Luecken".
#
# **Die Id-Entscheidung faellt hier ANDERS aus als in Plan 02-27, und das ist Absicht.** 02-27
# traegt `G-02-25` weiter, weil `02-VERIFICATION.md` Runde 5 unter `re_verification.gaps_remaining`
# woertlich "G-02-25 ... VERENGT, NICHT GESCHLOSSEN" fuehrt — dieselbe Eigenschaft, dreimal verengt,
# eine Spur. Die beiden Luecken dieses Plans sind dagegen in Runde 5 zum ersten Mal aufgetreten:
# Ledger-Eintrag 69 wurde in Runde 5 angelegt und ist erst dadurch falsch geworden, und die vierte
# Zwei-Bedingungen-Aussage hat erst der Sweep von Plan 02-25 zurueckgelassen. Neue Befunde bekommen
# neue Nummern; die naechsten freien der durchlaufenden Phasenserie G-02-1 … G-02-25 sind G-02-26
# und G-02-27. Naechste freie Nummer nach diesem Plan: G-02-28.
#
#   G-02-26  ==  02-VERIFICATION.md Runde 5, gaps[1], status: failed   (rundenlokal G5-2)
#   G-02-27  ==  02-VERIFICATION.md Runde 5, gaps[2], status: partial  (rundenlokal G5-3)
gap_ids: [G-02-26, G-02-27]
gap_id_mapping:
  - gap_id: G-02-26
    verification_round: 5
    round_local_label: G5-2
    verification_status: failed
    id_decision: "neue Id — der Befund entstand in Runde 5 (Ledger-Eintrag 69 wurde dort angelegt) und ist kein Rest einer aelteren Luecke"
    truth: "Ein Abschlussdokument und der Ledger sagen nur, was von der Codebasis wahr ist (T-02-62 / G-02-1-Linie, angewandt auf den Ledger statt auf den Store)"
  - gap_id: G-02-27
    verification_round: 5
    round_local_label: G5-3
    verification_status: partial
    id_decision: "neue Id — die Aussage wurde erst durch den Sweep von Plan 02-25 zu einem Rest; vor Runde 5 gab es sie als Befund nicht"
    truth: "Jede Aussage im Baum, die bisher zwei Bedingungen des Refresh nennt, nennt jetzt drei (Plan 02-25, must_haves.truths[5])"

files_modified:
  - internal/httpapi/handlers/schematics.go
  - .planning/WINDOWS.md
  - .planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md
autonomous: true
requirements: [FACT-02, FACT-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Keine Zeile im Baum, die auf `refreshTheStoredVerdict` verweist, nennt noch eine andere Zahl von Bedingungen als drei. Gemessen wird das mechanisch und nicht durch Aufzaehlung dreier Stellen — genau die Aufzaehlung, die in Runde 5 eine vierte uebersehen hat."
    - "Der Sweep ueber den ganzen Baum ist ausgezaehlt und jede verbleibende Fundstelle ist einzeln beurteilt und in der SUMMARY benannt. `internal/httpapi/handlers/schematics.go:628` sagt weiterhin \"the other two declining conditions\" und das ist richtig, weil es aus einer der drei Bedingungen heraus ueber die beiden anderen spricht; die drei Fundstellen in `schematics_test.go` sagen \"both architectures\" bzw. \"both Talos versions\" ueber je zwei benannte Werte in einer Meldung und nicht ueber eine Zahl von Bedingungen."
    - "`.planning/WINDOWS.md` traegt einen Eintrag, der in Grossbuchstaben eroeffnet, was er an Eintrag 69 ueberholt, den gemessenen Praefix-Fall und die fehlende Bound-Validierung benennt — und der `open` bleibt, weil nur eine Verifikationsrunde feststellen kann, dass die Eigenschaft gilt."
    - "Eintrag 69 bleibt woertlich stehen und bleibt `fixed`: es gibt kein Aenderungs-Verb, und eine bearbeitete Zeile waere eine zweite Unwahrheit ueber dieselbe Sache."
    - "Die drei Repraesentationen des Ledgers — Frontmatter-Zaehler, Markdown-Tabelle und JSON-Block — stimmen nach dieser Runde weiterhin ueberein, unabhaengig vom Werkzeug nachgezaehlt, und die ids sind lueckenlos und doppelfrei."
    - "`02-26-SUMMARY.md` behauptet an keiner Stelle mehr, der Waechter scheitere bei jeder Umbenennung oder G-02-25 sei geschlossen. Die drei betroffenen Stellen — die `coverage`-Zeile D1 sowie die beiden Saetze unter `## Next Phase Readiness` — tragen die Korrektur in der Form, die `02-21-SUMMARY.md:345-358` in dieser Phase etabliert hat: durchgestrichen, WITHDRAWN, datierter Korrekturblock mit der Messung."
    - "Was an `02-26-SUMMARY.md` wahr war, bleibt woertlich stehen. Der Plan korrigiert drei Saetze und schreibt kein Abschlussdokument um."
  prohibitions:
    - statement: "`.planning/WINDOWS.md` nicht von Hand bearbeiten — weder die Markdown-Tabelle noch den JSON-Block noch die Frontmatter-Zaehler. Nur `gsd-tools windows append`, `windows fixed` und `windows status`. Drei Repraesentationen muessen im Gleichschritt bleiben, und nur das Werkzeug haelt sie so. **Es gibt kein Aenderungs-Verb**: eine Behauptung, die sich verschoben hat, bekommt einen neuen Eintrag, der in Grossbuchstaben eroeffnet, was er ueberholt — nie eine bearbeitete Zeile."
      status: unresolved
    - statement: "Eintrag 69 wird weder bearbeitet noch entfernt noch umgedeutet. Er bleibt woertlich stehen und bleibt `fixed`. Er ist die Aufzeichnung dessen, was Runde 5 geglaubt hat, und der neue Eintrag braucht ihn, um darauf zeigen zu koennen."
      status: unresolved
    - statement: "Der neue Eintrag ueber den Waechter wird **nicht** als `fixed` markiert, auch nicht nachdem Plan 02-27 seine vier Korrekturen geliefert hat. `workflow.windows_enforce` liest genau dieses Register beim Ship und ein `fixed`-Eintrag ist dort unsichtbar; ihn bei der Anlage zu schliessen waere woertlich der Fehler, den Runde 5 an Eintrag 69 gefunden hat, ein zweites Mal."
      status: unresolved
    - statement: "Die Eintraege 56, 58 und 66 bleiben unveraendert und weiterhin `open`. 56 zeichnet auf, dass die SERVER-Ablehnungsmenge nicht erschoepfend gemessen ist; 58 und 66 tragen G-02-9. Keiner von ihnen ist von dieser Runde beruehrt."
      status: unresolved
    - statement: "**G-02-9 bleibt offen.** Kein Eintrag, kein Kommentar und keine SUMMARY-Zeile dieses Plans darf ihn als geschlossen melden oder so gelesen werden koennen."
      status: unresolved
    - statement: "Der Text eines angelegten Ledger-Eintrags darf weder ein Pipe-Zeichen noch einen echten Zeilenumbruch enthalten — er ist ein durchgehender Absatz in einer Zeile, so wie die Eintraege 66, 67, 68 und 69 es sind. Beides bricht dieselbe Sache aus demselben Grund: die Markdown-Repraesentation ist pipe-getrennt und zeilenweise, und die Konsistenzpruefung dieses Plans liest den Status positionsbasiert als achtes Feld der Zeile. `renderTable`s `cell()` maskiert `\\` und `|`, aber **keine** Zeilenumbrueche (im Quelltext bei #3689 vermerkt), also erzeugt ein `\n` in der Beschreibung eine Tabellenzeile, die physisch ueber mehrere Dateizeilen laeuft — und das achte Feld der ersten davon ist dann nicht der Status. Eintrag A wird als vierteiliger Text beschrieben, das ist also der wahrscheinliche und nicht der exotische Fall."
      status: unresolved
    - statement: "In `internal/httpapi/handlers/schematics.go` wird ausser dem einen Zahlwort im Kommentar bei `:458` nichts geaendert. Kein Verhalten, keine Signatur, keine Bedingung. Der Diff dieses Tasks ist eine Zeile."
      status: unresolved
    - statement: "An `02-26-SUMMARY.md` wird nur korrigiert, was gemessen falsch ist. Die `coverage`-Eintraege D2 und D3, die Commit-Liste, der Ledger-Abschnitt und die Self-Check-Liste bleiben unberuehrt — sie sind wahr, und ein Abschlussdokument umzuschreiben, weil man es anfasst, ist eine andere Sorte Unwahrheit."
      status: unresolved
  artifacts:
    - path: "internal/httpapi/handlers/schematics.go"
      provides: "den Verweis auf `refreshTheStoredVerdict`, der die richtige Zahl seiner Bedingungen nennt"
      contains: "refreshTheStoredVerdict for the three"
    - path: ".planning/WINDOWS.md"
      provides: "den ueberholenden, offen bleibenden Eintrag zu 69 und den Eintrag ueber die vierte, uebersehene Zwei-Bedingungen-Aussage"
      contains: "SUPERSEDES ENTRY 69"
    - path: ".planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md"
      provides: "die drei WITHDRAWN-Korrekturen in der Form, die 02-21-SUMMARY.md etabliert hat"
      contains: "WITHDRAWN"
  key_links:
    - from: "internal/httpapi/handlers/schematics.go:458"
      to: "internal/httpapi/handlers/schematics.go:513"
      via: "ein Doc-Verweis 55 Zeilen ueber der Funktion, auf die er zeigt. Er ist der einzige Ort, an dem ein Leser der 409-Stelle erfaehrt, wieviele Bedingungen der Refresh hat, bevor er die Funktion selbst liest"
      pattern: "refreshTheStoredVerdict"
    - from: ".planning/WINDOWS.md"
      to: "workflow.windows_enforce"
      via: "`/gsd-ship` blockiert, solange `open_count > 0`. Genau deshalb ist der Unterschied zwischen `open` und `fixed` hier keine Formulierung, sondern ein Mechanismus: ein `fixed`-Eintrag ist beim Ship unsichtbar"
      pattern: "open_count"
    - from: ".planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md"
      to: ".planning/phases/02-transport-seam-talossim-image-factory/02-21-SUMMARY.md"
      via: "`:345-358` — die in dieser Phase etablierte Korrekturform: durchgestrichen, WITHDRAWN, datierter Korrekturblock, die Messung, und die Begruendung, warum die falsche Aussage stehenbleibt statt geloescht zu werden"
      pattern: "WITHDRAWN"
---

<objective>
Der Ledger und ein Abschlussdokument sagen wieder nur, was gemessen ist — und der letzte Verweis im
Baum, der die Zahl der Refresh-Bedingungen nennt, nennt die richtige.

Purpose: Diese Phase korrigiert seit fuenf Runden eine einzige Defektklasse — eine Behauptung, die
breiter ist als ihre Messung. Runde 5 hat sie an zwei Stellen gefunden, und die erste ist die
unangenehmste, weil sie das Register betrifft, mit dem die Phase ihre eigenen Luecken fuehrt.

**Der Ledger.** `.planning/WINDOWS.md` Eintrag 69 ist bei der Anlage `fixed` und sagt "Plan 02-26 hat
es geschlossen: `refusedRangesDecl` schneidet die Deklaration zuerst heraus ..., ihr Fehlen ist ein
Fehler statt eines kuerzeren Ergebnisses". Gemessen ist ihr Fehlen kein Fehler, sobald der Name
praefixiert weiterlebt: `REFUSED_RANGES_LEGACY` allein liefert `ranges=[{0 0}] err=<nil>`. Dieselbe
Behauptung steht in der `coverage`-Metadatenzeile D1 von `02-26-SUMMARY.md` ("scheitert, wenn sie
fehlt, umbenannt oder verschoben ist"), und zwei Zeilen unter `## Next Phase Readiness` (`:316-317`)
melden G-02-25 als geschlossen, obwohl Runde 5 ihn unter `gaps_remaining` als verengt fuehrt. Der
Unterschied zu einer blossen Ungenauigkeit ist mechanisch: `workflow.windows_enforce` liest genau
dieses Register beim Ship, und ein `fixed`-Eintrag ist dort unsichtbar.

Der Ledger fuehrt sonst vorbildlich Buch — 66 amendiert 58 ausdruecklich, 67 zeichnet den
geschlossenen Defekt mit seinen Commits auf, 68 sagt ausdruecklich, dass eine Pruefung nichts
gefunden hat, und alle drei Repraesentationen stimmen ueberein (54 open / 0 waived / 15 fixed /
69 total, unabhaengig dreifach geparst). Genau deshalb faellt der eine Eintrag auf, der mehr
behauptet, als gemessen wurde.

**Der Verweis.** `internal/httpapi/handlers/schematics.go:457-459` verweist weiterhin auf
`refreshTheStoredVerdict` und nennt dabei zwei Bedingungen — 55 Zeilen ueber einer Funktion, die seit
`d4ee1f5` drei prueft (`fresh.ProbedAt.IsZero()`, `stored.Arch != fresh.Arch`,
`stored.TalosVersion != fresh.TalosVersion`). Ein Leser, der dem Verweis folgt, faengt mit einer
falschen Erwartung an. Die Wahrheit aus Plan 02-25 ist allquantifiziert — "jede Aussage im Baum" —,
und Runde 5 hat diese Stelle gefunden, weil der Fix von Runde 4 drei Stellen namentlich aufgezaehlt
und die vierte nicht gesehen hat. Deshalb ersetzt dieser Plan die Aufzaehlung durch einen
mechanischen Sweep, dessen Ergebnis ausgezaehlt in der SUMMARY steht.

Output: ein neuer, offen bleibender Ledger-Eintrag, der Eintrag 69 ueberholt statt ihn zu bearbeiten;
zwei WITHDRAWN-Korrekturen und eine berichtigte `coverage`-Zeile in `02-26-SUMMARY.md` in der Form,
die diese Phase bereits etabliert hat; ein Wort in einem Kommentar; und zwei Sweeps, die die
allquantifizierte Aussage kuenftig mechanisch pruefbar machen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-VERIFICATION.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-27-PLAN.md
@.planning/WINDOWS.md
</context>

## Recorded decisions

**Der spezifikationslose Probe-Fallback ist in diesem Lauf AKTIV** (`EDGE_ABSENT=1`,
`PROHIB_ABSENT=1`; diese Phase hat keine SPEC.md). Die deterministische Edge-Probe lief ueber die 14
Requirement-Ids der Phase und lieferte
`{"applicable":14,"resolved":0,"unresolved":14,"byVerification":{"explicit":0,"backstop":0}}` — jede
Zeile `unresolved`, dreizehn davon `category: unclassified`. Nach den `--auto`-Regeln darf eine
`unclassified`-Zeile weder auf `explicit` noch auf `backstop` promoviert werden; sie bleibt
`unresolved` und wird als markierte Annahme sichtbar gemacht. Der Abschnitt
`## Flagged assumptions — spec-less edge probe` fuehrt alle 14 Zeilen auf. Keine Zeile wurde still
fallengelassen und keine stillschweigend aufgeloest.

**Die `must_haves.truths` dieses Plans stammen nicht aus der Probe**, sondern aus dem Luecken-Text von
`02-VERIFICATION.md` Runde 5, `gaps[1]` und `gaps[2]`, deren `missing:`-Listen die Korrekturen bereits
gemessen benennen.

**Die `must_haves.prohibitions` sind descriptor-los** (kein `check_kind`, `check_target`,
`check_rule`, `check_violation_fixture`, `check_clean_fixture`) und disponieren damit als
flagged-unverified. Kein Kandidat wurde automatisch verworfen. Ein Kandidat der Recall-Stufe wurde
als Kanon-Verweis fallengelassen und hier als Brotkrume vermerkt statt gemuenzt: das Schreiben in
`.planning/`-Pfade aus einem Werkzeugaufruf heraus (Pfad-Traversal, OWASP-Kanon; `gsd-tools windows`
schreibt genau eine feste Datei).

**Dieser Plan besitzt `.planning/WINDOWS.md` fuer diese Runde.** Plan 02-27 sagt es in seinen eigenen
Success Criteria ausdruecklich und schreibt den Ledger nicht. Zwei Plaene, die in derselben Runde
daran anhaengen, bringen die drei Repraesentationen auseinander.

**Warum dieser Plan in Welle 2 liegt.** Der neue Ledger-Eintrag ist erst formulierbar, wenn feststeht,
was Runde 6 am Waechter tatsaechlich geaendert hat; er zitiert die Messungen aus `02-27-SUMMARY.md`.
Ausserdem benennt Plan 02-27 `web/src/routes/images.tsx` fuer seinen Lebendbeleg voruebergehend um —
ein paralleler Lauf, der `go test ./...` ueber den ganzen Baum faehrt, saehe dabei ein rotes
`internal/imagefactory`, das nichts mit ihm zu tun hat. Die Abhaengigkeit ist damit sowohl inhaltlich
als auch zustandsbedingt echt.

**Warum der neue Eintrag `open` bleibt, obwohl Plan 02-27 in derselben Runde vier Korrekturen
liefert.** Diese Phase hat einen Praezedenzfall fuer beides. Eintrag 67 wurde bei der Anlage `fixed`
markiert, weil der Defekt eine benannte Reproduktion hatte, die geschlossen und vom Verifier
nachgemessen wurde. Eintrag 69 wurde bei der Anlage `fixed` markiert fuer eine allquantifizierte
Eigenschaft ueber kuenftige Umbenennungen — und genau das war der Fehler, weil eine solche
Eigenschaft nicht durch Aufzaehlung geschlossen wird. Der neue Eintrag traegt dieselbe
allquantifizierte Eigenschaft, also bekommt er dieselbe Behandlung wie jede andere offene Aussage
dieser Phase: er bleibt `open`, sein Text nennt die Schliessbedingung, und eine Verifikationsrunde
stellt sie fest. Plan 02-27 haelt in seinen eigenen Success Criteria fest, dass er G-02-25 nicht als
geschlossen meldet; dieser Plan widerspricht dem nicht.

## API coverage

`.planning/phases/02-transport-seam-talossim-image-factory/COVERAGE.md` traegt die Matrix dieser Phase
und ist von diesem Plan unberuehrt. Der Detektor lief in diesem Lauf ueber den Phasen-Scope und
lieferte `{"detected":false}` (kein `skipped`) — eine in `COVERAGE.md` bereits dokumentierte
Vokabular-Luecke, weil ROADMAP und CONTEXT deutsch sind. Dieser Plan aendert ein Wort in einem
Kommentar und zwei Planungsdokumente; er spricht mit keinem externen Dienst, fuegt keinen Aufruf
hinzu und entfernt keinen.

<assumption_delta_decision>
Der Detektor (`assumption-delta scan 02`) meldet `detected: true` mit drei
`pluralization`-Signalen — `second` aus dem Szenarionamen `second_bootstrap_returns_AlreadyExists`,
`fallback` aus dem Warncode `installer.repo-fallback-unverified`, `also` als deutsches Adverb im
ROADMAP-Fliesstext. Alle drei sind Fehlalarme; keiner beschreibt einen singular→plural-,
required→optional- oder derived→chosen-Uebergang.

- **Primaeres Substantiv:** unveraendert `refreshTheStoredVerdict` als die eine Stelle, an der die
  ablehnenden Bedingungen des 409-Refresh stehen. Dieser Plan fuegt keine vierte Bedingung hinzu und
  keine zweite Stelle; er sorgt dafuer, dass die Verweise auf diese eine Stelle ihre Zahl richtig
  nennen.
- **Entscheidung:** `no-change`.
- Ein Begleit-Invariantentest ist hier ausdruecklich Teil des Plans und nicht bloss vorgeschlagen:
  der Sweep in Task 1 geht rot, sobald irgendwo im Baum wieder eine Zeile entsteht, die auf
  `refreshTheStoredVerdict` verweist und dabei eine andere Zahl nennt.
</assumption_delta_decision>

## Flagged assumptions — spec-less edge probe

Die Edge-Probe hat 14 Zeilen erzeugt, alle `unresolved`. Nach §C gilt: (Zahl der von der Probe
aufgeworfenen Punkte) == (Zahl der in `must_haves` uebernommenen + Zahl der hier als markierte
Annahme sichtbar gemachten). 14 == 0 + 14. **Keine stille Auslassung.**

| requirement | category | status | Disposition in diesem Plan |
|---|---|---|---|
| FACT-02 | unclassified | unresolved | **live** — die drei ablehnenden Bedingungen des 409-Refresh gehoeren zur Probe-Haelfte von FACT-02. Dieser Plan korrigiert den letzten Verweis, der ihre Zahl falsch nennt; er loest die Requirement-Zeile nicht auf |
| FACT-06 | unclassified | unresolved | **live** — die `coverage`-Zeile D1 von `02-26-SUMMARY.md` haengt an FACT-06 und behauptet mehr, als gemessen ist. Dieser Plan korrigiert die Aussage; er loest die Requirement-Zeile nicht auf |
| FACT-01 | unclassified | unresolved | nicht in diesem Plan — von Plan **02-27** dieser Runde beruehrt (der Browser-Ablehnungs-Waechter) |
| FOUND-12 | unclassified | unresolved | von den ausgefuehrten Plaenen 02-01…02-26 abgedeckt; von dieser Runde unberuehrt |
| TRANS-01 | concurrency | unresolved | von 02-01/02-08 abgedeckt; von dieser Runde unberuehrt. Nicht auf `backstop` promoviert, weil dieser Plan die Transport-Naht nicht anfasst und ein Backstop-Marker hier eine Zusicherung waere, die kein Task einloest |
| TRANS-02 | unclassified | unresolved | von 02-01 abgedeckt; von dieser Runde unberuehrt |
| TRANS-03 | unclassified | unresolved | von 02-01 abgedeckt; von dieser Runde unberuehrt |
| TRANS-04 | unclassified | unresolved | von 02-01/02-22 abgedeckt; von dieser Runde unberuehrt |
| TRANS-05 | unclassified | unresolved | Transport-Haelfte belegt; UI-Haelfte in `02-VERIFICATION.md` `deferred` nach Phase 3 |
| TRANS-06 | unclassified | unresolved | REQUIREMENTS.md fuehrt TRANS-06 als `Complete`; von dieser Runde unberuehrt |
| TRANS-07 | unclassified | unresolved | von 02-03 abgedeckt; von dieser Runde unberuehrt |
| FACT-03 | unclassified | unresolved | von 02-04/02-23 abgedeckt; von dieser Runde unberuehrt |
| FACT-04 | unclassified | unresolved | von 02-06 abgedeckt; von dieser Runde unberuehrt |
| FACT-05 | unclassified | unresolved | von 02-04 abgedeckt; von dieser Runde unberuehrt |

<tasks>

<!-- planner-discipline-allow: \btwo\b -->
<!-- planner-discipline-allow: umbenannt oder verschoben -->

<task type="tracer">
  <name>Task 1: Der letzte Verweis nennt die richtige Zahl — und ein Sweep ersetzt die Aufzaehlung, die eine Stelle uebersehen hat</name>
  <reversibility rating="reversible">Ein Wort in einem Kommentar. Kein Verhalten, keine Signatur, kein Datenformat; der Diff ist eine Zeile und laesst sich durch dieselbe Ersetzung rueckwaerts aufheben.</reversibility>
  <read_first>
    - `internal/httpapi/handlers/schematics.go:437-462` — der 409-Zweig vollstaendig: das Argument,
      warum ein zweiter POST das Verdikt nicht mitbringt, die Aufzaehlung der geschriebenen Felder
      (`Usable`, `ProbedAt`, `ProbeReason`), der G-02-9-Beleg mit `HTTP 409 in 4.186898875s`, und der
      Doc-Verweis, der ueber zwei Zeilen (`:458-459`) laeuft und mit dem Zahlwort am Zeilenende
      bricht. **Zeile 458 ist die einzige Zeile, die dieser Task aendert.**
    - `internal/httpapi/handlers/schematics.go:495-590` — `refreshTheStoredVerdict` vollstaendig, vor
      allem die drei mit "The first/second/third condition" ueberschriebenen Bloecke:
      `fresh.ProbedAt.IsZero()` (`:525`), `stored.Arch != fresh.Arch` (`:552`) und
      `stored.TalosVersion != fresh.TalosVersion` (`:580`). Das ist die Messung, gegen die der Verweis
      falsch ist.
    - `internal/httpapi/handlers/schematics.go:626-634` — `versionMismatchReason` mit dem Doc-Satz
      "so an operator who meets the declined refresh can tell it from the other two declining
      conditions". Diese Stelle ist **richtig** und wird **nicht** geaendert: sie spricht aus einer
      der drei Bedingungen heraus ueber die beiden anderen. Sie muss der Sweep finden und der
      ausfuehrende Agent muss sie beurteilen statt sie mitzuaendern.
    - `internal/model/model.go:100-140` — die von Plan 02-25 nachgezogenen Stellen bei `ID` und bei
      `TalosVersion`, insbesondere `:139` "und Arch's below for the one way the two conditions
      differ". Auch diese Stelle ist richtig: sie vergleicht die Versions- mit der
      Architektur-Bedingung und zaehlt nicht die Bedingungen des Refresh. Ebenfalls nicht aendern.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-VERIFICATION.md` `gaps[2]`
      vollstaendig — der Befund in den Worten des Verifiers, einschliesslich der Beobachtung, dass die
      Wahrheit allquantifiziert ist und die drei namentlich aufgezaehlten Stellen aus Plan 02-25
      nachgezogen wurden.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-25-PLAN.md` — die Task-3-Passage
      mit der Aufzaehlung der drei Stellen. Sie ist die Ursache des Rests: eine Aufzaehlung kann eine
      vierte Stelle nicht finden, ein Sweep schon.
  </read_first>
  <files>internal/httpapi/handlers/schematics.go</files>
  <action>
Zuerst den Sweep fahren und sein heutiges Ergebnis festhalten, dann das Wort aendern.

**Sweep A — die praezise Messung des Defekts.** Alle Zeilen im Baum, die auf `refreshTheStoredVerdict`
verweisen, und davon die, die zugleich das englische Zahlwort fuer 2 als eigenstaendiges Wort tragen:

    grep -rn 'refreshTheStoredVerdict' --include='*.go' --include='*.md' --include='*.ts' --include='*.tsx' internal docs web cmd | grep -c '\btwo\b'

Beim Planen gemessen: **1**. Es gibt insgesamt vier Verweise auf den Bezeichner, alle in
`internal/httpapi/handlers/schematics.go` (`:458` der Doc-Verweis, `:461` der Aufruf, `:495` der
Doc-Kommentar der Funktion, `:513` ihre Signatur), und genau einer davon nennt die falsche Zahl.
Diese Eins ist die Rot-Messung dieses Tasks; sie gehoert in die SUMMARY.

**Sweep B — die Vollstaendigkeitsaufnahme.** Alle Zeilen, in denen von `refresh` oder `409` die Rede
ist und zugleich ein Zahlwort fuer 2 vorkommt:

    grep -rniE '(refresh|409)' --include='*.go' --include='*.md' --include='*.ts' --include='*.tsx' internal docs web cmd | grep -icE '\b(two|zwei|both)\b'

Beim Planen gemessen: **5**. Die fuenf sind namentlich bekannt und im Voraus beurteilt — der
ausfuehrende Agent prueft jede einzeln nach und schreibt seine Beurteilung in die SUMMARY, statt die
hier stehende abzuschreiben:

  - `internal/httpapi/handlers/schematics.go:458` — **falsch**, das ist der Gap. Wird geaendert.
  - `internal/httpapi/handlers/schematics.go:628` — **richtig**. `versionMismatchReason` spricht aus
    einer der drei Bedingungen heraus ueber die beiden anderen. Bleibt.
  - `internal/httpapi/handlers/schematics_test.go:3143`, `:3178`, `:3244` — **richtig**. Sie sagen
    "both architectures" bzw. "both Talos versions" ueber je zwei benannte Werte in einer
    Fehlermeldung, nicht ueber eine Zahl von Bedingungen. Bleiben.

Nach der Aenderung ist Sweep A **0** und Sweep B **4**.

**Beide Sweeps zuerst neu messen, nicht die Zahlen von hier uebernehmen.** Die 1 und die 5 sind beim
Planen gemessen worden, also **vor** Plan 02-27. Dieser Plan laeuft in Welle 2, und 02-27 hat
`internal/imagefactory/guard_drift_test.go` angefasst. Sweep B ist absichtlich breit — er trifft jede
Zeile, in der `refresh` oder `409` und zugleich ein Zahlwort fuer 2 vorkommt —, und eine neue
Kommentar- oder Fehlermeldungszeile aus 02-27 koennte ihn treffen. Das ist unwahrscheinlich (02-27
arbeitet an "Refusal", nicht an "refresh"), aber es ist nicht ausgeschlossen. Also: Sweep B unmittelbar
nach dem Vorliegen von 02-27 erneut fahren und den Ausgangswert festhalten, bevor irgendetwas geaendert
wird.

Liegt er dann ueber 5, ist die zusaetzliche Fundstelle **einzeln zu beurteilen und in der SUMMARY zu
benennen** — sie ist entweder richtig und bleibt, oder sie ist eine neue Zwei-Bedingungen-Aussage und
gehoert korrigiert. Die erwartete Zahl in der Abnahme darf dafuer **nicht** angehoben werden. Eine
Abnahme, die man an den Befund anpasst, misst den Befund nicht mehr; genau so ist die vierte Stelle in
Runde 5 entstanden. Ergibt die Beurteilung, dass eine legitime neue Fundstelle die 4 unerreichbar macht,
haelt dieser Task an und meldet das, statt die Zahl zu bewegen.

**Die Aenderung.** In `internal/httpapi/handlers/schematics.go` endet Zeile 458 auf das englische
Zahlwort fuer 2; es wird durch `three` ersetzt. Der ueber `:458-459` verteilte Satz lautet danach —
mit entfernten Kommentarpraefixen und zusammengefuegten Zeilen gelesen —
`see refreshTheStoredVerdict for the three conditions and for why a failed refresh is silent`.
Eine Neuumbruch der beiden Kommentarzeilen ist erlaubt, solange dieser zusammengefuegte Satz woertlich
so herauskommt; die Abnahme prueft ihn zeilenunabhaengig. Sonst wird in dieser Datei nichts geaendert:
kein Verhalten, keine Signatur, keine Bedingung.

**Warum ein Sweep und keine Aufzaehlung.** Schreib den Grund in die SUMMARY, weil er der eigentliche
Ertrag dieses Tasks ist: Plan 02-25 hat drei Stellen namentlich genannt und nachgezogen, und die
vierte lag im selben File wie die geaenderte Funktion. Eine allquantifizierte Aussage ist nur so gut
wie ihre Suche. Die beiden Sweeps sind ab jetzt die Suche, sie stehen als Kommandos in der SUMMARY,
und Sweep A ist als Abnahmebedingung dieses Plans festgenagelt.
  </action>
  <acceptance_criteria>
    - `sed -n '450,465p' internal/httpapi/handlers/schematics.go | sed 's|^[[:space:]]*//[[:space:]]*||' | tr '\n' ' '` enthaelt woertlich `see refreshTheStoredVerdict for the three conditions and for why a failed refresh is silent`
    - Sweep A meldet 0. Die SUMMARY nennt den gemessenen Wert **vor** der Aenderung (1) und danach (0), mit dem Kommando
    - Sweep B meldet 4. Die SUMMARY nennt den beim Planen gemessenen Wert (5), den **nach dem Vorliegen von 02-27 neu gemessenen** Ausgangswert und den Wert danach (4), und listet alle vier verbleibenden Fundstellen mit Datei, Zeile und der Begruendung, warum jede richtig ist. Weicht der neu gemessene Ausgangswert von 5 ab, benennt die SUMMARY die zusaetzliche Fundstelle einzeln und begruendet ihre Beurteilung; die erwartete Zahl 4 wurde in keinem Fall angehoben
    - `internal/httpapi/handlers/schematics.go:628` (`versionMismatchReason`) ist unveraendert; `internal/model/model.go:139` ist unveraendert
    - `git diff --stat -- internal/httpapi/handlers/schematics.go` zeigt genau eine geaenderte Datei, und der Diff beruehrt ausschliesslich Kommentarzeilen
    - `go build ./... && go vet ./...` endet mit Exit 0, `gofmt -l ./internal` gibt nichts aus
    - `go test ./internal/httpapi/handlers -count=1` endet mit Exit 0
  </acceptance_criteria>
  <verify>
    <automated>sed -n '450,465p' internal/httpapi/handlers/schematics.go | sed 's|^[[:space:]]*//[[:space:]]*||' | tr '\n' ' ' | grep -q 'see refreshTheStoredVerdict for the three conditions and for why a failed refresh is silent'</automated>
    <fails_when>Exit 1 — der zusammengefuegte Satz im Bereich 450-465 lautet nicht so. Entweder steht dort weiterhin das alte Zahlwort, oder der Kommentar wurde so umgebrochen bzw. umformuliert, dass der Satz nicht mehr woertlich entsteht. Die Pipeline entfernt Kommentarpraefixe und fuegt die Zeilen zusammen, damit ein reiner Neuumbruch nicht faelschlich als Fehlschlag zaehlt — bleibt sie trotzdem stumm, ist der Satz wirklich anders.</fails_when>
    <automated>grep -rn 'refreshTheStoredVerdict' --include='*.go' --include='*.md' --include='*.ts' --include='*.tsx' internal docs web cmd | grep -c '\btwo\b' | grep -qx '0'</automated>
    <fails_when>Eine andere Zahl als 0 auf stdout, also Exit 1 der letzten Stufe. Beim Planen war der Wert 1 — diese Pruefung ist heute rot und wird durch die Aenderung gruen; sie kann also nicht stumm bestehen. Ein Wert ab 1 bedeutet, dass irgendeine Zeile im Baum weiterhin auf `refreshTheStoredVerdict` verweist und dabei die falsche Zahl nennt, gleich in welcher Datei.</fails_when>
    <automated>grep -rniE '(refresh|409)' --include='*.go' --include='*.md' --include='*.ts' --include='*.tsx' internal docs web cmd | grep -icE '\b(two|zwei|both)\b' | grep -qx '4'</automated>
    <fails_when>Eine andere Zahl als 4, also Exit 1. 5 heisst, die Aenderung ist nicht gelandet. Weniger als 4 heisst, eine der vier korrekten Aussagen wurde mitgeaendert — `versionMismatchReason` oder eine der drei Fehlermeldungen in `schematics_test.go` —, was ein anderer Defekt waere als der behobene. Mehr als 5 heisst, in dieser Runde ist eine neue Zaehl-Aussage entstanden, die niemand beurteilt hat.</fails_when>
    <automated>go build ./... && go vet ./... && go test ./internal/httpapi/handlers -count=1</automated>
    <fails_when>Exit ungleich 0: ein Uebersetzungsfehler, ein `vet`-Befund, oder eine `--- FAIL:`-Zeile aus dem Handler-Paket. Da der Diff ausschliesslich Kommentarzeilen beruehrt, waere jede dieser Ausgaben der Beleg, dass mehr als der Kommentar angefasst wurde.</fails_when>
  </verify>
  <done>Kein Verweis auf `refreshTheStoredVerdict` nennt mehr eine andere Zahl als drei, die vier korrekten Aussagen im Baum sind einzeln beurteilt und benannt, und der Sweep ist ab jetzt das Kommando, das die allquantifizierte Aussage prueft.</done>
</task>

<task type="auto">
  <name>Task 2: Der Ledger ueberholt Eintrag 69, statt ihn zu bearbeiten — und bleibt offen</name>
  <reversibility rating="costly">Ein angelegter Ledger-Eintrag laesst sich nicht zurueckziehen: `gsd-tools windows` kennt `append`, `waive` und `fixed` und kein Loesch- oder Aenderungs-Verb. Eine Korrektur ist nur als weiterer, ueberholender Eintrag moeglich — genau die Mechanik, die dieser Task benutzt. Deshalb muss der Wortlaut vor dem `append` stimmen; danach ist er Teil der Aufzeichnung. Kein `one-way`: der Eintrag ist ueberholbar und markierbar, es entsteht kein irreversibler Zustand ausserhalb der Datei.</reversibility>
  <precondition>`.planning/phases/02-transport-seam-talossim-image-factory/02-27-SUMMARY.md` existiert und nennt die vier Korrekturen aus Plan 02-27 mit ihren gemessenen Rot-Ausgaben. Pruefen mit `test -f` und `grep -c 'prefixed' <pfad>`; fehlt die Datei, hat Plan 02-27 nicht abgeschlossen und dieser Task haelt an, statt sich den Inhalt des Eintrags auszudenken — ein Ledger-Eintrag ueber eine nicht gemessene Aenderung waere derselbe Defekt wie der, den er korrigiert.</precondition>
  <read_first>
    - `.planning/WINDOWS.md` Eintrag 69 vollstaendig — sein `fixed`-Status, sein Text, und
      insbesondere der Satz "Plan 02-26 hat es geschlossen: `refusedRangesDecl` schneidet die
      Deklaration zuerst heraus ..., ihr Fehlen ist ein Fehler statt eines kuerzeren Ergebnisses".
      Genau dieser Satz wird ueberholt; alles andere in 69 bleibt richtig und wird im neuen Eintrag
      ausdruecklich als weiterhin gueltig benannt.
    - `.planning/WINDOWS.md` Eintraege 51, 52, 63 und 66 — die Form des ueberholenden Eintrags, auf
      die sich diese Phase festgelegt hat: Eroeffnung in Grossbuchstaben mit dem, was ueberholt wird,
      die ausdrueckliche Klausel, dass es kein Aenderungs-Verb gibt, und bei 66 die Zweiteilung "was
      weiterhin gilt" / "was ueberholt ist".
    - `.planning/WINDOWS.md` Eintraege 56 und 58 vollstaendig — beide bleiben unberuehrt offen. 56
      traegt die nicht erschoepfend gemessene SERVER-Menge, 58 traegt G-02-9. Der neue Eintrag nennt
      56 als Nachbarn mit anderer Aussage und ueberholt ihn nicht.
    - `.planning/WINDOWS.md` Frontmatter — die vier Zaehler, gegen die die drei Repraesentationen
      geprueft werden. Beim Planen standen dort `open_count: 54`, `waived_count: 0`,
      `fixed_count: 15`, `total_count: 69`, und die Runde-5-Verifikation hat alle drei
      Repraesentationen unabhaengig geparst und uebereinstimmend gefunden, ids 1..69 lueckenlos und
      doppelfrei. Das ist der Ausgangszustand.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-27-SUMMARY.md` vollstaendig — die
      vier Korrekturen mit ihren gemessenen Rot-Ausgaben, die beiden Anzahlen (Rumpf/Datei) mit dem
      neuen Anker und der Lebendbeleg mit leerem `git diff`. Der neue Eintrag zitiert daraus, statt
      zu paraphrasieren.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-VERIFICATION.md` `gaps[0]` und
      `gaps[1]` vollstaendig — die gemessenen Werte `ranges=[{0 0}] err=<nil>` fuer
      `REFUSED_RANGES_LEGACY`, `ranges=[{0 1}] err=<nil>` fuer `REFUSED_RANGESX`, die vier
      Bound-Faelle, und die Begruendung, warum ein `fixed`-Eintrag beim Ship unsichtbar ist.
  </read_first>
  <files>.planning/WINDOWS.md</files>
  <action>
Den Ledger fuehren, ausschliesslich mit `gsd-tools windows append`, `gsd-tools windows fixed` und
`gsd-tools windows status`. Kein Handanlegen an der Markdown-Tabelle, am JSON-Block oder an den
Frontmatter-Zaehlern. Die Aufrufform ist
`windows append --kind <kind> --phase 02 --file <pfad> --description "<text>"`, optional mit
`--line <n>`; `windows fixed <id>` markiert einen bestehenden Eintrag. Lies die vergebene id jeweils
aus der Ausgabe von `append` und verwende genau diese, statt eine zu raten. Genau **zwei** Aufrufe von
`append` und genau **einer** von `fixed`; die Abnahme nagelt die resultierenden Zaehler fest.

Kein Beschreibungstext darf ein Pipe-Zeichen **oder einen echten Zeilenumbruch** enthalten. Beide
brechen dieselbe Sache: die Markdown-Repraesentation ist pipe-getrennt und zeilenweise, und die
Konsistenzpruefung unten liest den Status positionsbasiert als achtes Feld der Zeile. `renderTable`s
`cell()` maskiert `\` und `|`, aber keine Zeilenumbrueche — ein `\n` im Text erzeugt also eine Zeile,
die physisch ueber mehrere Dateizeilen laeuft, und das achte Feld der ersten davon ist nicht mehr der
Status. Eintrag A hat vier Teile; schreib sie als einen durchgehenden Absatz mit Satzzeichen, so wie es
die Eintraege 66 bis 69 tun, und uebergib `--description` als **ein** Argument in einfachen
Anfuehrungszeichen ohne eingebettete Umbrueche.

**Eintrag A — der ueberholende Eintrag zu 69.** `--kind unmet-truth`, `--file
internal/imagefactory/guard_drift_test.go`, `--line 83`. Sein Text traegt zuerst die
Provenienz-Klammer, mit der die Eintraege 51, 52, 63 und 66 dieser Phase eroeffnen — hier
`[from 02-28]` —, und unmittelbar danach in Grossbuchstaben
`SUPERSEDES ENTRY 69, WHICH STAYS FIXED (no amend verb exists)`. Die Klammer gehoert zur Form:
`gsd-tools windows append` reicht die Beschreibung unveraendert durch und stellt nichts voran,
also ist sie das Einzige, was den Eintrag seinem Plan zuordnet. Die maschinelle Pruefung sucht
die Grossbuchstaben-Formel deshalb als Teilzeichenkette und nicht am Zeichen 0. Danach sagt der
Eintrag in dieser Reihenfolge:

  - **was ueberholt ist:** 69 sagt "Plan 02-26 hat es geschlossen ... ihr Fehlen ist ein Fehler statt
    eines kuerzeren Ergebnisses" und ist bei der Anlage `fixed`. Runde 5 der Verifikation hat
    gemessen, dass das fuer eine praefixierende Umbenennung nicht gilt: `REFUSED_RANGES_LEGACY` allein
    liefert `ranges=[{0 0}] err=<nil>`, `REFUSED_RANGESX` liefert `ranges=[{0 1}] err=<nil>`, und am
    echten Baum blieben mit `REFUSED_RANGES_LEGACY` sowohl
    `TestBrowserRefusalSetEqualsTheServers` als auch
    `TestBrowserRefusalGuardRefusesToPassWithoutItsDeclaration` mit allen vier Untertests gruen. Dazu
    die zweite Haelfte: die gelesenen Bounds waren nicht validiert, `{0xFFFFFFFF,0xFFFFFFFF}` fiel auf
    `{-1 -1}`, `{0x10FFFF,0xFFFFFFFF}` auf `{1114111 -1}`, `{0x001f,0x0000}` auf `{31 0}` — jeder
    dieser Eintraege deckt keinen Codepoint ab und wurde still uebergangen.
  - **was an 69 weiterhin gilt:** die Beschreibung des unverankerten Zustands zwischen Plan 02-20 und
    Runde 5, die Messung `ok ... 0.546s`, und die Feststellung, dass nichts maskiert war — die beiden
    Ablehnungsmengen stimmen ueberein und das Loeschen des `U+FEFF`-Eintrags laesst den Waechter
    scheitern. Diese Saetze bleiben richtig und werden nicht ueberholt.
  - **was Runde 6 getan hat:** die vier Korrekturen aus Plan 02-27, jede mit ihrer gemessenen
    Rot-Ausgabe aus `02-27-SUMMARY.md` — der auf `:` oder `=` gebundene Anker, die
    Bound-Validierung gegen `utf8.MaxRune` und `from <= to`, die getrennte Abschneide-Diagnose, und
    die drei Tabellen mit 6, 3 und 2 Zeilen.
  - **warum dieser Eintrag `open` bleibt und was ihn schliesst:** die Eigenschaft ist
    allquantifiziert ueber kuenftige Umbenennungen und kuenftige Eintragsformen; diese Phase hat
    daran in drei aufeinanderfolgenden Runden je ein engeres Loch gemessen. Ein bei der Anlage
    geschlossener Eintrag ueber eine solche Eigenschaft war genau der Fehler von 69, und er ist
    mechanisch und nicht bloss stilistisch: `workflow.windows_enforce` blockiert `/gsd-ship`, solange
    `open_count > 0`, und ein `fixed`-Eintrag ist dort unsichtbar. Die Schliessbedingung steht im
    Text und ist pruefbar: eine Verifikationsrunde falsifiziert den Waechter selbst — sie benennt
    `REFUSED_RANGES` praefixiert um und sieht `TestBrowserRefusalSetEqualsTheServers` rot, und sie
    setzt einen Eintrag mit einer Grenze oberhalb `utf8.MaxRune` und sieht den Waechter rot — und
    erst dann wird der Eintrag mit `windows fixed` markiert.
  - **die Nachbarschaft:** Eintrag 56 bleibt daneben offen und traegt eine andere Aussage (die
    SERVER-Menge ist nicht erschoepfend gemessen und erbt 02-14s Extrapolation); die Eintraege 58 und
    66 tragen G-02-9 und bleiben unberuehrt offen. **G-02-9 ist offen.**

Dieser Eintrag wird **nicht** mit `windows fixed` markiert.

**Eintrag B — die vierte, uebersehene Zwei-Bedingungen-Aussage.** `--kind deviation`, `--file
internal/httpapi/handlers/schematics.go`, `--line 458`. Er sagt:

  - was der Zustand war: der Doc-Verweis bei `:458` nannte zwei Bedingungen, 55 Zeilen ueber einer
    Funktion, die seit `d4ee1f5` drei prueft. Gefunden von Runde 5 der Verifikation
    (`02-VERIFICATION.md`, `gaps[2]`);
  - warum er entstanden ist: die Wahrheit aus Plan 02-25 ist allquantifiziert, aber ihr Task hat drei
    Stellen namentlich aufgezaehlt (`internal/model/model.go` bei `ID` und bei `TalosVersion`,
    `docs/api-contract.md:620-627`) und nachgezogen. Eine Aufzaehlung findet keine vierte Stelle;
  - was Runde 6 getan hat: das Zahlwort korrigiert und die Aufzaehlung durch zwei Sweeps ersetzt, mit
    den Kommandos und den gemessenen Werten vor und nach der Aenderung (Sweep A: 1 -> 0, Sweep B:
    5 -> 4) sowie der namentlichen Beurteilung der vier verbleibenden, korrekten Fundstellen;
  - dass Eintrag 66 daneben stehen bleibt: er amendiert 58 um die dritte ablehnende Bedingung und ist
    davon unberuehrt.

Dieser Eintrag ist die Aufzeichnung eines in derselben Runde und mechanisch belegbar geschlossenen
Defekts — der Sweep ist eine endliche, wiederholbare Messung ueber den ganzen Baum, keine
Behauptung ueber kuenftiges Verhalten. Also nach dem `append` mit `windows fixed <id>` markieren, in
der Form, die Eintrag 63 etabliert hat, und der Grund dafuer steht im Text des Eintrags, weil das
`fixed`-Verb keinen traegt. Der Text nennt zugleich die Grenze seiner eigenen Messung: die Sweeps
decken `internal`, `docs`, `web` und `cmd` ueber `.go`, `.md`, `.ts` und `.tsx` ab und nichts
darueber hinaus.

**Zuletzt die Uebereinstimmung der drei Repraesentationen unabhaengig pruefen**, so wie es die
adversarische Pruefung der Runde 5 getan hat und nicht auf das Wort des Werkzeugs hin: die
Frontmatter-Zaehler lesen, die Zeilen der Markdown-Tabelle nach Status zaehlen, die Eintraege des
JSON-Blocks nach Status zaehlen, und die ids auf Lueckenlosigkeit und Doppelfreiheit pruefen. Die
Signatur, die dabei herauskommen muss, steht in der Abnahme. Die vier Zahlen vor der Runde
(`open 54 / waived 0 / fixed 15 / total 69`), die vier danach und die beiden vergebenen ids gehoeren
in die SUMMARY, damit die Differenz nachrechenbar ist — zusammen mit jedem abgesetzten Kommando.
  </action>
  <acceptance_criteria>
    - Genau zwei `windows append` und genau ein `windows fixed` wurden abgesetzt; die SUMMARY listet jeden Aufruf mit der zurueckgegebenen id
    - `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` endet mit Exit 0 und die Ausgabe enthaelt `"ok": true`
    - Die unabhaengige Dreifach-Zaehlung ergibt die Signatur `fm=55/0/16/71 md=55/16/71 js=55/16/71 ids=71/71` — Frontmatter `open/waived/fixed/total`, Markdown `open/fixed/rows`, JSON `open/fixed/entries`, ids `eindeutige/hoechste`
    - Ein Eintrag traegt die Teilzeichenkette `SUPERSEDES ENTRY 69, WHICH STAYS FIXED (no amend verb exists)` unmittelbar hinter seiner Provenienz-Klammer `[from 02-28]`, nennt `REFUSED_RANGES_LEGACY`, `ranges=[{0 0}] err=<nil>` und `utf8.MaxRune`, und ist `open`
    - Derselbe Eintrag benennt eine pruefbare Schliessbedingung (Falsifikation durch eine Verifikationsrunde in beiden Richtungen) und wird **nicht** als `fixed` markiert
    - Der zweite Eintrag nennt `schematics.go`, die Zeile 458, die beiden Sweep-Kommandos mit den Werten vor und nach der Aenderung, die Grenze seiner Messung — und ist `fixed`
    - Eintrag 69 ist unveraendert und weiterhin `fixed`; die Eintraege 56, 58 und 66 sind unveraendert und weiterhin `open`
    - Kein angelegter Eintrag sagt oder legt nahe, dass G-02-9 geschlossen ist
    - Kein angelegter Beschreibungstext enthaelt ein Pipe-Zeichen oder einen echten Zeilenumbruch; jede der 71 Markdown-Zeilen des Ledgers ist genau ein Eintrag. Nachweisbar daran, dass die Zahl der Zeilen, die auf `^| <ziffern> | ` passen, gleich der Zahl der JSON-Eintraege ist — beide 71, was die Signaturpruefung mitprueft
    - `.planning/WINDOWS.md` wurde ausschliesslich ueber `gsd-tools windows` geschrieben
  </acceptance_criteria>
  <verify>
    <automated>W=.planning/WINDOWS.md; J() { awk '/^````json/{f=1;next} /^````$/{f=0} f' "$W"; }; S=$(grep '^| [0-9]* | ' $W | awk -F'|' '{gsub(/ /,"",$8); print $8}'); echo "fm=$(grep -m1 '^open_count:' $W | awk '{print $2}')/$(grep -m1 '^waived_count:' $W | awk '{print $2}')/$(grep -m1 '^fixed_count:' $W | awk '{print $2}')/$(grep -m1 '^total_count:' $W | awk '{print $2}') md=$(printf '%s\n' "$S" | grep -c '^open$')/$(printf '%s\n' "$S" | grep -c '^fixed$')/$(grep -c '^| [0-9]* | ' $W) js=$(J | grep -c '"status": "open"')/$(J | grep -c '"status": "fixed"')/$(J | grep -c '"id": [0-9]*') ids=$(J | grep -o '"id": [0-9]*' | awk '{print $2}' | sort -n | uniq | wc -l | tr -d ' ')/$(J | grep -o '"id": [0-9]*' | awk '{print $2}' | sort -n | tail -1)" | grep -qx 'fm=55/0/16/71 md=55/16/71 js=55/16/71 ids=71/71'</automated>
    <fails_when>Exit 1, weil die gedruckte Signaturzeile nicht woertlich `fm=55/0/16/71 md=55/16/71 js=55/16/71 ids=71/71` lautet. Diese Pruefung ist heute rot — beim Planen ergab dieselbe Zeile `fm=54/0/15/69 md=54/15/69 js=54/15/69 ids=69/69` —, sie kann also nicht stumm bestehen. Sie liest alle drei Repraesentationen getrennt und ohne das Werkzeug zu fragen: eine Abweichung in einem der zwoelf Felder heisst, Frontmatter, Tabelle und JSON-Block sagen nicht dasselbe, oder die Zahl der Eintraege stimmt nicht (zwei Appends erwartet), oder die ids sind lueckig bzw. doppelt. Weichen die beiden ersten `md`-Zahlen ab, waehrend `fm` und `js` stimmen und die dritte `md`-Zahl bei 71 bleibt, hat der Text eines angelegten Eintrags die positionsbasierte Lesung zerstoert: die Summe aus offenen und fixen Markdown-Zeilen ist dann kleiner als die Zahl der Zeilen. Zwei Ursachen fuehren dorthin, beide von derselben Prohibition verboten, und sie sind vor jeder Aenderung zu unterscheiden. Ein Pipe-Zeichen in der Beschreibung verschiebt den Status aus dem achten Feld heraus. Ein echter Zeilenumbruch, den `renderTable`s `cell()` nicht maskiert (#3689), laesst den Eintrag physisch ueber mehrere Dateizeilen laufen; an einem synthetischen Fragment nachgemessen heisst das, dass die Zeilenzahl korrekt bleibt und das achte Feld der ersten Zeile leer ist — der Zaehler verliert also genau einen Status, ohne dass eine Zeile fehlt. Der Unterschied ist am physischen Umfang des Tabellenblocks zu erkennen, und zwar so: `awk '/^\| id \|/{f=1} f{ if ($0 ~ /^$/) exit; n++ } END{print n+0}' .planning/WINDOWS.md` zaehlt die Zeilen von der Kopfzeile bis zur ersten Leerzeile. Gesund ist genau `Zeilen == Eintraege + 2` (Kopf- und Trennzeile), nach dieser Runde also 73 zu 71; beim Planen gemessen 71 zu 69. Ist die Differenz groesser als 2, war es ein Zeilenumbruch — am Fragment gemessen 6 zu 3 statt 5 zu 3. Ist sie genau 2, war es ein Pipe. Der JSON-Block bleibt in beiden Faellen korrekt, weil er die Beschreibung als JSON-String maskiert; dass `js` weiterhin auf 55/16/71 steht, ist deshalb kein Entwarnungssignal, sondern der Hinweis, dass nur die Markdown-Haelfte betroffen ist.</fails_when>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status | grep -q '"ok": true'</automated>
    <fails_when>Exit ungleich 0, oder in der Ausgabe fehlt die Zeile `"ok": true` — dann konnte das Werkzeug den Ledger nicht als konsistent bestaetigen. Diese Pruefung steht neben der unabhaengigen Zaehlung darueber und ersetzt sie nicht: sie faengt Formen der Inkonsistenz ab, die die Zaehlung nicht sieht, etwa einen unparsbaren JSON-Block.</fails_when>
    <automated>grep -c 'SUPERSEDES ENTRY 69, WHICH STAYS FIXED (no amend verb exists)' .planning/WINDOWS.md | grep -qx '2'</automated>
    <fails_when>Eine andere Zahl als 2, also Exit 1. Die Eroeffnungsformel muss genau zweimal vorkommen — einmal in der Markdown-Tabelle und einmal im JSON-Block, weil beide dieselbe Beschreibung tragen. 0 heisst, der ueberholende Eintrag wurde nie angelegt oder eroeffnet nicht in der Form, auf die sich diese Phase festgelegt hat. 1 heisst, nur eine der beiden Repraesentationen traegt ihn, der Ledger ist also auseinandergelaufen.</fails_when>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status --raw | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const e=JSON.parse(s).ledger.entries;const by=i=>e.find(x=>x.id===i);const keep=[56,58,66].every(i=>by(i)&&by(i).status==="open");const sixtynine=by(69)&&by(69).status==="fixed";const sup=e.find(x=>/SUPERSEDES ENTRY 69, WHICH STAYS FIXED/.test(x.description||""));process.exit(keep&&sixtynine&&sup&&sup.status==="open"?0:1)})'</automated>
    <fails_when>Exit 1 — mindestens eine der vier Bedingungen haelt nicht: einer der Eintraege 56, 58 oder 66 ist nicht mehr `open` (dann hat diese Runde eine fremde Aussage geschlossen, insbesondere G-02-9), Eintrag 69 ist nicht mehr `fixed` (dann wurde er bearbeitet statt ueberholt), keine Beschreibung enthaelt die Teilzeichenkette `SUPERSEDES ENTRY 69, WHICH STAYS FIXED`, oder der Eintrag, der sie enthaelt, ist nicht `open` — der Fall, in dem der Plan den Fehler von 69 ein zweites Mal begeht. Die Formel wird als Teilzeichenkette und nicht am Zeichen 0 gesucht, weil die Eintraege 51, 52, 63 und 66 dieser Phase mit einer Provenienz-Klammer `[from NN]` vor der Grossbuchstaben-Eroeffnung beginnen; ein Anker auf den Zeilenanfang wuerde einen formgerechten Eintrag ablehnen.</fails_when>
  </verify>
  <done>Der Ledger traegt den ueberholenden, offen bleibenden Eintrag zu 69 mit seiner pruefbaren Schliessbedingung und den Eintrag ueber die vierte Zwei-Bedingungen-Aussage; 56, 58, 66 und 69 sind unberuehrt, und die drei Repraesentationen stimmen unabhaengig nachgezaehlt ueberein.</done>
</task>

<task type="auto">
  <name>Task 3: `02-26-SUMMARY.md` sagt nur noch, was gemessen ist — durchgestrichen, WITHDRAWN, mit datiertem Korrekturblock</name>
  <reversibility rating="reversible">Eine Markdown-Datei unter Versionskontrolle; der Diff ist umkehrbar und erzeugt keinen Zustand ausserhalb der Datei.</reversibility>
  <precondition>Der ueberholende Ledger-Eintrag aus Task 2 existiert und seine id ist bekannt. Pruefen mit `grep -c 'SUPERSEDES ENTRY 69' .planning/WINDOWS.md` (erwartet 2, weil Tabelle und JSON-Block dieselbe Beschreibung tragen) und die id aus `windows status` lesen. Ohne sie kann der Korrekturblock nicht auf den Eintrag zeigen, und eine Korrektur, die nirgendwo hinzeigt, ist die naechste unbelegte Behauptung.</precondition>
  <read_first>
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-21-SUMMARY.md:340-362` — die in
      dieser Phase etablierte Korrekturform vollstaendig: die durchgestrichene Ueberschrift mit dem
      Zusatz "WITHDRAWN, this was false", der eingerueckte Korrekturblock mit Urheber und Datum, der
      Satz "The claim below is wrong and is retained only so the correction has something to point
      at", die Messung, die Begruendung warum es der Korrektur wert war, und der durchgestrichene
      Originaltext darunter. **Diese Form wird uebernommen, nicht neu erfunden.**
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md:1-117` — das
      Frontmatter vollstaendig, damit klar ist, wo es endet. Die `coverage`-Zeile D1 (`:57-71`) liegt
      **innerhalb** des YAML; eine Durchstreichung wuerde dort den Wert veraendern, aber der Block
      bleibt YAML und muss parsbar bleiben. D2 und D3 sind wahr und bleiben unberuehrt.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md:310-321` — der
      Abschnitt `## Next Phase Readiness`. `:316` sagt "Die Luecke **G-02-25** ... ist geschlossen",
      `:317` sagt "sind beide Luecken aus Verifikationsrunde 4 geschlossen". Die zweite Aussage ist
      halb wahr: G-02-24 ist geschlossen und von Runde 5 nachgemessen, G-02-25 nicht. Die drei
      folgenden Zeilen (G-02-9, Eintrag 56, naechste Threat-Id) sind wahr und bleiben woertlich.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-VERIFICATION.md`
      `re_verification.gaps_remaining` — der Satz "G-02-25 ... VERENGT, NICHT GESCHLOSSEN" mit seinem
      Verweis auf `gaps[0]` und `adversarial_checks_run[0]`. Das ist die Messung, auf die die
      Korrektur sich beruft.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-27-SUMMARY.md` — was Runde 6
      tatsaechlich geliefert hat, damit der Korrekturblock den Stand richtig beschreibt und nicht
      seinerseits zu weit greift.
  </read_first>
  <files>.planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md</files>
  <action>
Drei Stellen korrigieren und sonst nichts.

**Stelle 1 — die `coverage`-Zeile D1 im Frontmatter (`:57-71`).** Sie liegt in YAML; eine
Durchstreichung im Klartext daneben wuerde die Datei zerschiessen. Also zwei Schritte, beide
innerhalb gueltigen YAML:

  - Die `description` von D1 auf das verengen, was gemessen ist: der Waechter liest die
    Ablehnungsmenge ausschliesslich aus der `REFUSED_RANGES`-Deklaration und scheitert, wenn sie
    fehlt oder unter einem Namen steht, der `REFUSED_RANGES` **nicht** als Praefix traegt. Die
    Formulierung "umbenannt oder verschoben" faellt weg, weil sie fuer eine praefixierende
    Umbenennung gemessen falsch ist.
  - Einen Geschwisterschluessel `correction:` unter D1 anlegen (neben `description`, `requirement`,
    `verification`, `human_judgment`), als mehrzeiliger Block-Skalar, der traegt: das Datum
    `2026-09-04`, den Urheber (Verifikation Runde 5 und Plan 02-28), den **woertlichen** alten
    Wortlaut in Anfuehrungszeichen, die Messung `REFUSED_RANGES_LEGACY` -> `ranges=[{0 0}] err=<nil>`
    mit dem Verweis auf `02-VERIFICATION.md` `gaps[0]`, den Verweis auf den in Task 2 angelegten
    Ledger-Eintrag mit seiner id, und den Satz, warum der alte Wortlaut nicht einfach geloescht wird:
    eine Korrektur braucht etwas, worauf sie zeigen kann.

Die drei `verification`-Eintraege von D1 bleiben unveraendert — die beiden Untertests und der
Lebendbeleg sind gelaufen und bestanden; falsch war nicht ihr Ergebnis, sondern die Breite der
Beschreibung darueber. `human_judgment: false` bleibt. D2 und D3 bleiben vollstaendig unberuehrt.

Nach der Aenderung muss das Frontmatter weiterhin als YAML parsen; das ist eine Abnahmebedingung.

**Stelle 2 und 3 — die beiden Saetze unter `## Next Phase Readiness` (`:316-317`).** Hier gilt die
Form aus `02-21-SUMMARY.md:345-358` woertlich. Ueber die beiden Aufzaehlungspunkte kommt eine
durchgestrichene, mit `— WITHDRAWN` markierte Zeile und darunter ein eingerueckter Korrekturblock
(`>`-Zitat), der traegt:

  - `**CORRECTION (Verifikation Runde 5 + Plan 02-28, 2026-09-04).**` als Eroeffnung;
  - was gemessen ist: `02-VERIFICATION.md` fuehrt G-02-25 unter `re_verification.gaps_remaining` als
    "VERENGT, NICHT GESCHLOSSEN"; die Falsifikation lief mit `REFUSED_RANGES_LEGACY` durch und liess
    beide Tests gruen;
  - was von `:317` wahr bleibt: **G-02-24 ist geschlossen**, von Runde 5 durch Ausbau der Bedingung
    und Beobachtung der roten Tests nachgemessen. Nur die Haelfte ueber G-02-25 ist falsch, und der
    Korrekturblock sagt ausdruecklich, welche Haelfte welche ist;
  - was Runde 6 getan hat und was sie ausdruecklich **nicht** behauptet: Plan 02-27 hat vier
    Korrekturen geliefert; ob die allquantifizierte Wahrheit damit gilt, stellt eine Verifikationsrunde
    fest. Der Korrekturblock darf an keiner Stelle so gelesen werden koennen, als sei G-02-25 jetzt
    geschlossen — sonst ersetzt er eine Ueberbehauptung durch die naechste;
  - den Verweis auf den in Task 2 angelegten Ledger-Eintrag mit seiner id;
  - warum das der Korrektur wert war und nicht bloss Kosmetik: `workflow.windows_enforce` liest den
    Ledger beim Ship, und ein Leser dieser SUMMARY, der den beiden Saetzen glaubt, haelt eine offene
    Luecke fuer erledigt.

Der Originaltext beider Punkte bleibt darunter stehen, durchgestrichen — geloescht wird nichts.

**Sonst nichts anfassen.** Die Commit-Liste, der Ledger-Abschnitt (`:199`), die
`gaps_mitigated_not_closed`-Buchhaltung, die Self-Check-Liste und die Zeilen ueber G-02-9, Eintrag 56
und die naechste Threat-Id bleiben woertlich. Der Plan korrigiert drei Saetze und schreibt kein
Abschlussdokument um.
  </action>
  <acceptance_criteria>
    - `grep -c 'WITHDRAWN' .planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md` meldet mindestens 2 — beide Saetze unter `## Next Phase Readiness` sind markiert
    - Der Abschnitt `## Next Phase Readiness` enthaelt einen Korrekturblock, der mit `**CORRECTION (` eroeffnet und das Datum `2026-09-04` traegt
    - Das Frontmatter von `02-26-SUMMARY.md` parst weiterhin als YAML, und `coverage` D1 traegt einen Schluessel `correction`
    - Die `description` von D1 enthaelt die Formulierung `umbenannt oder verschoben` **nicht** mehr; der alte Wortlaut steht stattdessen woertlich im `correction`-Block
    - Die drei `verification`-Eintraege von D1 sind unveraendert; D2 und D3 sind byteweise unveraendert
    - Der Originaltext der beiden Punkte `:316-317` steht weiterhin in der Datei, durchgestrichen — nichts wurde geloescht
    - Beide Korrekturen nennen die id des in Task 2 angelegten Ledger-Eintrags
    - Kein Korrekturtext behauptet oder legt nahe, dass G-02-25 geschlossen sei; keiner behauptet, G-02-24 sei offen
    - `git diff --stat -- .planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md` zeigt genau eine geaenderte Datei, und der Diff beruehrt nur den `coverage`-D1-Block und den Abschnitt `## Next Phase Readiness`
  </acceptance_criteria>
  <verify>
    <automated>S=.planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md; grep -c 'WITHDRAWN' $S | grep -qv '^[01]$'</automated>
    <fails_when>Exit 1 — die Datei traegt hoechstens eine `WITHDRAWN`-Markierung. Beim Planen war der Wert 0, diese Pruefung ist also heute rot und kann nicht stumm bestehen. 1 heisst, nur einer der beiden Saetze unter `## Next Phase Readiness` wurde zurueckgezogen — typischerweise `:316`, waehrend `:317` weiterhin beide Luecken der Runde 4 als geschlossen meldet.</fails_when>
    <automated>S=.planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md; O=$(awk 'NR>1 && /^---$/{exit} NR>1{ if ($0 ~ /^    [a-z_]+:/) key=$0; if ($0 ~ /umbenannt oder verschoben/) print key }' "$S"); [ -n "$O" ] && ! printf '%s\n' "$O" | grep -qv '^    correction:'</automated>
    <fails_when>Exit 1, und zwar aus zwei entgegengesetzten Gruenden, die beide Fehlschlaege sind. Die Pruefung ermittelt fuer jedes Vorkommen der Formulierung `umbenannt oder verschoben` im Frontmatter den naechstgelegenen vorangehenden Schluessel und verlangt, dass jedes Vorkommen unter `correction:` haengt. Steht auch nur eines noch unter `description:` — der Zustand beim Planen, gemessen als Eigner `    description: "Der Waechter liest die Browser-Ablehnungsmenge …` —, ist die Behauptung nicht verengt worden. Gibt es gar kein Vorkommen mehr, wurde der alte Wortlaut geloescht statt in den Korrekturblock uebernommen, und die Korrektur hat nichts, worauf sie zeigen kann; das verbietet die Form aus `02-21-SUMMARY.md:347` ausdruecklich. Die Pruefung ist damit schluessel-skopiert und nicht dateiweit: dieselbe Zeichenkette MUSS in der Datei stehen, nur an einer anderen Stelle.</fails_when>
    <automated>S=.planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md; awk 'NR>1 && /^---$/{exit} NR>1{print}' "$S" | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{if(!/^    correction:/m.test(s))process.exit(1);let d=0;for(const l of s.split("\n")){if(/^\t/.test(l)){d=1;break}}process.exit(d?1:0)})'</automated>
    <fails_when>Exit 1 — entweder traegt der `coverage`-Block keinen `correction:`-Schluessel auf der Einrueckungsebene der D1-Felder, oder eine Zeile des Frontmatters beginnt mit einem Tabulator. Beides macht den Block als YAML unbrauchbar bzw. bedeutet, dass der Korrekturschluessel nie angelegt wurde. Diese Pruefung steht neben der schluessel-skopierten darueber, weil jene nur etwas ueber den Ort der alten Formulierung sagt und nichts darueber, ob das Frontmatter danach noch lesbar ist — und eine Durchstreichung im YAML waere genau der Schaden, den Task 3 vermeidet.</fails_when>
    <automated>S=.planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md; grep -c 'Die Luecke \*\*G-02-25\*\*' $S | grep -qv '^0$'</automated>
    <fails_when>Exit 1 — der urspruengliche Satz aus `:316` steht nicht mehr in der Datei. Er soll durchgestrichen stehenbleiben und nicht geloescht werden: die Korrekturform dieser Phase behaelt die falsche Aussage ausdruecklich, damit die Korrektur etwas hat, worauf sie zeigen kann (`02-21-SUMMARY.md:347`). Eine Loeschung waere eine zweite Art, die Aufzeichnung unwahr zu machen.</fails_when>
    <automated>S=.planning/phases/02-transport-seam-talossim-image-factory/02-26-SUMMARY.md; sed -n '/^## Next Phase Readiness/,$p' $S | grep -q '\*\*CORRECTION ('</automated>
    <fails_when>Exit 1 — der Abschnitt `## Next Phase Readiness` enthaelt keinen Korrekturblock in der etablierten Form. Die Pruefung ist auf den Abschnitt begrenzt, damit ein Korrekturblock anderswo in der Datei sie nicht faelschlich erfuellt; genau dort stehen die beiden zurueckgezogenen Saetze.</fails_when>
  </verify>
  <done>Die drei ueberbreiten Aussagen in `02-26-SUMMARY.md` sind zurueckgezogen, datiert korrigiert und zeigen auf den Ledger-Eintrag aus Task 2 — und alles, was an dem Dokument wahr war, steht woertlich unveraendert.</done>
</task>

</tasks>

## Artifacts this phase produces

| Kind | Symbol | Location |
|---|---|---|
| comment change | der Doc-Verweis bei `:458` nennt drei Bedingungen statt zwei; der zusammengefuegte Satz lautet `see refreshTheStoredVerdict for the three conditions and for why a failed refresh is silent` | `internal/httpapi/handlers/schematics.go` |
| sweep command | Sweep A — `grep -rn 'refreshTheStoredVerdict' … \| grep -c '\btwo\b'`, gemessen 1 -> 0; ab jetzt Abnahmebedingung statt Aufzaehlung | `02-28-SUMMARY.md`, `.planning/WINDOWS.md` |
| sweep command | Sweep B — `grep -rniE '(refresh\|409)' … \| grep -icE '\b(two\|zwei\|both)\b'`, gemessen 5 -> 4, mit namentlicher Beurteilung der vier verbleibenden Fundstellen | `02-28-SUMMARY.md`, `.planning/WINDOWS.md` |
| ledger entry | `SUPERSEDES ENTRY 69, WHICH STAYS FIXED (no amend verb exists)` — `unmet-truth` gegen `internal/imagefactory/guard_drift_test.go:83`, bleibt `open`, mit pruefbarer Schliessbedingung | `.planning/WINDOWS.md` |
| ledger entry | die vierte, uebersehene Zwei-Bedingungen-Aussage bei `schematics.go:458`, `deviation`, bei der Anlage als `fixed` markiert, mit der Grenze ihrer eigenen Messung im Text | `.planning/WINDOWS.md` |
| yaml key | `coverage[D1].correction` — datierter Korrekturblock mit dem woertlichen alten Wortlaut, der Messung und der Ledger-id | `02-26-SUMMARY.md` |
| doc correction | zwei WITHDRAWN-Durchstreichungen mit einem `**CORRECTION (…, 2026-09-04)**`-Block unter `## Next Phase Readiness` | `02-26-SUMMARY.md` |
| measurement | Ledger-Signatur vor und nach der Runde (`fm=54/0/15/69 …` -> `fm=55/0/16/71 …`) und die beiden vergebenen ids | `02-28-SUMMARY.md` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/WINDOWS.md` → `workflow.windows_enforce` beim Ship | `/gsd-ship` blockiert, solange `open_count > 0`. Der Status eines Eintrags ist damit kein Kommentar, sondern ein Schalter; ein `fixed`-Eintrag ist an dieser Grenze unsichtbar |
| `.planning/WINDOWS.md` → ein Leser, der nicht dabei war | drei Repraesentationen derselben Aussage — Frontmatter-Zaehler, Markdown-Tabelle, JSON-Block. Laufen sie auseinander, sagt der Ledger dreierlei, und keine der drei Antworten ist als die richtige erkennbar |
| ein Abschlussdokument → die naechste Verifikationsrunde | eine SUMMARY ist die Uebergabe. Was sie als erledigt meldet, wird von der naechsten Runde nicht noch einmal gemessen — genau deshalb kostet eine ueberbreite Zeile dort eine ganze Runde |
| ein Doc-Verweis → ein Leser des 409-Zweigs | `schematics.go:458` ist die einzige Stelle, an der ein Leser die Zahl der ablehnenden Bedingungen erfaehrt, bevor er 55 Zeilen weiter die Funktion selbst liest |

## STRIDE Threat Register

Ids T-02-124..T-02-125. Plan 02-27 dieser Runde vergibt T-02-121..T-02-123; die naechste freie Id
nach diesem Plan ist T-02-126.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-124 | Repudiation | `.planning/WINDOWS.md` — eine offene Eigenschaft ist als `fixed` verbucht und damit beim Ship unsichtbar; `/gsd-ship` laesst eine Luecke passieren, die niemand abgewaehlt hat, weil niemand sie sieht | high | mitigate | ein neuer `unmet-truth`-Eintrag, der in Grossbuchstaben eroeffnet, was er an 69 ueberholt, `open` bleibt und eine pruefbare Schliessbedingung im Text traegt; Eintrag 69 bleibt woertlich stehen. Die drei Repraesentationen werden unabhaengig vom Werkzeug nachgezaehlt und gegen die Signatur `fm=55/0/16/71 md=55/16/71 js=55/16/71 ids=71/71` geprueft, und ein maschineller Check bestaetigt, dass 56, 58 und 66 offen und 69 `fixed` geblieben sind |
| T-02-125 | Repudiation | `internal/httpapi/handlers/schematics.go:458` — ein Doc-Verweis nennt eine falsche Zahl von Bedingungen und schickt einen Leser mit falscher Erwartung in eine Funktion, deren dritte Bedingung eine echte Ablehnung vor dem Ueberschreiben schuetzt | low | mitigate | das Zahlwort korrigiert; die Aufzaehlung dreier Stellen durch zwei mechanische Sweeps ersetzt, deren Werte vor und nach der Aenderung in der SUMMARY stehen und deren erster als Abnahmebedingung festgenagelt ist. Die vier verbleibenden, korrekten Fundstellen sind einzeln beurteilt und benannt |
| T-02-SC | Tampering | npm/pip/cargo installs | low | mitigate | dieser Plan fuegt kein Go-Modul und kein npm-Paket hinzu; es gibt keinen Installationsschritt und daher nichts zu pruefen. Geaendert werden ein Kommentar, der Ledger ueber `gsd-tools windows` und ein Markdown-Dokument |
</threat_model>

<verification>
- `go build ./... && go vet ./...` ist sauber
- `go test ./internal/httpapi/handlers -count=1` ist gruen
- `task test` (`go test ./... -count=1 -race`) ist ueber alle Pakete gruen, einschliesslich der Aenderungen aus Plan 02-27
- `golangci-lint run` endet mit 0 (Binary unter `~/go/bin/golangci-lint`)
- `gofmt -l ./internal` gibt nichts aus
- **Sweep A** (`grep -rn 'refreshTheStoredVerdict' … | grep -c '\btwo\b'`) meldet 0; die SUMMARY nennt den Wert davor (1)
- **Sweep B** (`grep -rniE '(refresh|409)' … | grep -icE '\b(two|zwei|both)\b'`) meldet 4; die SUMMARY nennt den Wert davor (5) und beurteilt alle vier verbleibenden Fundstellen einzeln mit Datei und Zeile
- Die Uebereinstimmung der drei Ledger-Repraesentationen wird **unabhaengig vom Werkzeug** nachgezaehlt und gegen die Signatur `fm=55/0/16/71 md=55/16/71 js=55/16/71 ids=71/71` geprueft; `gsd-tools windows status` bestaetigt zusaetzlich `"ok": true`. Beide Pruefungen sind heute rot bzw. wuerden heute eine andere Signatur liefern — eine Konsistenzpruefung, die auf einem unveraenderten Ledger besteht, prueft nichts
- Ein maschineller Check ueber `windows status --raw` bestaetigt: 56, 58 und 66 sind `open`, 69 ist `fixed`, und der Eintrag, dessen Beschreibung die Teilzeichenkette `SUPERSEDES ENTRY 69, WHICH STAYS FIXED` traegt, ist `open`
- Das Frontmatter von `02-26-SUMMARY.md` parst nach der Korrektur weiterhin als YAML
- `npm --prefix web run test` und `npm --prefix web run typecheck` bleiben gruen; dieser Plan aendert keine Datei unter `web/`
</verification>

<success_criteria>
- Kein Verweis im Baum auf `refreshTheStoredVerdict` nennt mehr eine andere Zahl von Bedingungen als drei, und die allquantifizierte Aussage wird ab jetzt von einem Sweep geprueft statt von einer Aufzaehlung — die vier verbleibenden, korrekten Fundstellen sind einzeln beurteilt und benannt
- `.planning/WINDOWS.md` traegt einen Eintrag, der in Grossbuchstaben eroeffnet, was er an 69 ueberholt, den gemessenen Praefix-Fall und die fehlende Bound-Validierung benennt, die vier Korrekturen aus Plan 02-27 aufzeichnet — und der **`open` bleibt**, mit einer pruefbaren Schliessbedingung im Text
- Eintrag 69 ist unveraendert und weiterhin `fixed`; er wurde ueberholt und nicht bearbeitet, weil es kein Aenderungs-Verb gibt
- Die drei Repraesentationen des Ledgers stimmen nach der Runde weiterhin ueberein, unabhaengig vom Werkzeug nachgezaehlt, ids lueckenlos und doppelfrei — und die SUMMARY nennt die Zahlen vor und nach der Runde sowie jeden abgesetzten Aufruf mit seiner id
- `02-26-SUMMARY.md` behauptet an keiner Stelle mehr, der Waechter scheitere bei jeder Umbenennung oder G-02-25 sei geschlossen; die drei Stellen tragen die Korrekturform aus `02-21-SUMMARY.md:345-358`, und der Originaltext bleibt durchgestrichen stehen
- Keine Korrektur ersetzt eine Ueberbehauptung durch die naechste: kein Text dieses Plans sagt, G-02-25 sei jetzt geschlossen, und keiner sagt, G-02-24 sei offen
- **G-02-9 bleibt offen** und keine Zeile dieses Plans sagt etwas anderes; die Eintraege 56, 58 und 66 bleiben unberuehrt offen
- Genau zwei Ledger-Eintraege wurden angelegt und genau einer davon als `fixed` markiert; kein Beschreibungstext enthaelt ein Pipe-Zeichen
</success_criteria>

<output>
Create `.planning/phases/02-transport-seam-talossim-image-factory/02-28-SUMMARY.md` when done
</output>
