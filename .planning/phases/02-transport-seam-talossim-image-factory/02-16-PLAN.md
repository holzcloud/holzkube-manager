---
phase: 02-transport-seam-talossim-image-factory
plan: 16
type: execute
wave: 2
# depends_on 02-14 is a serialisation dependency, not a data one. Both plans edit files in the
# Go package `imagefactory`/`imagefactory_test`, and two agents editing one package in the same
# wave means each one's `go test ./internal/imagefactory/` compiles the other's half-finished file
# and goes red for reasons that have nothing to do with the plan.
depends_on: ["02-14"]
gap_closure: true
gap_ids: [G-02-18, G-02-13, G-02-12]
files_modified:
  - internal/imagefactory/installer.go
  - internal/imagefactory/installer_test.go
  - internal/imagefactory/fake_test.go
  - internal/imagefactory/live_test.go
autonomous: false
requirements: [FACT-02, FACT-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The live drift guard fails, or refuses to report an observation, when the installer name it asserts about was reached by falling back rather than by resolving the preferred name."
    - "The guard asserts which repository name resolved, by name, rather than only that the two references differ."
    - "That behaviour is provable offline: the fake can make the preferred candidate silent, and a test drives the guard's own assertion helper through that state and shows it does not report a pass."
    - "No statement in the package claims `installer-secureboot` is an alias of `metal-installer-secureboot`; the two are recorded as different images, with the observation dated and reproducible rather than frozen as a digest literal in a comment."
    - "What the installer-name matrix settles is stated as what it probed — the pinned version and the oldest supported one — and never as both ends of the supported range."
    - "The fake's v1.9.0 row is described correctly: it carries both the legacy ordinary name and the legacy SecureBoot name, and the matrix subtest cannot reach v1.9.0 at all."
    - "The substitution of `live_test.go` for `fake_test.go` in the round-2 UAT — which turned a cross-check into a transcription check that cannot fail — is written down where the next reader of these two tables will find it."
  prohibitions:
    - statement: "Do not reopen T-02-53. The user ratified `never substitute` unchanged on 2026-08-30: a SecureBoot request is never answered with an ordinary installer. The decision in task 2 is only about whether the *legacy SecureBoot* name stays a candidate, which is a different question."
      status: unresolved
    - statement: "Do not merge the two tables. `fake_test.go`'s `installerRepos` is branch-coverage fixture and `live_test.go`'s `liveInstallerMatrix` is a registry observation. Both files say so and both name the other; merging them deletes the premise `TestInstallerImageFallsBackToTheLegacyName` rests on. This plan corrects how each is *described*, not what either contains — except where task 3 explicitly says otherwise."
      status: unresolved
    - statement: "Do not add a retry and do not change any timeout, deadline, cache TTL or budget in `internal/imagefactory`. Every budget in this package belongs to the open `02-DECISION-probe-budget.md`, and WINDOWS entry 5 stays open."
      status: unresolved
    - statement: "Do not write a digest literal into a source comment as a standing fact. That is precisely what went stale: `installer.go` carries two digests that no longer reproduce. A digest belongs in a dated observation the live test can re-measure."
      status: unresolved
    - statement: "Do not edit `internal/imagefactory/schematicid.go`, `schematicid_test.go` or `canonical_live_test.go`. Plan 02-14 owns them and has already landed; `liveEnv`, `catalogVersion` and `consoleSchematicID` are shared and must keep their names."
      status: unresolved
    - statement: "Do not touch `.planning/WINDOWS.md`. Plan 02-21 owns the ledger for this round; residuals go in this plan's SUMMARY under a `## Ledger entries to file` heading."
      status: unresolved
    - statement: "Do not run any gate through `Taskfile.yml`. Plan 02-17 rewrites that file in this same wave, so `task test` and `task lint:go` may be read mid-edit. Invoke `go test ./... -count=1 -race`, `golangci-lint run` and `gofmt` directly; the Taskfile is not in this plan's files_modified and must not be."
      status: unresolved
  artifacts:
    - path: "internal/imagefactory/live_test.go"
      provides: "A drift guard that distinguishes a resolved preferred name from a fallback, and records the four names' digests as a dated observation"
      contains: "installerNameMatrixSubtests"
    - path: "internal/imagefactory/installer_test.go"
      provides: "The offline proof that the guard's assertion does not pass on a provisional answer"
    - path: "internal/imagefactory/installer.go"
      provides: "Comments that claim only what has been measured"
  key_links:
    - from: "internal/imagefactory/live_test.go"
      to: "internal/imagefactory/installer.go"
      via: "InstallerImage's second return value is the warnings slice; a non-empty one means the reference was reached past a candidate that never answered, which is the state the guard was blind to"
      pattern: "InstallerImage"
    - from: "internal/imagefactory/installer_test.go"
      to: "internal/imagefactory/fake_test.go"
      via: "setRepoUnreachable makes a candidate silent at the transport level, which is the only way to reproduce a partial throttle offline"
      pattern: "setRepoUnreachable"
---

<objective>
Close G-02-18, G-02-13 and G-02-12 — one causal pair and the record around it.

Purpose: `resolveInstallerRepo` returns `ErrUpstreamUnavailable` only when *no* candidate answered
2xx. On a partial throttle the preferred name times out, the legacy name answers, and a nil error
comes back with a `installer.repo-fallback-unverified` warning riding it. The live drift guard tests
`errors.Is(err, ErrUpstreamUnavailable)`, which never fires, and then asserts only that the two
references differ and that the SecureBoot one names a SecureBoot repository — both of which the
legacy fallback satisfies. Nothing asserts *which* name resolved. The guard passes green on exactly
the failure mode it exists to catch, and this is the shape 02-09's own first run hit. The single
edit that would have closed it went the other way: `cffa851` turned `plain, err :=` into
`plain, _, err :=`, two hunks, no comment, in no record.

That blindness matters more now than it did, because the second gap removes the assumption that made
it harmless. `installer-secureboot` is **not** an alias of `metal-installer-secureboot`: measured
live twice at v1.13.9, `metal-installer-secureboot` is `sha256:dd035140` and `installer-secureboot`
is `sha256:eb5cfa79` — different images. Round 1's G-02-4 evidence recorded them as identical, and
that recording is the basis on which the SecureBoot legacy fallback was treated as harmless. So the
fallback silently swaps one SecureBoot image for another, and the only thing that says so is the
warning `cffa851` discarded.

The third gap is the record around both. `installer.go:197` says the matrix "is settled by" a
subtest that probed two versions — never `MaxSupportedVersion` v1.14 and never v1.13.x below the
pin — and that sentence is what T-02-53's merge acceptance rests on. `fake_test.go:69-71` claims the
matrix settles a v1.9.0 SecureBoot cell the subtest cannot reach and describes the v1.9.0 row as
pinning the legacy ordinary name alone, which is false in both files. And the round-2 UAT restated
the plan's cross-check against `fake_test.go` as a check against `live_test.go` — the same file,
written in the same commit, from the same run — turning it into a transcription check that cannot
fail.

Output: a drift guard that can fail, an offline proof that it can, a decision about the legacy
SecureBoot candidate taken with the measurement in hand, and comments that claim only what was
measured.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-UAT.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-09-SUMMARY.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-12-SUMMARY.md
@docs/api-contract.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A guard that can fail — assert the warnings and the name, and prove it offline</name>
  <read_first>
    - `internal/imagefactory/live_test.go` lines 182-245 — `installerNameMatrixSubtests` in full: the
      two halves (an assertion and a recording), the `ErrUpstreamUnavailable` skips, the two
      assertions at the end that the UAT names as satisfiable by a fallback, and the discarded
      second return value on both `InstallerImage` calls.
    - `internal/imagefactory/live_test.go` lines 245-300 — the recording half and
      `liveInstallerMatrix`, so the new assertions are added in the idiom the file already uses.
    - `internal/imagefactory/installer.go` lines 118-175 — `InstallerImage`'s doc comment and body:
      the three-way outcome (proven, provisional, refused), and the fact that the warning rides
      every answer including cached ones. The warnings slice is the signal the guard was throwing
      away.
    - `internal/imagefactory/installer.go` lines 240-400 — `installerRepo`, `storeInstallerRepo` and
      `requestionInstallerRepo`, enough to know when a provisional entry is written and when it is
      re-questioned, because a cached provisional answer must also fail the guard.
    - `internal/imagefactory/fake_test.go` — `setRepoUnreachable` and `setRepoReachable`, added by
      `cffa851`: a repository can be made silent at the transport level (hijack and close, no HTTP
      response). This is the only way to reproduce a partial throttle offline and it already exists.
    - `internal/imagefactory/installer_test.go` lines 840-900 — the existing provisional-fallback
      tests written against those helpers, which is the shape the new offline test copies.
    - `internal/imagefactory/warnings.go` — `WarningInstallerRepoFallbackUnverified` and its code
      constant, for asserting on the code rather than on the sentence.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-UAT.md` gap `G-02-18`, whole
      entry, including the `cffa851` artifact line.
  </read_first>
  <behavior>
    - Given a resolution that carries no warnings, the guard's assertion reports an observation and
      checks the resolved repository name against the expected one.
    - Given a resolution that carries `installer.repo-fallback-unverified`, the guard's assertion
      reports a non-observation naming the warning, and does not report a pass.
    - Given a resolved reference whose repository segment is not the expected name, the guard fails
      with a message naming both the expected and the actual repository.
    - The offline test reproduces the middle case through the fake with the preferred candidate made
      silent, without touching the network.
  </behavior>
  <files>internal/imagefactory/live_test.go, internal/imagefactory/installer_test.go</files>
  <action>
Stop discarding the warnings. Both `InstallerImage` calls in `installerNameMatrixSubtests` bind the
second return value, and the two assertions at the end of the subtest gain a precondition: a
resolution carrying any warning is not an observation about which name answers, because the answer
came from a candidate the resolver fell back to. Treat it exactly as the file already treats a
transport failure — skip with a `NOT OBSERVED:` message that names the warning code and the
candidate that went unheard — rather than failing, because a throttle is not a defect and
factory.talos.dev throttles (WINDOWS entry 5). What must change is that it stops being reported as a
pass.

Then assert the name. `plain` must resolve to the platform-prefixed ordinary repository and `secure`
to the platform-prefixed SecureBoot one at the pinned version, by exact repository segment and not
by substring containment — the current containment check is satisfied by the legacy SecureBoot name
too, which is the whole gap. Parse the repository segment out of the reference rather than matching
inside the whole string; a reference is host, repository, id and tag, and the id is 64 hex
characters that can contain anything a substring test looks for.

Factor both of those into one small helper so the assertion has a name and a single home — something
that takes the reference, the warnings and the expected repository name and reports one of three
outcomes: observed and correct, observed and wrong, or not observed because the answer was
provisional. The whole point of the helper is that it can then be exercised without the network.

Write the offline test that does exactly that, in `installer_test.go` against the fake. Make the
preferred candidate silent with `setRepoUnreachable`, resolve, and assert the helper reports a
non-observation naming the fallback warning — not a pass. Then make it reachable again and assert
the helper reports an observation. That pair is the Nyquist check for this gap: it fails today,
because today's code has no such helper and the guard has no way to tell the two states apart.

Also assert the cached case, because `storeInstallerRepo` keeps the warning on the cached entry: a
second resolution served from cache must still be reported as provisional. A guard that is only
honest on a cold path is honest once per process.

Finally, extend the live recording half to capture the manifest digest of each of the four names at
the pinned version, from the `Docker-Content-Digest` response header (falling back to the SHA-256 of
the body if the header is absent), and log the four digests. Do not assert on them and do not write
them into any source comment — they are the input to task 2 and a dated observation for the SUMMARY.
The point of measuring them here rather than by hand is that the next person can re-measure them by
running one test.
  </action>
  <acceptance_criteria>
    - `go test ./internal/imagefactory/ -count=1 -race` is green
    - An offline test exists that makes the preferred candidate silent and asserts the guard's helper does not report a pass; it fails if the helper's provisional check is removed (verify by removing it locally, watching it go red, and restoring)
    - Neither `InstallerImage` call in `installerNameMatrixSubtests` discards its warnings — established by reading the two call sites, not by a grep: a discarded return is spelled several ways and a pattern match on one of them reads far stronger than it is. The binding assertion is the offline test above; this criterion is the reviewer's eye on the two lines `cffa851` changed
    - The executor runs `HOLZKUBE_FACTORY_LIVE=1 go test ./internal/imagefactory/ -run TestLiveFactory -count=1 -v` once and records in the SUMMARY: which repository name resolved for each of the two requests, whether either carried a warning, and the four manifest digests with the date
    - `go test ./internal/imagefactory/ -run TestLiveFactory -count=1 -v` without the opt-in exits 0 and skips loudly
    - `gofmt -l ./internal` prints nothing and `golangci-lint run` exits 0 — invoked directly and **not** through `task lint:go`, because plan 02-17 rewrites `Taskfile.yml` in this same wave
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/imagefactory/ -count=1 -race</automated>
  </verify>
  <done>A fallback can no longer be reported as an observation about which installer name resolves, and a test that needs no network proves it.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Whether `installer-secureboot` stays a fallback candidate now that it is known to be a different image from `metal-installer-secureboot`</decision>
  <context>
Task 1's live run measured the four names' manifest digests. The round-2 audit measured the same
thing twice at v1.13.9: `metal-installer-secureboot` is `sha256:dd035140` and `installer-secureboot`
is `sha256:eb5cfa79`. They are two images, not two names for one. Round 1's G-02-4 evidence recorded
them as identical, and that recording is the reason the SecureBoot legacy fallback was treated as
harmless when `installerCandidates` was written.

What is **not** in question: the ratified "never substitute" rule (T-02-53, ratified by the user on
2026-08-30). A SecureBoot request is never answered with an ordinary installer, and no option below
changes that. The question here is narrower — whether the *legacy SecureBoot* name remains a
candidate when the platform-prefixed one does not answer.

The fallback is reachable in practice: factory.talos.dev throttles without an HTTP response (WINDOWS
entry 5, demonstrated twice), and a throttled preferred candidate with an answering legacy one is
exactly the state that produces it. After task 1 the drift guard can no longer mistake that state
for a proven answer, but the operator's asset panel still can: the answer is returned with a warning
whose text says the preferred name was unheard, not that the image is a different one.
  </context>
  <options>
    <option id="option-a">
      <name>Keep the fallback, unchanged</name>
      <pros>Both candidates are SecureBoot installers, so the drift the refusal exists to prevent — SecureBoot paired with an ordinary installer — cannot occur through this path. The answer already carries `installer.repo-fallback-unverified`, and after task 1 nothing reports it as proven. No contract change.</pros>
      <cons>On a throttle an operator can receive a different SecureBoot image than the one they would have received a minute earlier, and the only thing that says so is a warning phrased as "the preferred name was unheard" rather than "this is a different image".</cons>
    </option>
    <option id="option-b">
      <name>Drop the legacy SecureBoot candidate</name>
      <pros>A SecureBoot request asks about one name only; if it does not answer, the request is refused with no installer reference, and no substitution of any kind is possible. Halves the worst-case cold walk for a SecureBoot request, which WINDOWS entry 20 records as meeting the 60s write timeout exactly.</pros>
      <cons>Turns a usable-but-provisional answer into a 502 on the exact upstream behaviour the fallback was built for. The live matrix shows all four names answering at both probed versions, so this trades a measured availability loss for a risk nobody has observed biting. `TestInstallerImageFallsBackToTheLegacySecureBootName` loses its premise and would be deleted, not adapted.</cons>
    </option>
    <option id="option-c">
      <name>Keep the fallback, and label it for what it is</name>
      <pros>Same availability as A, plus a warning code specific to the SecureBoot fallback whose detail says the legacy SecureBoot repository is a different image rather than another name for the same one. The operator reading the panel learns the thing that is actually true of their reference.</pros>
      <cons>A fourth warning code in a taxonomy `docs/api-contract.md` calls closed, declared in `warnings.go` and mirrored in `web/src/api.ts` — neither of which is in this plan's `files_modified`, so the two-file addition has to be handed to plan 02-19 (which owns the contract in wave 4) and recorded as a follow-up here.</cons>
    </option>
  </options>
  <recommendation>Option C, with A as the fallback if the cost of a fourth warning code is judged too high. The failure mode is not a broken image — both are SecureBoot installers — it is a *different* image than the reference the operator may already have copied, indistinguishable from an ordinary provisional answer. That is a labelling problem, and C fixes the label. B pays for it with an outage on precisely the upstream behaviour the fallback exists for.</recommendation>
  <pre-decision decided_by="user" decided_at="2026-08-30" answer="option-c">
Answered ahead of execution at the user's instruction, so this gate does NOT block: proceed on
Option C without pausing.

The measurement this decision needs is already in hand and did not have to wait for execution —
the round-2 audit resolved both names live against factory.talos.dev at v1.13.9 and recorded
`metal-installer-secureboot` as sha256:dd035140 and `installer-secureboot` as sha256:eb5cfa79.
They are different images, which is the fact Option C turns into a label. Round 1's G-02-4
evidence recording them as an identical legacy alias is the claim being corrected.

If execution finds that the digests now agree, or that a fourth warning code cannot be added
without widening past this plan's gaps, stop and report rather than silently falling back to
Option A — the fallback named in the recommendation is a human's call, not an executor's.
  </pre-decision>
  <resume-signal>Reply with the option id, or dictate the candidate list and the warning wording directly.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Make every statement in the package claim only what was measured</name>
  <read_first>
    - `internal/imagefactory/installer.go` lines 174-215 — `installerCandidates`' doc comment in
      full. Three things in it are now wrong or overstated: the digest pair, the implicit treatment
      of the legacy SecureBoot name as an equivalent, and the closing sentence claiming the
      version-range question "is settled by" the matrix subtest.
    - `internal/imagefactory/installer.go` lines 10-20 — `legacyInstallerRepo` and its comment, the
      other place the legacy name is described.
    - `internal/imagefactory/fake_test.go` lines 45-82 — the whole `installerRepos` comment and the
      map. Compare the prose against the map literally: the `installerLegacyVersion` row is
      `{"installer": true, "installer-secureboot": true}`, and the comment says the row pins v1.9.0
      to the legacy ordinary name alone and that the matrix subtest settles the SecureBoot cell.
    - `internal/imagefactory/live_test.go` — `liveInstallerMatrix` and the versions the matrix
      subtest actually loops over (`catalogVersion` and `liveOlderVersion`), which is the fact both
      overstatements are measured against.
    - `internal/imagefactory/client.go` — `MaxSupportedVersion`, so the restatement names the bound
      that was never probed by its own identifier.
    - `.planning/phases/02-transport-seam-talossim-image-factory/02-09-PLAN.md` line 459 and
      `02-UAT.md` line 113 — the plan's cross-check and the UAT's restatement of it. The difference
      between the two is the third record to write down.
    - This plan's SUMMARY `## Decision` section from task 2.
  </read_first>
  <files>internal/imagefactory/installer.go, internal/imagefactory/fake_test.go, internal/imagefactory/live_test.go, internal/imagefactory/installer_test.go</files>
  <action>
First, implement task 2's decision. If it was A, nothing changes in the resolver. If it was C, add
the distinct SecureBoot fallback warning where task 2 says and record the two-file follow-up for
plan 02-19. If it was B, narrow `installerCandidates` and delete the legacy-SecureBoot fallback test
rather than adapting it.

Then the record, in three parts.

**The alias claim and the digests.** Replace the digest pair in `installerCandidates`' comment with
the property it was there to demonstrate — that the SecureBoot repository name selects a different
image, which is why the name is the only thing that selects SecureBoot — and point at the live
subtest as the thing that measures it, with the date of the recorded observation. Do not write new
digest literals in their place; a digest in a comment is a fact with an expiry date and nothing in
the build checks it, which is exactly how the current pair went stale. Add the sentence the package
is missing outright: the legacy SecureBoot name is a *different image* from the platform-prefixed
one and not another name for it, measured, with the date. That sentence is the correction of a claim
that has been load-bearing since round 1.

**What the matrix settles.** Rewrite the closing sentence of `installerCandidates`' comment so it
states what was probed rather than what was concluded: the pinned version and the oldest supported
version, both of which answer on all four names. Then state, in the same place, what is therefore
still open — `MaxSupportedVersion` was never probed, and neither was any v1.13.x below the pin — and
that the new-502 path's reachability is unknown there rather than ruled out. This sentence is the one
T-02-53's merge acceptance rests on, so it must not read as a guarantee that a two-version sample
cannot support.

While in `live_test.go`, make the matrix cover more of the range rather than only describing its
limits: ask `client.Versions()` for the newest version inside the supported range at run time and,
if it is above the pin, probe it as a third row and record it; if there is none, record that there is
none. A range bound that is never a concrete tag cannot be probed, but the newest concrete version
inside the range can, and doing so converts one sentence of the overclaim into a measurement.

**The fake's v1.9.0 row.** Correct the comment to describe the map that is actually there: the row
carries the legacy ordinary name and the legacy SecureBoot name, and the fallback test rests on the
first of those. Delete the claim that the matrix subtest settles the SecureBoot cell — it loops over
two modern versions and cannot reach v1.9.0 at all — and replace it with what is true: that cell is a
constructed fixture nothing has checked against the registry, in the same register the file already
uses for `installerNoSecureBootVersion`. Change no map entry; the prose is what is wrong.

**The substituted check.** Write the third record into `live_test.go`'s file-level comment, beside
the two-tables explanation, because that is where the next person deciding whether these tables agree
will be standing. State it plainly: 02-09-PLAN.md's task-3 check asked whether the recorded matrix
agrees with the table in `fake_test.go`, which has no v1.12.0 row at all; the round-2 UAT restated it
as agreement with `live_test.go`, which was written in the same commit by the same author from the
same run, making it a transcription check that cannot fail. Say which comparison is the meaningful
one and why the two tables are allowed to disagree. Repeat the record in this plan's SUMMARY.
  </action>
  <acceptance_criteria>
    - `go test ./internal/imagefactory/ ./internal/httpapi/handlers/ -count=1 -race` is green
    - `go test ./internal/imagefactory/ -run TestInstallerImageFallsBackToTheLegacyName -count=1` exits 0 — the premise that test rests on is intact, or the test is gone because task 2 chose Option B, and the SUMMARY says which
    - No digest literal remains in any non-test source file in the package: `grep -rc 'sha256:' internal/imagefactory/installer.go` reports 0
    - `internal/imagefactory/installer.go` names `MaxSupportedVersion` in the sentence describing what the matrix leaves open: `grep -c 'MaxSupportedVersion' internal/imagefactory/installer.go` reports at least 1
    - `internal/imagefactory/live_test.go` carries the record of the substituted check: `grep -c 'fake_test.go' internal/imagefactory/live_test.go` reports at least 1
    - The executor re-runs the opt-in live test once after the change and records the third version row (or its absence) in the SUMMARY
    - `golangci-lint run` exits 0 and `gofmt -l ./cmd ./internal` prints nothing — invoked directly, not through the Taskfile plan 02-17 is editing in this wave
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/imagefactory/ ./internal/httpapi/handlers/ -count=1 -race && test "$(grep -c 'sha256:' internal/imagefactory/installer.go)" = "0"</automated>
  </verify>
  <done>Every sentence in the package about the installer names is one the recorded observations support, and the three corrections are in the tree rather than only in a UAT file.</done>
</task>

</tasks>

## Interaction with the open probe-budget decision

`02-DECISION-probe-budget.md` owns G-02-1, G-02-2 and G-02-9 and stays open. Two adjacencies:

1. **Option B would move a budget question.** Dropping the legacy SecureBoot candidate halves the
   worst-case cold walk for a SecureBoot request, which is the composition WINDOWS entry 20 records
   as `2 × 30s` against a `60s` write timeout. That is a side effect, not a reason: if B is chosen it
   is chosen on the image-identity argument, and the budget improvement is recorded as residue.
2. **Nothing here re-probes.** The guard reads what the resolver returns and asserts about it. No
   new request shape, no retry, no cache change.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| factory.talos.dev registry -> holzkube's installer reference | a third party decides which repository names answer, and it throttles without an HTTP response |
| the installer reference -> the upgrade RPC | a wrong reference produces an upgrade that reports success and silently drops every system extension |
| a test's green -> a reader's belief | a guard that passes on the failure it exists to catch is worse than no guard, because it is cited as evidence |

## STRIDE Threat Register

Ids T-02-81..T-02-84. The next free id after this plan is T-02-85, which plan 02-17 takes. Grep the
phase directory before assigning any id.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-81 | Spoofing | the live drift guard reporting a fallback as an observation about the preferred name | high | mitigate | the guard binds the warnings, treats any warning as a non-observation, and asserts the resolved repository segment by exact name; an offline test through the fake proves it |
| T-02-82 | Tampering | a SecureBoot request served a different SecureBoot image than the one it would have resolved a minute earlier | medium | mitigate | task 2 decides the fallback with the measured digests in hand and records the answer; whichever option is chosen, the answer is labelled rather than silent |
| T-02-83 | Repudiation | comments asserting a version-range guarantee a two-version sample cannot support | medium | mitigate | the closing sentence states what was probed and names `MaxSupportedVersion` as unprobed; the matrix additionally probes the newest concrete version in range at run time |
| T-02-84 | Repudiation | a cross-check silently replaced by one that cannot fail | low | mitigate | the substitution is written into `live_test.go`'s file comment and into the SUMMARY, beside the explanation of why the two tables are allowed to disagree |
| T-02-SC | Tampering | package-manager installs | low | mitigate | this plan adds no Go module and no npm package; `go.mod` and `go.sum` are unchanged |
</threat_model>

<verification>
- `go test ./... -count=1 -race` is green across every package — run directly rather than through `task test`, because plan 02-17 edits `Taskfile.yml` in this same wave and a target read mid-edit is not a gate
- `golangci-lint run` exits 0 and `gofmt -l ./cmd ./internal` prints nothing
- `go test ./internal/imagefactory/ -run TestLiveFactory -count=1 -v` skips loudly without the opt-in
- The SUMMARY records: which name resolved for each request, whether either was provisional, the four digests with a date, the third version row or its absence, the `## Decision` from task 2, and a `## Ledger entries to file` section
- `go.mod` and `go.sum` are unchanged
</verification>

<success_criteria>
- G-02-18 closed: the drift guard asserts zero warnings and the exact resolved repository name, and an offline test proves it does not pass on a fallback
- G-02-13 closed: the alias claim is corrected with a dated measurement instead of stale digest literals, and the fallback question is decided on the record
- G-02-12 closed: what the matrix settles matches what it probed, the fake's v1.9.0 row is described correctly, and the substituted UAT check is written down where the next reader will find it
- T-02-53's ratified "never substitute" rule is unchanged
</success_criteria>

<output>
Create `.planning/phases/02-transport-seam-talossim-image-factory/02-16-SUMMARY.md` when done
</output>
