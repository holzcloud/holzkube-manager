---
phase: 02-transport-seam-talossim-image-factory
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-08"]
files_modified:
  - internal/talos/deadline.go
  - internal/talos/deadline_test.go
  - internal/talos/retry.go
  - internal/talos/retry_test.go
  - internal/talos/breaker.go
  - internal/talos/breaker_test.go
  - internal/talos/fanout.go
  - internal/talos/fanout_test.go
  - internal/talos/maintenance.go
  - internal/talos/maintenance_test.go
  - internal/talos/errors.go
  - internal/talos/errors_test.go
  - internal/talos/client.go
  - internal/talos/contract_test.go
autonomous: false
requirements: [TRANS-03, TRANS-04, TRANS-05, TRANS-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A call issued with a context carrying no deadline is refused before the connection is used; there is no default-less path through the wrapper."
    - "Retries never extend a call's deadline: the budget covers every attempt, proven by a test where the retry window is smaller than the backoff sum."
    - "An RPC absent from the retry allowlist is not retried, proven at the sim by a call counter rather than by a client-side error."
    - "A cluster-only method does not exist on MaintenanceClient: a compile-time assertion proves the call fails to build rather than failing at runtime."
    - "A target that has gone silent for 90 seconds does not delay a concurrent call to a second target beyond that second call's own deadline."
    - "After the configured consecutive transport failures the per-node breaker opens and short-circuits without dialing; an RPC-level failure from a reachable node does not open it."
    - "The per-node breaker map does not grow without bound: a test asserts its length after a sweep."
    - "Every one of the nine talossim scenarios produces the client behaviour written in the talossim registry's ExpectedClient column, asserted call by call."
  prohibitions:
    - statement: "Retry must never be the default: an RPC that nobody classified must not be retried. A denylist of non-idempotent RPCs forgets the next mutating RPC a later phase adds, and a retried Bootstrap or Reset is the cluster-destroying case this allowlist exists to prevent."
      status: unresolved
    - statement: "A per-node fan-out must never turn one unreachable node into a whole-inventory outage — neither by blocking on a shared lock, nor by driving the fail-closed audit writer's global mutex once per node per operation."
      status: unresolved
    - statement: "A transport failure must never reach the operator as an anonymous internal error; the failure that is hardest to diagnose is the one whose only record says nothing happened."
      status: unresolved
  artifacts:
    - path: "internal/talos/deadline.go"
      provides: "Deadline classes as a reviewable table keyed by RPC, and the structural refusal of a deadline-less call (D-04)"
      contains: "ErrNoDeadline"
    - path: "internal/talos/retry.go"
      provides: "The read-only retry allowlist, the 200ms-to-800ms full-jitter backoff, and the in-budget attempt loop"
    - path: "internal/talos/breaker.go"
      provides: "Per-node circuit breaker with a sweep driven by the only method that adds entries, plus Len for the growth assertion"
    - path: "internal/talos/fanout.go"
      provides: "Per-node fan-out where one dead node costs one dead node's worth of work (TRANS-05)"
    - path: "internal/talos/maintenance.go"
      provides: "MaintenanceClient as a distinct type with a non-overlapping method set (TRANS-03, D-06)"
      contains: "type MaintenanceClient struct"
    - path: "internal/talos/errors.go"
      provides: "Classified transport errors carrying operation, target and kind"
    - path: "internal/talos/contract_test.go"
      provides: "The client-side half of the TRANS-07 contract suite: one assertion per scenario against the documented expectation"
  key_links:
    - from: "internal/talos/contract_test.go"
      to: "internal/talossim/scenario.go"
      via: "the suite iterates talossim.Registry, so a scenario added there without an assertion here fails the suite"
      pattern: "talossim\\.Registry"
    - from: "internal/talos/errors_test.go"
      to: "internal/httpapi/problem.go"
      via: "a test-only import asserting each transport error kind maps to exactly one reserved upstream code token"
      pattern: "upstream\\.node-"
    - from: "internal/talos/client.go"
      to: "internal/talos/deadline.go"
      via: "every call path goes through the deadline gate before the connection is used"
      pattern: "requireDeadline"
---

<objective>
Make an unreachable node cost one unreachable node: enforced deadlines, retries only for a
read allowlist, a per-node circuit breaker, per-node fan-out, and a maintenance client that
cannot be confused with a cluster client because the compiler will not let it.

Purpose: ROADMAP success criterion 3, in full. It is also the point at which the phase's
oracle pays for itself — every property here is asserted against the nine `talossim`
scenarios from plan 02-03, so "der Client verhält sich definiert statt zufällig" becomes a
running test rather than a claim.

Output: the policy layer of `internal/talos`, and the client-side half of the contract suite
that Phase 3 will later run against real Talos too (TRANS-08).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-CONTEXT.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-PATTERNS.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-01-SUMMARY.md
@.planning/phases/02-transport-seam-talossim-image-factory/02-03-SUMMARY.md
@docs/talossim.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the deadline classes and the retry allowlist — and decide what to do about the conflict they create</decision>
  <context>
`02-CONTEXT.md` `<deadline_policy>` is explicitly headed **"PROPOSED, awaiting confirmation"**.
The operator asked to be shown a proposal rather than decide from scratch, and it has not
been confirmed. Everything in this plan's tasks 2 to 4 implements it as written, and plan
02-03's `ExpectedClient` column was derived from it — so a change here changes both.

**The proposal, verbatim in substance:**

*Deadline classes.* Probe 5 s (`Version` used as the D-05 liveness check). Fast read 10 s
(`Version`, `Hostname`, `ServiceList`, `Memory`, `LoadAvg`, `SystemStat`, `CPUInfo`,
`CPUFreqStats`, `DiskStats`, `NetworkDeviceStats`, `Mounts`, `Netstat`, `Processes`, `Stats`,
`Containers`, `EtcdMemberList`, `EtcdStatus`, `EtcdAlarmList`, COSI Get/List). Mutation 30 s
to *initiate* (`ApplyConfiguration`, `Bootstrap`, `Reset`, `Reboot`, `Shutdown`, `Upgrade`,
`Rollback`, `MetaWrite`, `MetaDelete`, `ServiceStart/Stop/Restart`, `ImagePull`,
`EtcdLeaveCluster`, `EtcdRemoveMemberByID`, `EtcdForfeitLeadership`, `EtcdDefragment`,
`EtcdRecover`, `EtcdDowngrade*`) — the long work is asynchronous on the node, so this bounds
the call and not the operation. Stream: no total deadline (`Logs`, `Dmesg`, `Events`, `Read`,
`Copy`, `List`, `DiskUsage`, `ImageList`, `EtcdSnapshot`, `PacketCapture`), bounded instead
by a **first-byte deadline of 10 s** and an **idle timeout of 60 s** without data.

*Retry allowlist.* Retried, and only on transport-level `Unavailable` or a connection
failure: every RPC in the Fast read class. Never retried: every Mutation (not idempotent — a
retried `Bootstrap` is exactly the `second_bootstrap_returns_AlreadyExists` scenario), every
Stream (a retry restarts a partially consumed stream, silently duplicating or dropping data),
and `EtcdSnapshot`, `PacketCapture`, `DiskUsage` (reads, but expensive enough that a retry
storm is its own outage). Policy: at most 2 retries (3 attempts), exponential backoff
200 ms → 800 ms with full jitter, all attempts inside the original call deadline.

**The conflict this creates, which CONTEXT names and does not resolve.**
`cmd/holzkubed/main.go` sets `WriteTimeout = 60 s` process-wide, and its comment scopes the
reasoning to argon2id plus the login rate limiter — nothing about upstream calls. Mutations
at 30 s fit. **Streams do not fit at all**, and none of the three `ResponseWriter` wrappers
in the middleware chain implements `Flush`, `Unwrap` or `Hijack`, so a streaming endpoint on
this chain silently buffers. CONTEXT states Phase 5 needs this resolved and that Phase 2
should not paper over it.
  </context>
  <options>
    <option id="option-a">
      <name>Confirm the table as proposed; record the stream conflict as a Phase 5 entry blocker and build no HTTP streaming endpoint here</name>
      <pros>The stream deadline policy is written down and testable at the transport layer now, where `talossim`'s `slow_log_consumer` can exercise it; nothing is papered over, because Phase 2 exposes no streaming HTTP route at all, so the `WriteTimeout` collision cannot bite yet; the deferred item is already recorded in `02-CONTEXT.md` `<deferred>`.</pros>
      <cons>The collision stays live in the codebase until Phase 5; a future plan that adds a streaming route before reading the deferred list would hit it.</cons>
    </option>
    <option id="option-b">
      <name>Confirm the table and fix the HTTP chain now: implement Flush/Unwrap on the ResponseWriter wrappers and scope WriteTimeout</name>
      <pros>Removes the trap at the moment it is understood rather than leaving it for a phase under time pressure.</pros>
      <cons>Touches the middleware chain and the composition root for a capability nothing in this phase uses; `02-CONTEXT.md` `<deferred>` explicitly assigns the WriteTimeout/streaming redesign to Phase 5, so doing it here contradicts a recorded deferral and expands a phase that already carries a release blocker.</cons>
    </option>
    <option id="option-c">
      <name>Change one or more values or class memberships before implementation</name>
      <pros>The numbers are constants and the operator may know their own network better than the proposal does.</pros>
      <cons>Plan 02-03's `ExpectedClient` column was derived from these values and must be updated in the same change; D-04 also notes that the *structure* — no call without a deadline — is the part that must not be relaxed, whatever the numbers become.</cons>
    </option>
  </options>
  <recommendation>Option A. It confirms a proposal that was derived from the real 54-method surface rather than invented, and it honours CONTEXT's own instruction on both halves: do not paper over the conflict, and do not do Phase 5's work here. Record the collision explicitly in the SUMMARY as a Phase 5 entry requirement.</recommendation>
  <resume-signal>Reply with the option id, or dictate the changed deadline values, class memberships or retry policy — noting that a change also amends the ExpectedClient column in `internal/talossim/scenario.go` and in `docs/talossim.md`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two client types the compiler keeps apart, and transport errors with names</name>
  <reversibility rating="costly">D-06: cluster and maintenance clients are two distinct
  wrapper types with non-overlapping method sets rather than one type with a mode flag.
  Collapsing them later touches every call site.</reversibility>
  <read_first>
    - `internal/talos/client.go` and `internal/talos/talos.go` from plan 02-01 — current state, including the D-05 liveness probe and the supported-range check plan 02-03 added.
    - `internal/store/store.go` lines 63-66 — the `SettingsStore` comment: "a singleton with a List method is an invitation to write code that assumes otherwise". That is the same argument D-06 makes, and the `MaintenanceClient` doc comment should make it in the same register.
    - `internal/auth/auth.go` lines 1-3 — the package doc that names what the package must *not* know; the `MaintenanceClient` doc says the mirror image.
    - `internal/httpapi/problem.go` as written by plan 02-02 — the reserved `upstream.node-*` code tokens this task maps onto error kinds.
    - `.planning/research/STACK.md` lines 87-101 — the verified maintenance-mode connect path: `client.WithTLSConfig(&tls.Config{InsecureSkipVerify: true})` plus `client.WithEndpoints(...)`, short-circuiting `buildTLSConfig` at `client/connection.go:67-70`; and the warning that `client/insecure_credentials.go` is behind a `sidero.debug` build tag and will not compile.
  </read_first>
  <behavior>
    - `MaintenanceClient` exposes `ApplyConfiguration`, `Version`, `Disks` and a COSI read accessor, and nothing else.
    - A source file calling a cluster-only method on a `MaintenanceClient` does not compile; the assertion lives in a `//go:build` -excluded file that the test runs `go build` against and expects to fail.
    - `NewMaintenanceClient` refuses `Creds` whose `Kind` is `CredCluster`, and `NewClusterClient` refuses `CredMaintenance`, each with a named error.
    - A dial failure produces a `*talos.Error` with kind unreachable, carrying the operation name and the `Target`.
    - A deadline expiry produces kind timeout; a gRPC status other than `Unavailable` from a reachable node produces kind rejected, carrying the upstream status code.
    - `Error.Error()` names the operation and the machine id, and never the raw address of a node that an unauthenticated caller could not otherwise learn.
  </behavior>
  <files>internal/talos/maintenance.go, internal/talos/maintenance_test.go, internal/talos/errors.go, internal/talos/errors_test.go, internal/talos/client.go</files>
  <action>
`maintenance.go`. `type MaintenanceClient struct` — a **separate type**, not a mode flag on
`ClusterClient`. machinery provides no such split: both paths produce the same
`*client.Client` and nothing on the value records its mode, so the separation is built
entirely here. The method set is deliberately small and closed: `ApplyConfiguration`,
`Version`, `Disks`, and a COSI read accessor. `func NewMaintenanceClient(ctx context.Context,
d Dialer, t Target, c Creds) (*MaintenanceClient, error)` connects with
`client.WithTLSConfig` — per the verified source reading, a non-nil `tlsConfig` short-circuits
before `buildTLSConfig(configContext)` is consulted, which is what lets a client connect with
no talosconfig and no CA, exactly as `talosctl --insecure` does. Do not reach for
`client/insecure_credentials.go`: it is behind a `sidero.debug` build tag and is not compiled
in a normal build. The doc comment carries D-06's argument: a compile error, not a runtime
rejection, is what stops a cluster-only call against a maintenance node, and that is what
"nicht verwechselbar" in success criterion 3 is asking for.

Both constructors validate `Creds.Kind` and refuse the wrong one. Two types that both accept
either credential kind would be two names for one type.

`errors.go`. `type ErrorKind int` with `KindUnreachable`, `KindTimeout`, `KindRejected`, and
`type Error struct { Op string; Machine model.MachineID; Kind ErrorKind; Status codes.Code; Err error }`
implementing `error` and `Unwrap`. Classification lives in one function so every call path
classifies identically: a dial or connection failure and a transport-level `Unavailable`
become `KindUnreachable`; a context deadline or `DeadlineExceeded` becomes `KindTimeout`; any
other gRPC status from a node that answered becomes `KindRejected` carrying the status code.
The distinction between unreachable and rejected is load-bearing for the breaker in task 4 —
"the node is gone" and "the node said no" must not open the same circuit — so the doc comment
says that, in the register of `problem.go`'s "who are you" versus "you may not" argument.

`Error.Error()` names the operation and the machine id. It does not name the node's address:
this value can travel to an HTTP handler, and the address is the same class of string
`ChainStatus.Public()` strips.

`errors_test.go`. A table asserting each `ErrorKind` maps to exactly one of the reserved
`upstream.node-*` code tokens declared in `internal/httpapi` by plan 02-02, and that the
mapping is total and injective. This test imports `internal/httpapi`; production code in
`internal/talos` must not, because the transport sits below HTTP. The HTTP adapter that
performs the mapping arrives in Phase 6 with the first route that drives a node call — this
test is what keeps the reserved tokens honest until then.

`client.go`: fold the shared connect logic used by both constructors into one unexported
helper so the two types cannot drift in how they dial.
  </action>
  <acceptance_criteria>
    - `internal/talos/maintenance.go` contains `type MaintenanceClient struct` and `func NewMaintenanceClient(`
    - `go doc ./internal/talos MaintenanceClient` lists exactly the four permitted methods plus any constructor and `Close`
    - `go test ./internal/talos/ -run TestMaintenanceClientRejectsClusterOnlyCall -count=1` exits 0, and the test proves the rejection by running `go build` against a build-tag-excluded fixture file and asserting a non-zero exit with a compiler error naming the undefined method
    - `NewMaintenanceClient` with `CredCluster` returns an error naming both the supplied and the required credential kind; `NewClusterClient` with `CredMaintenance` does the same
    - `go test ./internal/talos/ -run TestErrorKindMapping -count=1` exits 0 and asserts a total, injective mapping onto the reserved `upstream.node-*` tokens
    - A test builds a `Target` whose `Addr` is a unique sentinel string, provokes a transport failure, and asserts `Error.Error()` contains the machine id while the sentinel is absent from the message
    - `go test ./... -count=1 -race` exits 0 and `golangci-lint run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/talos/ -count=1 -race</automated>
  </verify>
  <done>Cluster and maintenance clients are separate types the compiler keeps apart, and every transport failure carries a classified kind that maps to a reserved contract code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: No call without a deadline, and retries only for the read allowlist</name>
  <reversibility rating="reversible">D-04: the deadline values and the allowlist membership
  are constants in one table. The structure — that there is no default-less path — is the
  part that must not be relaxed.</reversibility>
  <read_first>
    - The resolved decision from task 1, which is normative for every value below.
    - `internal/config/config.go` lines 150-190 — `optionTable`: the project's way of expressing a policy as a reviewable table with a per-entry rationale, and the refusal-with-a-reason wording at lines 180-186 ("there is no value that disables the sudo gate", "a window longer than a session is not a window"). The deadline table and its refusal copy this register exactly.
    - `internal/auth/ratelimit.go` lines 140-170 — `backoff` written as a loop rather than a shift, and the overflow rationale; reuse the shape with this phase's constants.
    - `internal/audit/redact.go` lines 40-63 — the argument for an allowlist over a denylist, which the retry table restates for RPCs.
    - `internal/talos/client.go` and `internal/talos/maintenance.go` as written by task 2 — the single shared call path the gate installs on.
    - `docs/talossim.md` from plan 02-03 — the `ExpectedClient` column these tests assert against.
  </read_first>
  <behavior>
    - A call whose context has no deadline returns an error satisfying `errors.Is(err, talos.ErrNoDeadline)` and the sim records zero requests.
    - A call for an RPC with no entry in the deadline table is refused at init time or by a test, not given a silent default.
    - Under `flap_connection`, a fast-read call makes at most 3 attempts and the sim's request counter proves it.
    - Under `flap_connection`, a mutation makes exactly 1 attempt.
    - Given a call deadline shorter than the sum of the backoff delays, the attempt loop stops at the deadline rather than completing its attempt budget.
    - Backoff delays fall inside [0, 200ms] for the first retry and [0, 800ms] for the second, because the jitter is full.
  </behavior>
  <files>internal/talos/deadline.go, internal/talos/deadline_test.go, internal/talos/retry.go, internal/talos/retry_test.go, internal/talos/client.go</files>
  <action>
`deadline.go`. Express the confirmed policy as a reviewable table, not as scattered constants:
`type DeadlineClass int` with `ClassProbe`, `ClassFastRead`, `ClassMutation`, `ClassStream`;
a `map[string]DeadlineClass` keyed by the gRPC full method name; and the four durations as
named constants with the rationale attached — in particular that the mutation deadline bounds
the *call* and not the operation, because the long work is asynchronous on the node.

`func requireDeadline(ctx context.Context) error` is the structural gate: it returns
`ErrNoDeadline` when the context carries none, and its message is written in the
`config.go:180-186` register — it must say that there is no value that disables it and no
default-less path exists. `func WithClassDeadline(ctx context.Context, method string)
(context.Context, context.CancelFunc, error)` applies the class deadline and refuses a method
with no entry in the table. An unclassified RPC is a gap in the specification, not a
candidate for a default: the doc comment says so and a test enumerates the table against the
class memberships confirmed in task 1.

Install the gate on the single shared call path from task 2, so every method on both client
types passes through it and no method can opt out. The stream class gets no total deadline but
a first-byte deadline and an idle timeout instead, both named constants.

`retry.go`. `var retryable = map[string]bool{…}` — an **allowlist** keyed by full method name,
containing exactly the fast-read class. Copy `redact.go`'s argument for the direction into the
doc comment: a denylist of non-idempotent RPCs forgets the next mutating RPC Phase 8 adds,
while a forgotten allowlist entry costs a retry nobody gets. Include the three deliberate
read exclusions (`EtcdSnapshot`, `PacketCapture`, `DiskUsage`) with the reason on the entry —
they are reads, but expensive enough that a retry storm is its own outage.

`func do(ctx context.Context, method string, call func(context.Context) error) error` runs at
most 3 attempts, retrying only when the method is in the allowlist **and** the classified
error kind is unreachable — never on a rejection, because a node that answered is not a
transport failure. Backoff doubles 200 ms → 800 ms with full jitter, written as a loop rather
than a shift for the reason `ratelimit.go` gives. Every attempt runs inside the original
deadline: compute the remaining budget before sleeping and abandon the loop when the next
delay would exceed it. That is D-04's "retries never extend a call's deadline" and it is the
property the test with a deliberately short deadline proves.

Tests first, from the behaviour block, all driven against `talossim` with the relevant
scenario injected and asserted on the sim's request counters rather than on client errors.
  </action>
  <acceptance_criteria>
    - `internal/talos/deadline.go` contains `ErrNoDeadline` and a table mapping gRPC full method names to a deadline class
    - `internal/talos/retry.go` declares the allowlist as a map whose entries are the fast-read class, and the three deliberate read exclusions each carry a comment
    - `go test ./internal/talos/ -run 'TestRequireDeadline|TestClassTable|TestRetry' -count=1 -race` exits 0
    - A test issues a call on a context with no deadline and asserts `errors.Is(err, talos.ErrNoDeadline)` and that the sim recorded zero requests
    - A test under `flap_connection` asserts the sim's request counter is at most 3 for a fast read and exactly 1 for a mutation
    - A test with a call deadline shorter than the backoff sum asserts the loop stopped at the deadline rather than completing its attempt budget
    - A test asserts every RPC named in the confirmed deadline policy has a class-table entry
    - `go test ./... -count=1 -race` exits 0 and `golangci-lint run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/talos/ ./internal/talossim/ -count=1 -race</automated>
  </verify>
  <done>Every node call carries a class deadline structurally, and only allowlisted reads retry — inside the original budget, proven at the sim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: One dead node costs one dead node — breaker, fan-out, and the contract suite</name>
  <read_first>
    - `internal/auth/ratelimit.go` in full — `Limiter`: per-key state under one mutex, `Delay` at lines 95-112, `Len()` at lines 113-118 ("It exists so a test can assert the map does not grow without bound"), and the no-goroutine `sweep` justification at lines 126-131. The per-node breaker is the same data structure keyed by machine id instead of source IP, and the justification transfers verbatim.
    - `internal/talos/errors.go` from task 2 — the unreachable/rejected distinction that decides whether the breaker counts a failure.
    - `internal/talossim/scenario.go` and `docs/talossim.md` from plan 02-03 — `Registry` and the `ExpectedClient` column the contract suite asserts against.
    - `02-CONTEXT.md` `<code_context>` — the warning that audit is fail-closed and fsyncs per record under one global mutex, sized on "a handful per minute", so a per-node fan-out that audits per node makes contention a *mutation failure* rather than latency.
    - `golang.org/x/sync` is already a direct module requirement; check whether `errgroup` or `semaphore` fits the fan-out before writing goroutine bookkeeping by hand.
  </read_first>
  <behavior>
    - After the configured consecutive unreachable failures for one machine, the next call short-circuits with `ErrCircuitOpen` and the sim records no additional request.
    - A `KindRejected` failure does not advance the breaker's failure count.
    - After the cooldown elapses the breaker allows one trial call; a success resets it, a failure re-opens it.
    - `Breaker.Len()` after a sweep excludes machines that have not failed within the retention window.
    - A fan-out over one silent target and one healthy target returns the healthy result within the healthy call's own deadline, and reports the silent one as unreachable.
    - Cancelling the fan-out's context terminates every in-flight per-node call.
    - The fan-out issues no audit record per node; it returns per-node results and leaves recording to its caller.
  </behavior>
  <files>internal/talos/breaker.go, internal/talos/breaker_test.go, internal/talos/fanout.go, internal/talos/fanout_test.go, internal/talos/contract_test.go</files>
  <action>
`breaker.go`. `type Breaker struct` holding `map[model.MachineID]*nodeState` under one mutex,
plus the injected `now func() time.Time` clock the rest of the package already uses. Methods
`Allow(model.MachineID) error` returning `ErrCircuitOpen` when the circuit is open,
`Success(model.MachineID)`, `Fail(model.MachineID)` and `Len() int`. Only a `KindUnreachable`
failure calls `Fail` — the doc comment says why, because that is the `etcd_down` case: a node
that answers with `Unavailable` for one subsystem is reachable, and opening its circuit would
turn one broken subsystem into a node-wide outage. Sweep from `Fail`, because `Fail` is the
only method that adds an entry, so the map cannot grow between sweeps; copy `ratelimit.go`'s
argument against a background goroutine verbatim in spirit, and copy `Len()` and its stated
reason for existing.

`fanout.go`. `func FanOut[T any](ctx context.Context, targets []Target, call func(context.Context, Target) (T, error)) []Result[T]`
where `Result[T]` carries the target, the value and the error. One goroutine per target, each
with its own class deadline, each consulting the breaker before dialing. Bound the concurrency
with a named constant so a hundred-node inventory does not open a hundred connections at once,
and use `golang.org/x/sync` rather than hand-rolled goroutine bookkeeping if it fits — the
module is already a direct requirement.

The function returns results and errors; it writes no audit record and takes no audit
dependency. That is deliberate and the doc comment says so: audit fsyncs per record under one
global mutex and is fail-closed, so a fan-out that audited per node would turn contention into
mutation failure. Recording an operation is the caller's single decision, not N.

`contract_test.go`. The client-side half of the TRANS-07 contract suite and the payoff for
plan 02-03. Iterate `talossim.Registry` — do not hand-list the scenarios, so a scenario added
there without an assertion here fails this suite — and for each one inject it, exercise the
production client, and assert the behaviour written in that entry's `ExpectedClient` column.
Every assertion is built on a timing bound, on the sim's own counters, or on a classified
error kind. None is built on a bare `err != nil`, for the reason `docs/talossim.md` records
about `Client.EventsWatch` returning nil on `io.EOF`, `codes.Canceled` and
`codes.DeadlineExceeded`.

Include the concurrency assertion TRANS-01 and TRANS-05 both need: with `go_silent` injected
on target A and target B healthy, a fan-out over both returns B's result within B's own
deadline, and cancelling the context terminates A's in-flight call.

Name this file and its top-level test so Phase 3 can run the same suite against real Talos
(TRANS-08) by substituting the transport — the suite must take its `Dialer` and `Creds` from a
parameter rather than constructing a sim inline in every case.
  </action>
  <acceptance_criteria>
    - `internal/talos/breaker.go` contains `func (b *Breaker) Len() int` and `ErrCircuitOpen`
    - `internal/talos/fanout.go` contains `func FanOut[`
    - `go test ./internal/talos/ -run 'TestBreaker|TestFanOut' -count=1 -race` exits 0
    - A test asserts a `KindRejected` failure leaves the breaker's failure count unchanged while a `KindUnreachable` failure advances it
    - A test asserts `Breaker.Len()` drops after entries age past the retention window
    - `go test ./internal/talos/ -run TestScenarioContract -count=1 -race` exits 0 and iterates `talossim.Registry`, covering all nine entries
    - The contract suite fails when a registry entry has no matching assertion — verified once by adding a tenth registry entry temporarily; record the observation in the SUMMARY
    - A test asserts a fan-out over one `go_silent` target and one healthy target returns the healthy result within the healthy call's deadline plus a tolerance
    - `go test ./... -count=1 -race` exits 0 with no single test exceeding 30 seconds, and `golangci-lint run` exits 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/talos/ ./internal/talossim/ -count=1 -race</automated>
  </verify>
  <done>An unreachable node is bounded by its own breaker and its own goroutine, and every one of the nine scenarios has a running assertion against its documented expected behaviour.</done>
</task>

</tasks>

## Artifacts this phase produces

New types: `talos.MaintenanceClient`, `talos.Error`, `talos.ErrorKind`,
`talos.DeadlineClass`, `talos.Breaker`, `talos.Result[T]`.

New constants: `talos.KindUnreachable`, `talos.KindTimeout`, `talos.KindRejected`,
`talos.ClassProbe`, `talos.ClassFastRead`, `talos.ClassMutation`, `talos.ClassStream`, the
four deadline durations, the stream first-byte and idle constants, the retry attempt count
and backoff bounds, the breaker's consecutive-failure threshold and cooldown, and the
fan-out concurrency bound.

New error sentinel: `talos.ErrCircuitOpen`.

New functions and methods: `talos.NewMaintenanceClient`, `talos.WithClassDeadline`,
`talos.FanOut`, `(*talos.Breaker).Allow/Success/Fail/Len`,
`(*talos.MaintenanceClient).ApplyConfiguration/Version/Disks/COSI/Close`, and the unexported
`requireDeadline`, `classify`, `do` and the shared connect helper.

New tests: `TestMaintenanceClientRejectsClusterOnlyCall`, `TestErrorKindMapping`,
`TestRequireDeadline`, `TestClassTable`, `TestRetry*`, `TestBreaker*`, `TestFanOut*`,
`TestScenarioContract`.

No new HTTP routes, no new RFC 9457 problem types (the `upstream` family was minted in plan
02-02 and its `upstream.node-*` tokens are wired to a handler in Phase 6), no new CLI flags,
no new module requirements.

## Flagged assumptions

1. **Probe-report rows owned by this plan: TRANS-03, TRANS-04, TRANS-05, and the explicit
   half of TRANS-01.** All returned `unclassified` / `unresolved` from the deterministic edge
   probe (TRANS-01 was classified `concurrency` but left unresolved). **Classifier language
   miss** — English-keyword classifier, German requirement text. Each was hand-derived into
   `must_haves.truths` above. TRANS-01's concurrency truth, authored as `backstop` in plan
   02-01 because no fan-out existed yet, becomes an explicit truth here.
2. **The deadline and retry policy is unconfirmed until task 1 resolves.** `02-CONTEXT.md`
   marks it "PROPOSED, awaiting confirmation" and notes one conflict it creates. Tasks 2 to 4
   implement it exactly as written; if the checkpoint changes anything, plan 02-03's
   `ExpectedClient` column and `docs/talossim.md` change in the same commit, because the
   contract suite in task 4 reads them.
3. **The `WriteTimeout` conflict is real, unresolved, and deliberately not fixed here.**
   `cmd/holzkubed/main.go` sets `WriteTimeout = 60 s` process-wide with reasoning scoped to
   argon2id and the login rate limiter; streams do not fit, and none of the three
   `ResponseWriter` wrappers implements `Flush`, `Unwrap` or `Hijack`, so a streaming endpoint
   would silently buffer. `02-CONTEXT.md` `<deferred>` assigns the redesign to Phase 5. Phase 2
   exposes no streaming HTTP route, so nothing here is papered over — but this must be
   recorded as a **Phase 5 entry requirement** in the SUMMARY and in STATE.md's blockers.
4. **The `upstream.node-*` code tokens get no production consumer in this phase.** They are
   minted and reserved in plan 02-02 and held honest by `TestErrorKindMapping`; the HTTP
   adapter that maps a `talos.Error` onto them arrives in Phase 6 with the first route that
   drives a node call. Recorded so a reviewer does not read the absence as an omission.
5. **`internal/talos` production code must not import `internal/httpapi`.** Only
   `errors_test.go` does, and only to assert the reserved-token correspondence. A production
   import would invert the layering and is the shape that later becomes a cycle.
6. **This plan declares a dependency on 02-02, which wave order alone already satisfied.**
   `errors.go` and `TestErrorKindMapping` code against the reserved `upstream.node-*` tokens,
   and those tokens are the *output of 02-02's blocking checkpoint* — so the coupling is to a
   decision, not merely to a file. Wave 4 > wave 1 makes it work today; the declaration is what
   keeps it working if the phase is ever re-sliced or this plan is re-run alone.
7. **This plan also declares 02-08 explicitly, though 02-03 already reaches it.** The contract
   suite calls sim methods 02-08 implements. A transitive path through 02-03 exists, but the
   direct coupling is real and is written down for the same reason as the row above.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| holzkube -> node (per-call, per-node) | every call crosses into a network the product does not control |
| maintenance credentials -> node | an unauthenticated, certificate-unverified channel by design |
| transport error -> HTTP response | a failure detail travels outward toward a browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-26 | Elevation of privilege | `MaintenanceClient` accepting cluster credentials | high | mitigate | both constructors validate `Creds.Kind` and refuse the wrong one with a named error; the method set is non-overlapping so a cluster-only call is a compile error (D-06) |
| T-02-27 | Spoofing | maintenance mode's `InsecureSkipVerify` connection | high | mitigate | the insecure TLS config is confined to `NewMaintenanceClient` and is unreachable from the cluster path; `Creds.Fingerprint` exists on the seam so a later phase can pin the maintenance certificate without a signature change; a test asserts no cluster-credential path produces a `tls.Config` with verification disabled |
| T-02-28 | Denial of service | a retry storm against a struggling node | high | mitigate | retries are allowlisted to reads, capped at 2, full-jittered, bounded by the original deadline, and further limited by the per-node breaker; `EtcdSnapshot`, `PacketCapture` and `DiskUsage` are excluded by name |
| T-02-29 | Denial of service | one unreachable node stalling the whole product | high | mitigate | per-node fan-out with per-node deadlines and a per-node breaker; the contract suite asserts a healthy target's call completes within its own deadline while a silent target is pending |
| T-02-30 | Denial of service | unbounded growth of the breaker map | medium | mitigate | the sweep runs from `Fail`, the only method that adds entries, and `Len()` exists so a test can assert the map does not grow without bound |
| T-02-31 | Denial of service | audit contention under fan-out | high | mitigate | `FanOut` takes no audit dependency and writes no per-node record; recording is the caller's single decision, which keeps the fail-closed, globally-mutexed, per-record-fsyncing writer at the rate it was sized for |
| T-02-32 | Information disclosure | node addresses in an error that reaches a browser | medium | mitigate | `Error.Error()` names the operation and the machine id and not the address, the same class of string `ChainStatus.Public()` already strips; asserted by a test |
| T-02-33 | Repudiation | an unclassified RPC silently taking a default deadline | medium | mitigate | `WithClassDeadline` refuses a method absent from the class table, and a test enumerates the table against the confirmed policy |
| T-02-SC | Tampering | package-manager installs | low | accept | this plan adds no module requirement; any use of `golang.org/x/sync` is of an already-present direct requirement |
</threat_model>

<verification>
- `task test` (`go test ./... -count=1 -race`) is green, with no single test exceeding 30 seconds
- `task lint:go` is green at zero findings
- `TestScenarioContract` covers all nine registry entries and would fail on a tenth without an assertion
- `go list -deps ./internal/talos` does not include `internal/httpapi`
</verification>

<success_criteria>
- An unreachable node blocks neither the UI nor another node (TRANS-05, ROADMAP success criterion 3)
- Every call carries an enforced deadline and retries exist only for a read allowlist (TRANS-04)
- Cluster and maintenance clients are separate types that cannot be confused (TRANS-03)
- Every one of the nine scenarios has a running assertion against its documented expected behaviour (ROADMAP success criterion 2)
</success_criteria>

<output>
Create `.planning/phases/02-transport-seam-talossim-image-factory/02-05-SUMMARY.md` when done
</output>
